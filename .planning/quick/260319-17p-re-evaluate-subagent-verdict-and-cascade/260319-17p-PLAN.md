---
phase: 260319-17p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md
autonomous: true
requirements: [QUICK-260319-17p]

must_haves:
  truths:
    - "Concept 7 verdict is CONDITIONAL GO (hybrid architecture) not NO-GO"
    - "Concept 7 stress-test cites real constraints (SubagentStop gap, bootstrap overhead, rate limits) not false claims about hook capabilities"
    - "Concept 7 steel-man cites additionalContext injection, custom subagent definition with model selection, and persistent memory"
    - "Consolidated verdict table shows 1 GO, 4 CONDITIONAL GO, 1 DEFER, 1 NO-GO (was 1 GO, 3 CONDITIONAL GO, 1 DEFER, 2 NO-GO)"
    - "Track B Section 1 pipelines show hybrid command-hook + subagent architecture for deliberation path"
    - "Track B Section 4 replaces 'pure CJS + direct API' with hybrid pattern including custom subagent definition"
    - "Track B Section 6 cost model reflects subscription-included subagent costs alongside per-API-call billing"
    - "Concepts 1-6 verdicts are preserved unchanged unless the corrected subagent capabilities change their analysis"
    - "All correct content from the original document is preserved"
  artifacts:
    - path: ".planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md"
      provides: "Corrected steel-man analysis with hybrid subagent architecture"
      contains: "CONDITIONAL GO"
  key_links:
    - from: ".planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md"
      to: ".planning/quick/260319-17p-re-evaluate-subagent-verdict-and-cascade/260319-17p-RESEARCH.md"
      via: "Research findings cited as corrections to previous false claims"
      pattern: "additionalContext|SubagentStart|SubagentStop|hybrid"
    - from: ".planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md"
      to: ".planning/research/INNER-VOICE-SYNTHESIS-v2.md"
      via: "Source concepts being re-evaluated"
      pattern: "Synthesis v2 Section 2"
---

<objective>
Rewrite INNER-VOICE-SYNTHESIS-RESEARCH.md to correct the false NO-GO verdict on Concept 7 (Claude Code native subagent implementation) and cascade corrections through all downstream sections affected by the flawed premise.

Purpose: The existing document gave Concept 7 a NO-GO based on incorrect claims: (1) hooks cannot inject arbitrary content, (2) `claude -p` is the only subagent path with 5-15s cold start, (3) cost model is equivalent. New research proves hooks CAN inject via `additionalContext`, custom subagents CAN be defined with model selection/tools/persistent memory, and the correct architecture is a HYBRID (command hooks for hot path + custom subagents for deliberation). On subscription plans, subagent processing is included -- materially changing the cost model.

Output: Corrected `.planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` with Concept 7 as CONDITIONAL GO and all downstream sections updated to reflect the hybrid architecture.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md (TARGET FILE -- read FIRST, this is the 1,040-line document to correct)
@.planning/quick/260319-17p-re-evaluate-subagent-verdict-and-cascade/260319-17p-RESEARCH.md (CORRECTED RESEARCH -- source of truth for what hooks/subagents can actually do)
@.planning/research/INNER-VOICE-SYNTHESIS-v2.md (original synthesis proposing subagent substrate -- Section 2 is the concept being re-evaluated)
@.planning/research/INNER-VOICE-SPEC.md (spec sections referenced throughout)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct Concept 7 verdict and cascade through Track A and consolidated table</name>
  <files>.planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md</files>
  <action>
Read the full existing document. Then rewrite the following sections IN PLACE, preserving all surrounding content exactly:

**A) Concept 7 section (lines ~372-428) -- FULL REWRITE:**

1. **Steel-Man Case:** Rewrite to cite ACTUAL capabilities from 260319-17p-RESEARCH.md:
   - Hooks CAN inject arbitrary content via `additionalContext` field in `hookSpecificOutput` (UserPromptSubmit, SessionStart, SubagentStart events)
   - Custom subagents can be defined in `.claude/agents/` with model selection (haiku/sonnet/opus), tool access (Read, Grep, Glob, Bash), filesystem access, isolated context windows, persistent memory, MCP server access, background execution
   - SubagentStart hook can inject state directly into subagent context at spawn time
   - On Pro/Max subscription plans, subagent processing is included in subscription (not per-token API billing)
   - The subagent model aligns with dual-process architecture: main session IS System 2, Inner Voice subagent IS System 1
   - Eliminates need for ANTHROPIC_API_KEY dependency, custom HTTP client code, error handling, retry logic for subscription users

2. **Stress-Test:** Rewrite with REAL constraints from the corrected research:
   - Hot path (<500ms) CANNOT be served by subagents due to 2-8 second bootstrap overhead (5,000-15,000 token context bootstrapping). Command hooks with deterministic processing remain mandatory for hot path.
   - SubagentStop CANNOT inject content directly back into parent context (GitHub issue #5812, `additionalParentContext` feature request closed as NOT_PLANNED). Workaround: write to state file during SubagentStop, read in next UserPromptSubmit hook. This is functional but indirect.
   - Subagents cannot spawn other subagents (no nested delegation). All Inner Voice processing must happen within single subagent context.
   - Rate limit interaction on subscription plans: Inner Voice subagent competes for same rate limits as main session. Heavy processing could cause rate limit errors for primary work.
   - Background subagent + UserPromptSubmit race condition: if subagent runs in background and writes results to disk, next hook might fire before subagent completes. State file needs "processing" flag.
   - For API plan users, subagent bootstrap overhead makes hot path MORE expensive than direct API calls.
   - The hybrid architecture adds complexity: two invocation paths (CJS command hooks + subagent definition) vs single path (CJS only).

3. **Verdict:** Change from NO-GO to CONDITIONAL GO (Confidence: HIGH). The hybrid architecture -- command hooks for hot path, custom subagents for deliberation -- is sound. Conditions:
   - Hot path MUST remain as CJS command hooks with deterministic processing and `additionalContext` injection
   - Deliberation path and REM consolidation CAN use custom subagent with Sonnet model selection
   - State file bridge pattern (SubagentStop writes, UserPromptSubmit reads) must be implemented for indirect context injection
   - For API plan users, direct API calls remain the fallback (subagent overhead not justified for hot path)
   - Implementation must handle the SubagentStop-to-parent context gap gracefully

4. **Integration Recommendation:** Replace the "Pure CJS with direct API calls" section with hybrid architecture:
   - v1.3: Hot path as CJS command hooks (unchanged). Deliberation path as EITHER direct API calls OR custom subagent (configurable based on billing model). REM consolidation can use subagent.
   - Define `~/.claude/agents/inner-voice.md` custom subagent with: model: sonnet, tools: Read/Grep/Glob/Bash, disallowedTools: Write/Edit/Agent, permissionMode: dontAsk, maxTurns: 10, memory: user
   - SubagentStart hook injects current IV state into subagent context
   - SubagentStop hook writes results to state file for next UserPromptSubmit pickup
   - CORTEX-07 (Agent coordination, v1.5) remains valid but v1.3 can now use native subagents for non-latency-critical paths

**B) Consolidated Verdict Table (lines ~432-451) -- UPDATE:**
- Change Concept 7 row from "NO-GO | HIGH | Never | Hooks cannot spawn subagents..." to "CONDITIONAL GO | HIGH | v1.3 (hybrid) | Hot path: CJS command hooks. Deliberation: custom subagent. Conditions: SubagentStop gap bridged via state file."
- Update summary counts: 1 GO, 4 CONDITIONAL GO, 1 DEFER, 1 NO-GO (was 1 GO, 3 CONDITIONAL GO, 1 DEFER, 2 NO-GO)
- Update "Surviving concepts for v1.3" to include subagent (hybrid) alongside frame-first and REM
- Update "Killed concepts" to remove "Claude Code native subagent implementation" -- only "Variable substitution debiasing" remains killed
- Add note that Concept 7 is a HYBRID implementation, not the pure subagent approach from Synthesis v2

**C) Check Concepts 1-6 for cascade impacts from corrected subagent capabilities:**
- Concept 1 (Frame-first): No change needed. Domain classification is deterministic; subagent does not affect it.
- Concept 2 (User-relative definitions): No change to DEFER verdict. Graph density is the blocker, not implementation substrate.
- Concept 3 (Variable substitution): No change to NO-GO. The technique is fundamentally flawed regardless of substrate.
- Concept 4 (IV Memory): Check if subagent persistent memory feature (`memory: user`) affects the analysis. If so, add a note that the subagent's native persistent memory could supplement or partially replace the custom IV memory JSON schema. The verdict (CONDITIONAL GO for v1.4) should remain unchanged but the integration recommendation may gain an additional option.
- Concept 5 (REM Consolidation): The GO verdict is strengthened -- REM Tier 3 (Stop hook) can now optionally use a custom subagent for deep synthesis instead of direct API call. Add a brief note to the integration recommendation.
- Concept 6 (Scalar compute): No change. The concept is about fan-out parallelism, not invocation substrate.

**D) Update document header metadata:**
- Change confidence note to reflect that Concept 7 verdict changed
- Update date to reflect the correction (add note: "Corrected 2026-03-19: Concept 7 re-evaluated from NO-GO to CONDITIONAL GO based on corrected research")
- Update lineage to include 260319-17p-RESEARCH.md
  </action>
  <verify>
    <automated>grep "Concept 7" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md | grep -q "CONDITIONAL GO" && grep "Summary counts" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md | grep -q "4 CONDITIONAL GO" && grep "Summary counts" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md | grep -q "1 NO-GO" && ! grep "Concept 7" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md | grep -q "NO-GO" && echo "PASS: Verdict counts correct" || echo "FAIL: Verdict counts wrong"</automated>
  </verify>
  <done>Concept 7 verdict is CONDITIONAL GO with hybrid architecture. Consolidated table shows 1 GO, 4 CONDITIONAL GO, 1 DEFER, 1 NO-GO. All concepts 1-6 checked for cascade impacts. Document header updated with correction note.</done>
</task>

<task type="auto">
  <name>Task 2: Cascade corrections through Track B sections affected by false NO-GO</name>
  <files>.planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md</files>
  <action>
Continuing the rewrite of the same file, update all Track B sections that referenced the "pure CJS + direct API calls" premise or the Concept 7 NO-GO. Preserve all content that was NOT based on the false premise.

**Track B Section 1 (Revised Processing Pipelines, lines ~456-569):**

Update the following pipeline steps that currently say "direct HTTP API" or "Concept 7 replacement":

- UserPromptSubmit pipeline step 7 DELIBERATION PATH: Change from "Call Haiku/Sonnet via direct HTTP API [MODIFIED - Concept 7 replacement]" to show the hybrid: "Call Haiku/Sonnet via EITHER direct HTTP API (API plan users) OR trigger custom subagent (subscription users) [MODIFIED - Concept 7 hybrid]. Subagent results available on next UserPromptSubmit via state file bridge."
- UserPromptSubmit pipeline "Changes from spec" note: Update to reflect hybrid instead of "direct API calls replacing subagent"
- SessionStart pipeline step 4: Change "direct Sonnet API call [MODIFIED - Concept 7 replacement]" to "Sonnet via direct API call OR custom subagent [MODIFIED - Concept 7 hybrid]"
- SessionStart "Changes from spec" note: Update similarly
- Stop pipeline step 2a: Change "Sonnet via direct API [MODIFIED - Concept 7 replacement]" to "Sonnet via direct API OR custom subagent [MODIFIED - Concept 7 hybrid]"
- Stop "Changes from spec" note: Update similarly
- All "[MODIFIED - Concept 7 replacement]" annotations should become "[MODIFIED - Concept 7 hybrid]"

**Track B Section 4 (Inner Voice Invocation Pattern, lines ~786-903):**

This section needs the most significant rewrite. Currently titled with "Definitive architecture per research findings" and starts with "pure CJS modules making direct HTTP API calls."

Rewrite to present the HYBRID architecture:

1. Replace the "Why Not Subagent" subsection with "Hybrid Architecture: Command Hooks + Custom Subagent" that explains:
   - Hot path: CJS command hooks with `additionalContext` injection (unchanged -- latency demands it)
   - Deliberation path: Custom `inner-voice` subagent with Sonnet model, spawned by main session via Agent tool
   - REM consolidation: Custom subagent OR direct API call (both viable, subagent preferred for subscription users)
   - State bridge: SubagentStop writes results to state file, UserPromptSubmit reads on next invocation

2. Add the custom subagent definition from 260319-17p-RESEARCH.md (the YAML frontmatter example for `~/.claude/agents/inner-voice.md`)

3. Update the Module Structure to ADD the subagent definition file:
   ```
   ~/.claude/agents/
     inner-voice.md              <- Custom subagent definition (NEW)
   ```
   Keep the existing CJS module structure under `~/.claude/dynamo/ledger/`

4. Update the Hook Handler Pattern to show `additionalContext` JSON response format instead of plain text return:
   ```javascript
   // Return injection via additionalContext for model-visible injection
   return JSON.stringify({
     hookSpecificOutput: {
       hookEventName: "UserPromptSubmit",
       additionalContext: result.injection || ""
     }
   });
   ```

5. Keep the Direct API Call Pattern subsection but reframe it as "Direct API Call Pattern (API plan fallback)" and note it is used when subagent is not preferred (API plan users, or hot-path-only mode)

6. Add a new subsection "Subagent Invocation Pattern" showing how the deliberation path triggers the custom subagent, and how SubagentStart/SubagentStop hooks bridge state

7. Remove the block quote "The research finding that killed Synthesis v2 Section 2's substrate assumption" and the quote from "260318-x21-RESEARCH.md, Primary recommendation" since that recommendation was based on incorrect research

**Track B Section 5 (Roadmap Impact Assessment, lines ~905-983):**

Update CORTEX-01 recommended addition text:
- Remove: "Inner Voice implemented as pure CJS modules with direct Anthropic API calls -- NOT as Claude Code subagent (Synthesis v2 Concept 7, NO-GO; confirmed by research)"
- Replace with: "Inner Voice implemented as hybrid architecture: CJS command hooks for hot path with `additionalContext` injection + custom subagent definition for deliberation path (Synthesis v2 Concept 7, CONDITIONAL GO; revised architecture per corrected research 260319-17p)"

Update "Cost Projection Revisions" paragraph:
- The statement "The subagent NO-GO verdict means no new infrastructure cost" must be corrected
- Replace with note that the subagent definition file is minimal infrastructure (a markdown file) and subscription users may see reduced costs since subagent processing is subscription-included

**Track B Section 6 (Updated Cost Model, lines ~986-1028):**

Update the cost model to reflect the dual billing paths:

1. Add a row or column distinguishing "API plan" vs "Subscription plan" costs for operations that can use subagents (deliberation, session start briefing, stop synthesis)

2. Update "Key Cost Insights" point 4: The statement "The CJS+API pattern (vs subagent cost model) is equivalent" is WRONG. Replace with: "For subscription plan users (Pro/Max), subagent-based deliberation and REM consolidation are included in subscription at no additional per-token cost. For API plan users, direct API calls remain the cost model. The hybrid architecture serves both billing models."

3. Add a new cost insight: "Subscription plan users can run the deliberation path and REM consolidation through the custom subagent at $0 additional marginal cost (subject to rate limits). This materially reduces the projected daily cost for subscription users. The $1.97/day v1.3 projection applies to API plan users; subscription users pay $0.37/day (hot path Haiku + deterministic processing only, with deliberation and REM covered by subscription)."

4. Update the monthly cost projection table to add subscription plan rows showing the reduced costs

**Footer/attribution (lines ~1031-1041):**

Update the "Analysis based on" list to add:
- 260319-17p-RESEARCH.md (corrected hook and subagent capabilities research)

Update the research date and validity period to reflect the correction.
  </action>
  <verify>
    <automated>! grep -q "Concept 7 replacement" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md && grep -q "Concept 7 hybrid" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md && ! grep -q "pure CJS" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md && grep -q "additionalContext" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md && grep -q "subscription" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md && echo "PASS: Track B cascaded correctly" || echo "FAIL: Track B cascade incomplete"</automated>
  </verify>
  <done>All Track B sections updated: pipelines show hybrid architecture, invocation pattern describes both CJS hooks and custom subagent, cost model distinguishes subscription vs API plan billing, no remaining "Concept 7 replacement" or "pure CJS" references from the false NO-GO premise.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full document:

1. `grep -c "NO-GO" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return exactly 1 (Concept 3 only, plus references in Concept 3's own section)
2. `grep "Concept 7" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md | grep "NO-GO"` should return zero lines (no Concept 7 NO-GO references remain)
3. `grep "pure CJS" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return zero lines
4. `grep "Concept 7 replacement" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return zero lines
5. `grep "additionalContext" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return multiple lines
6. `grep "hybrid" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return multiple lines
7. `grep "SubagentStart\|SubagentStop" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return lines showing the lifecycle hooks
8. `grep "subscription" .planning/research/INNER-VOICE-SYNTHESIS-RESEARCH.md` should return lines in the cost model section
9. Document should still contain all 7 concept sections, all Track B sections 1-6
10. Concepts 1-6 verdicts should be unchanged: CONDITIONAL GO, DEFER, NO-GO, CONDITIONAL GO, GO, CONDITIONAL GO
</verification>

<success_criteria>
- Concept 7 verdict changed from NO-GO to CONDITIONAL GO with hybrid architecture
- All false claims about hook/subagent capabilities removed and replaced with verified facts
- Track B Sections 1, 4, 5, 6 updated to reflect hybrid architecture
- Cost model distinguishes subscription vs API plan billing
- Concepts 1-6 verdicts preserved (with minor notes where subagent capabilities add options)
- Document remains self-contained and internally consistent
- No orphaned references to the old "pure CJS + direct API" as the sole architecture
</success_criteria>

<output>
After completion, create `.planning/quick/260319-17p-re-evaluate-subagent-verdict-and-cascade/260319-17p-SUMMARY.md`
</output>
