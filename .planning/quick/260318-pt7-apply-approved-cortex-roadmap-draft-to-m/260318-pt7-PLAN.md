---
phase: quick
plan: 260318-pt7
type: execute
wave: 1
depends_on: []
files_modified:
  - MASTER-ROADMAP.md
autonomous: true
requirements: ["ROADMAP-UPDATE"]

must_haves:
  truths:
    - "MASTER-ROADMAP.md contains all Cortex requirements (CORTEX-01 through CORTEX-11)"
    - "No [CORTEX], [RECONCILED], or [CORTEX REVISED] tags remain in the file"
    - "No reconciliation changelog or draft tracking sections remain"
    - "No DRAFT language appears in the document"
    - "The requirement index includes all CORTEX-XX entries alongside existing MENH/MGMT/UI/STAB entries"
    - "The guiding principles include the four Cortex principles without tags"
    - "Last updated date reflects today (2026-03-18)"
  artifacts:
    - path: "MASTER-ROADMAP.md"
      provides: "Canonical project roadmap with Cortex integration"
      contains: "CORTEX-01"
  key_links: []
---

<objective>
Apply the user-approved Cortex roadmap draft (MASTER-ROADMAP-DRAFT-v1.3-cortex.md) to the live MASTER-ROADMAP.md, cleaning up all draft-tracking artifacts in the process.

Purpose: The draft has been researched, reconciled with the Inner Voice spec, and approved by the user. The live roadmap must now reflect the Cortex integration so all future planning reads the correct milestone structure and requirements.

Output: Updated MASTER-ROADMAP.md with clean, production-ready content.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/MASTER-ROADMAP-DRAFT-v1.3-cortex.md
@MASTER-ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply cleaned Cortex draft to MASTER-ROADMAP.md</name>
  <files>MASTER-ROADMAP.md</files>
  <action>
Read `.planning/research/MASTER-ROADMAP-DRAFT-v1.3-cortex.md` as the source. Write its content to `MASTER-ROADMAP.md` with the following cleanups applied:

**1. Remove the DRAFT blockquote header (lines 3-5 of draft):**
Delete the entire blockquote that starts with "> **DRAFT -- Proposed revision..." through "> Changes from the current roadmap are marked with `[CORTEX]`." These three lines are draft tracking only.

**2. Update the "How to Use" note:**
Change the requirement ID list from `(MENH-XX, MGMT-XX, UI-XX, STAB-XX)` to `(MENH-XX, MGMT-XX, UI-XX, STAB-XX, CORTEX-XX)` to reflect the new requirement namespace.

**3. Remove the Cortex Integration blockquote (line 13 of draft):**
Delete the `> **[CORTEX] Ledger Cortex Integration:** ...` paragraph. This was a draft-level explanation of how Cortex was being integrated. The live roadmap should not contain meta-commentary about its own changes.

**4. Strip all `[CORTEX]` tags from the content:**
Remove every occurrence of `` `[CORTEX]` `` from the text. This includes:
- Inline tags in table cells (e.g., `` `[CORTEX]` Inner Voice... `` becomes `Inner Voice...`)
- Tags in section headers (e.g., `` #### `[CORTEX]` Ledger Cortex Requirements `` becomes `#### Ledger Cortex Requirements`)
- Tags in guiding principles (e.g., `` - `[CORTEX]` **Prove before scaling.** `` becomes `- **Prove before scaling.**`)
- Tags in the Requirement Index Notes column
- The plain text `[CORTEX]` (without backticks) in the Guiding Principles additions note -- remove the entire note about "Four new principles added" since it was draft context

**5. Strip all `[RECONCILED]` tags from the content:**
Remove every occurrence of `` `[RECONCILED]` `` from the text. Some lines have both `[CORTEX]` and `[RECONCILED]` tags -- remove both. The reconciliation notes embedded inline (like "Inner Voice v1.3 Artifacts:..." blocks) should REMAIN -- they are substantive content, not tracking artifacts. Only the `` `[RECONCILED]` `` prefix tag itself is removed.

**6. Strip all `[CORTEX REVISED]` tags from section headers:**
- `### v1.3 -- Intelligent Memory and Modularity \`[CORTEX REVISED]\`` becomes `### v1.3 -- Intelligent Memory and Modularity`
- Same for v1.4, v1.5, v2.0 headers

**7. Remove the "Removed from v1.4" note:**
Delete the line `` `[CORTEX]` **Removed from v1.4:** MGMT-09 (Human cognition patterns) -- moved to v1.3 as Inner Voice component. `` This was tracking a change between draft versions.

**8. Remove the Inner Voice milestone annotation blocks:**
Delete the standalone `[RECONCILED]` annotation blocks at the top of v1.3, v1.4, v1.5, and v2.0 sections (the ones that start with `**Inner Voice v1.X Artifacts:**` or `**Inner Voice v1.X Advancements:**` or `**Inner Voice v1.X:**`). These were reconciliation notes tracking what the Inner Voice spec contributed to each milestone. The substantive content from these blocks is already incorporated into the requirement descriptions and goal text.

**9. Delete the entire Change Log section:**
Remove everything from `## Change Log: Differences from Current MASTER-ROADMAP.md` through the end of that section (including all subsections: "Requirements Moved Between Milestones", "New CORTEX Requirements Added", "Existing Requirements Absorbed", "Theme/Goal Text Changes", "Requirement Count Changes", "Guiding Principles Additions", "Reconciliation with Inner Voice Spec").

**10. Delete the draft footer:**
Remove the final lines starting with `*DRAFT -- Proposed revision created:...` through `*Source: .planning/REQUIREMENTS.md + ...`

**11. Update the footer:**
Replace with a clean footer matching the current roadmap's style:
```
---
*Master Roadmap created: 2026-03-18*
*Last updated: 2026-03-18 -- Cortex integration (Ledger Cortex phased delivery, CORTEX-01 through CORTEX-11)*
*Source: .planning/REQUIREMENTS.md + .planning/research/LEDGER-CORTEX-ANALYSIS.md + .planning/research/INNER-VOICE-SPEC.md*
```

**12. Clean up column artifacts in Requirement Index:**
In the Requirement Index table, the Notes column entries like `NEW -- smart curation, semantic shift detection...` should be simplified. Remove the `NEW -- ` prefix and any remaining tag fragments. Keep the descriptive text.

**13. Verify "Last updated" date is 2026-03-18** (it already is in the draft).

**Important:** Do NOT remove the `Source` column from the Cortex requirement tables in v1.3, v1.4, v1.5, v2.0 -- those are part of the live data showing provenance (New, Absorbed, Moved). Do NOT remove the "Absorbed by CORTEX-XX" text in the Source column of absorbed requirements -- that is live requirement metadata.
  </action>
  <verify>
    <automated>
# Verify no draft tags remain
grep -c '\[CORTEX\]\|\[RECONCILED\]\|\[CORTEX REVISED\]' MASTER-ROADMAP.md && echo "FAIL: draft tags found" || echo "PASS: no draft tags"

# Verify no DRAFT language
grep -ci 'DRAFT' MASTER-ROADMAP.md && echo "FAIL: DRAFT text found" || echo "PASS: no DRAFT text"

# Verify CORTEX requirements present
grep -c 'CORTEX-01\|CORTEX-11' MASTER-ROADMAP.md | grep -q '0' && echo "FAIL: missing CORTEX reqs" || echo "PASS: CORTEX reqs present"

# Verify no Change Log section
grep -c 'Change Log: Differences' MASTER-ROADMAP.md && echo "FAIL: changelog still present" || echo "PASS: no changelog"

# Verify CORTEX-XX in How to Use note
grep 'CORTEX-XX' MASTER-ROADMAP.md && echo "PASS: CORTEX-XX in How to Use" || echo "FAIL: missing CORTEX-XX in How to Use"
    </automated>
  </verify>
  <done>
MASTER-ROADMAP.md contains the full Cortex-integrated roadmap with:
- All 11 CORTEX requirements properly placed in their milestones
- All absorbed/moved requirements documented with their new status
- Updated milestone themes, goals, and requirement counts
- Four new guiding principles integrated
- Complete requirement index with CORTEX-XX entries
- Zero [CORTEX], [RECONCILED], or [CORTEX REVISED] tags
- No reconciliation changelog or draft footer
- No DRAFT language anywhere in the document
- Clean footer with Cortex integration note
  </done>
</task>

</tasks>

<verification>
- `grep -c '\[CORTEX\]\|\[RECONCILED\]' MASTER-ROADMAP.md` returns 0
- `grep -c 'CORTEX-01' MASTER-ROADMAP.md` returns 2+ (milestone table + requirement index)
- `grep -c 'Change Log' MASTER-ROADMAP.md` returns 0
- `grep -ci 'DRAFT' MASTER-ROADMAP.md` returns 0
- `grep 'CORTEX-XX' MASTER-ROADMAP.md` finds the How to Use note
- File begins with `# Dynamo Master Roadmap` (no draft preamble)
</verification>

<success_criteria>
MASTER-ROADMAP.md is the canonical, clean roadmap with Cortex integration. No draft artifacts remain. All future planning reads from this file and sees the correct milestone structure (v1.2.1 through v2.0) with all CORTEX requirements properly assigned.
</success_criteria>

<output>
After completion, create `.planning/quick/260318-pt7-apply-approved-cortex-roadmap-draft-to-m/260318-pt7-SUMMARY.md`
</output>
