
1. dynamo node cli should be usable with bare `dynamo` invocation and not require including a node or node + path prefix. For dev or repo use, a flag override default pointer with repo version of dynamo.cjs should be supported.
2. might need to use this PR temporarily to support small_model for Graphiti
