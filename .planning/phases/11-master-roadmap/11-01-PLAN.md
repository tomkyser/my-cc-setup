---
phase: 11-master-roadmap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MASTER-ROADMAP.md
autonomous: true
requirements:
  - MRP-01
  - MRP-02

must_haves:
  truths:
    - "Every deferred requirement (MENH-01 through MENH-09, MGMT-01 through MGMT-10, UI-01 through UI-07) appears in the document under a specific milestone"
    - "The MASTER-ROADMAP.md file exists in the project root and is valid markdown"
    - "Each requirement assignment includes a brief rationale for why it belongs in that milestone"
    - "Milestones are ordered logically with dependency-aware sequencing (foundational capabilities before features that depend on them)"
  artifacts:
    - path: "MASTER-ROADMAP.md"
      provides: "Living roadmap for Dynamo post-v1.2"
      contains: "MENH-01"
  key_links:
    - from: "MASTER-ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement IDs referenced from Future Requirements section"
      pattern: "MENH-\\d{2}|MGMT-\\d{2}|UI-\\d{2}"
---

<objective>
Create the Dynamo Master Roadmap document that prioritizes all 26 deferred requirements from REQUIREMENTS.md and assigns them to future milestones (v1.3, v1.4, v1.5, v2.0).

Purpose: Provide a living reference document so future Claude Code sessions know what comes after v1.2 and in what order.
Output: MASTER-ROADMAP.md in the project root.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MASTER-ROADMAP.md with all 26 deferred requirements assigned to milestones</name>
  <files>MASTER-ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Create MASTER-ROADMAP.md in the project root with the following structure and content.

## Document Structure

The document must have these sections in order:

1. **Header** -- Title "Dynamo Master Roadmap", last-updated date, and a one-paragraph summary stating this covers v1.3 through v2.0 and that v1.0-v1.2 are complete.

2. **Milestone Overview** -- A table listing each milestone (v1.3, v1.4, v1.5, v2.0) with its theme, requirement count, and one-sentence description.

3. **Completed Milestones** -- A brief collapsed/summary section listing v1.0 (Research), v1.1 (Memory Fixes), v1.2 (CJS Foundation) as completed with their completion dates.

4. **Milestone Details** -- One section per future milestone, each containing:
   - Milestone version and theme name
   - One-paragraph goal statement
   - A table of requirements assigned to that milestone with columns: ID, Name, Rationale
   - Dependencies note (what must be complete before this milestone)

5. **Requirement Index** -- A single table mapping every deferred requirement ID to its assigned milestone for quick lookup.

6. **Guiding Principles** -- 3-5 bullets explaining the ordering rationale (build foundational capabilities first, memory quality before UI, etc.)

## Milestone Assignments

Use these specific assignments (derived from dependency analysis and the project's core value of self-manageability):

### v1.3 -- Intelligence and Modularity
Theme: Make the memory system smarter and the management system modular.
Depends on: v1.2 (CJS substrate operational)

Assign these 10 requirements:
- MENH-01 (Decision engine -- infer context type): Foundational for all memory quality improvements; the CJS substrate from v1.2 provides the hook points needed
- MENH-02 (Preload engine -- auto inference and injection): Builds directly on MENH-01's context type inference; together they make memory useful proactively
- MENH-06 (Support both API and native Haiku): Removes OpenRouter single-point-of-failure for curation; straightforward transport layer change on CJS
- MENH-07 (Support other model choices for curation): Natural extension of MENH-06; once transport is flexible, model selection follows
- MGMT-01 (Self-contained dependency management): Core self-manageability requirement; Dynamo should manage its own dependencies (GSD, Graphiti, etc.)
- MGMT-02 (Domain-specific on-demand modules): Enables skill loading (WPCS, Context7, Playwright); requires modular injection pattern from v1.2
- MGMT-03 (CC skill inference and internal use): Makes Claude Code aware of available skills; builds on MGMT-02's module system
- MGMT-05 (Hooks replacing CLAUDE.md for dynamic behavior): Architectural evolution -- hooks already exist from v1.2, this makes them the primary behavior mechanism
- MGMT-08 (Jailbreak/hijacking protection patterns): Security hardening of the hook system; should happen early while the system is well-understood
- MGMT-10 (Modular injection with better control): Refined injection control building on v1.2's CJS foundation; enables precise capability management

### v1.4 -- Memory Quality and Preferences
Theme: Make stored memories higher quality and let user preferences persist intelligently.
Depends on: v1.3 (intelligence layer and modular injection operational)

Assign these 7 requirements:
- MENH-03 (Memory synthesis and export): Requires MENH-01's context typing to know what to synthesize; produces shareable knowledge artifacts
- MENH-04 (Memory inference and understanding): Builds on MENH-01/02 intelligence; moves from storage to comprehension
- MENH-05 (Flat file support alongside Graph DB): Alternative storage backend for users who cannot run Docker/Neo4j; broadens accessibility
- MENH-08 (Native or local text embedding model): Removes dependency on external embedding APIs; improves latency and privacy
- MGMT-06 (Global CC preferences through memories): Requires v1.3's decision engine to correctly scope preference storage
- MGMT-07 (Project CC preferences through memories): Same dependency as MGMT-06 but project-scoped; natural pair
- MGMT-09 (Human cognition patterns as prompt engineering): Requires mature memory system to store and apply cognitive patterns effectively

### v1.5 -- Dashboard and Visibility
Theme: Give users visual insight into what Dynamo knows and does.
Depends on: v1.4 (memory quality and preferences stable enough to display)

Assign these 7 requirements:
- UI-01 (Global, Session, Project, Task views): Core navigation structure; must exist before specialized views
- UI-02 (Modular and insightful dashboard): Main landing page; requires UI-01's view structure
- UI-03 (Preload control): Visual control for MENH-02's preload engine; requires that engine to exist (v1.3)
- UI-04 (Memory system config): Settings interface for memory parameters; requires stable memory system (v1.4)
- UI-05 (Asset management and browser): Browse and manage stored knowledge artifacts; requires MENH-03's export (v1.4)
- UI-06 (Memory CRUD operations): Direct manipulation of memories; requires mature memory system with quality controls (v1.4)
- MGMT-04 (TweakCC integration): UI-adjacent tool for Claude Code behavior overrides; ships with dashboard for cohesive UX

### v2.0 -- Advanced Capabilities
Theme: Ambitious features requiring significant research and architectural decisions.
Depends on: v1.5 (stable, visible, well-understood system)

Assign these 2 requirements:
- MENH-09 (Council-style AI agent deliberation): Multi-agent reasoning; needs research, mature infrastructure, and proven memory quality from v1.4
- UI-07 (Memory with desktop app and mobile): Cross-platform UI; requires v1.5's web dashboard as foundation and stable API surface

## Formatting Requirements

- Use standard markdown (no HTML)
- Requirement IDs must appear exactly as defined in REQUIREMENTS.md (e.g., MENH-01, MGMT-01, UI-01)
- Each milestone section should use ### heading level
- The requirement index table must have columns: Requirement ID, Name, Milestone
- Include a "How to Use This Document" note at the top explaining this is a living document that Claude Code should read to understand future direction
  </action>
  <verify>
    <automated>test -f MASTER-ROADMAP.md && echo "File exists" && grep -c "MENH-0[1-9]" MASTER-ROADMAP.md && grep -c "MGMT-\(0[1-9]\|10\)" MASTER-ROADMAP.md && grep -c "UI-0[1-7]" MASTER-ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - MASTER-ROADMAP.md exists in the project root
    - All 9 MENH requirements appear: grep -c "MENH-0[1-9]" MASTER-ROADMAP.md returns 9 or more (each appears in milestone section + index)
    - All 10 MGMT requirements appear: grep -cE "MGMT-(0[1-9]|10)" MASTER-ROADMAP.md returns 10 or more
    - All 7 UI requirements appear: grep -c "UI-0[1-7]" MASTER-ROADMAP.md returns 7 or more
    - Four future milestones defined: grep -c "v1\.[345]\|v2\.0" MASTER-ROADMAP.md returns 4 or more
    - Document contains rationale: grep -c "Rationale" MASTER-ROADMAP.md returns 1 or more
    - Document references completed milestones: grep "v1\.0\|v1\.1\|v1\.2" MASTER-ROADMAP.md returns matches
  </acceptance_criteria>
  <done>MASTER-ROADMAP.md exists in the project root containing all 26 deferred requirements (9 MENH + 10 MGMT + 7 UI) each assigned to exactly one milestone (v1.3, v1.4, v1.5, or v2.0) with a brief rationale, plus a requirement index table and guiding principles section.</done>
</task>

</tasks>

<verification>
1. File existence: `test -f MASTER-ROADMAP.md`
2. All 26 requirements present: count unique requirement IDs in the file
3. Four milestones defined: grep for v1.3, v1.4, v1.5, v2.0
4. Each requirement has rationale text adjacent to its ID
5. Document is valid markdown (no broken formatting)
</verification>

<success_criteria>
- MASTER-ROADMAP.md exists in project root
- All 26 deferred requirements from REQUIREMENTS.md are assigned to milestones
- Each assignment includes rationale
- Milestones are logically ordered with dependency awareness
- Document is self-contained and readable by future Claude Code sessions
</success_criteria>

<output>
After completion, create `.planning/phases/11-master-roadmap/11-01-SUMMARY.md`
</output>
