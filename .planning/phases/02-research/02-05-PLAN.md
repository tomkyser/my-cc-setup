---
phase: 02-research
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-research/setup/GSD-LIFECYCLE.md
  - .planning/phases/02-research/setup/COEXISTENCE.md
autonomous: true
requirements: [GMGR-01, GMGR-02]
must_haves:
  truths:
    - "GSD lifecycle runbook covers all 6 operations (install, update with 6-step process, uninstall, version check, troubleshoot, health check) with exact commands CC can execute"
    - "Coexistence strategy documents all config file namespaces (mcpServers, hooks, permissions, env, plugins, skills) and identifies interaction risks with mitigations"
    - "Both documents are dual-audience: structured for CC to execute commands from, readable for user to review"
  artifacts:
    - path: ".planning/phases/02-research/setup/GSD-LIFECYCLE.md"
      provides: "GSD framework self-management lifecycle runbook"
      contains: "## GSD Framework"
    - path: ".planning/phases/02-research/setup/COEXISTENCE.md"
      provides: "Global scope coexistence strategy"
      contains: "## Coexistence Strategy"
  key_links:
    - from: "GSD-LIFECYCLE.md update section"
      to: "~/.claude/get-shit-done/workflows/update.md"
      via: "documents all 6 update steps from update.md"
      pattern: "npx get-shit-done-cc"
    - from: "COEXISTENCE.md config map"
      to: "~/.claude.json and ~/.claude/settings.json"
      via: "documents ownership and conflict zones"
      pattern: "mcpServers|hooks|permissions"
---

<objective>
Document the GSD framework self-management lifecycle (GMGR-01) and the global scope coexistence strategy (GMGR-02).

Purpose: GMGR-01 produces an operational runbook so CC can manage the GSD framework autonomously. GMGR-02 maps how all global tools (GSD, Graphiti, MCPs, plugins, skills) share the ~/.claude namespace without conflicts. Both feed into Phase 3's self-management lifecycle sections.

Output: Two documentation files in `.planning/phases/02-research/setup/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/02-research/02-CONTEXT.md
@.planning/phases/02-research/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document GSD framework self-management lifecycle (GMGR-01)</name>
  <files>.planning/phases/02-research/setup/GSD-LIFECYCLE.md</files>
  <read_first>
    - ~/.claude/get-shit-done/workflows/update.md (full 6-step update process — CRITICAL: must document all 6 steps, not just "run install again")
    - ~/.claude/get-shit-done/VERSION (current installed version)
    - .planning/phases/02-research/02-RESEARCH.md (GSD Self-Management Commands section, Pitfall 5 about missing update path details)
    - .planning/phases/02-research/02-CONTEXT.md (locked decision: operational runbook depth, dual audience)
  </read_first>
  <action>
Create `.planning/phases/02-research/setup/GSD-LIFECYCLE.md` as an operational runbook.

**Document structure (per locked decision — operational runbook depth):**

```markdown
## GSD Framework — Self-Management Lifecycle

### Overview
[What GSD is, where it lives, how CC manages it]

### Current State
[Version, install location, components]

### Lifecycle Operations

#### Install
[Exact commands, what gets installed where]

#### Update (6-Step Process)
[All 6 steps from update.md — this is the critical section]

#### Uninstall
[Exact command, what gets removed]

#### Version Check
[How to check installed vs. latest]

#### Troubleshoot
[Decision tree for common issues]

#### Health Check
[Commands to verify GSD integrity]

### Configuration Structure
[What files GSD owns, where they live]

### Known Issues
[Documented gotchas]

### Recovery Procedures
[How to recover from common failures]
```

**Critical pitfall avoidance (02-RESEARCH.md Pitfall 5):** The update workflow is MORE than just "run install again." Read `~/.claude/get-shit-done/workflows/update.md` fully and document all 6 steps:
1. Detect installed version from `~/.claude/get-shit-done/VERSION`
2. Check npm for latest: `npm view get-shit-done-cc version`
3. If update available: fetch changelog from GitHub raw URL, extract entries between versions, display preview
4. Confirm with user
5. Run `npx get-shit-done-cc@latest --claude --global`
6. Clear update cache: `rm -f ~/.claude/cache/gsd-update-check.json`

Also document: the installer backs up locally modified GSD files to `gsd-local-patches/` before overwriting.

**Fill each section with exact commands verified against local installation:**

- **Install:** `npx get-shit-done-cc@latest --claude --global` (global) or `npx get-shit-done-cc@latest --claude --local` (local). Source: GSD README.
- **Update:** `/gsd:update` (CC command) or raw: `npx get-shit-done-cc@latest --claude --global`. Document all 6 steps.
- **Uninstall:** `npx get-shit-done-cc --claude --global --uninstall`. Document what gets removed.
- **Version check:** `cat ~/.claude/get-shit-done/VERSION` (installed) and `npm view get-shit-done-cc version` (latest).
- **Troubleshoot:** `node ~/.claude/get-shit-done/bin/gsd-tools.cjs validate health` or `/gsd:health`. Build decision tree: "If X error, try Y."
- **Health check:** `/gsd:health` or the gsd-tools command.

**Configuration structure:** Document all GSD-owned paths:
- `~/.claude/get-shit-done/` — workflows, references, templates, VERSION, bin/
- `~/.claude/commands/gsd/` — user-facing slash commands
- `~/.claude/agents/gsd-*` — agent markdown files
- `~/.claude/hooks/gsd-*.js` — GSD hook scripts (gsd-check-update.js, gsd-context-monitor.js, gsd-statusline.js)

**Dual audience (per locked decision):** Each command should have both the exact command (for CC to execute) and a brief explanation (for user to understand).

**Verify by running:** `cat ~/.claude/get-shit-done/VERSION` and `npm view get-shit-done-cc version` to confirm current state. Read update.md to confirm all 6 steps.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/setup/GSD-LIFECYCLE.md" && grep -q "GSD Framework\|GSD" ".planning/phases/02-research/setup/GSD-LIFECYCLE.md" && grep -q "Install\|install" ".planning/phases/02-research/setup/GSD-LIFECYCLE.md" && grep -q "Update\|update" ".planning/phases/02-research/setup/GSD-LIFECYCLE.md" && grep -q "npx get-shit-done-cc" ".planning/phases/02-research/setup/GSD-LIFECYCLE.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/setup/GSD-LIFECYCLE.md` exists
    - Contains Install section with `npx get-shit-done-cc@latest --claude --global`
    - Contains Update section documenting all 6 steps (not just "run install again")
    - Update section mentions `~/.claude/get-shit-done/VERSION` (step 1)
    - Update section mentions `npm view get-shit-done-cc version` (step 2)
    - Update section mentions changelog fetch (step 3)
    - Update section mentions `rm -f ~/.claude/cache/gsd-update-check.json` (step 6)
    - Contains Uninstall section with `--uninstall` flag
    - Contains Version Check section with both installed and latest commands
    - Contains Troubleshoot section with decision tree or structured diagnostics
    - Contains Configuration Structure listing all 4 GSD-owned paths
    - Contains Recovery Procedures section
    - Commands are exact (copy-pasteable), not paraphrased
  </acceptance_criteria>
  <done>GSD lifecycle runbook is complete with all 6 update steps documented, all lifecycle operations covered with exact commands, configuration structure mapped, and troubleshooting decision tree included.</done>
</task>

<task type="auto">
  <name>Task 2: Document global scope coexistence strategy (GMGR-02)</name>
  <files>.planning/phases/02-research/setup/COEXISTENCE.md</files>
  <read_first>
    - ~/.claude/settings.json (actual hook registrations, permissions, env vars — the shared namespace)
    - ~/.claude.json (MCP server registrations — mcpServers key)
    - .planning/phases/02-research/02-RESEARCH.md (Coexistence Strategy section: config file separation table, hook registrations, 5 coexistence risks, PATH env var finding)
    - .planning/phases/02-research/02-CONTEXT.md (locked decision: primary focus on config conflict prevention, flags interaction risks but NO full recovery procedures)
    - .planning/research/PITFALLS.md (config corruption patterns)
  </read_first>
  <action>
Create `.planning/phases/02-research/setup/COEXISTENCE.md`.

**Document structure (per locked decision — config conflict prevention focus):**

```markdown
## Coexistence Strategy — Global Scope Tool Harmony

### Overview
[Why this matters: multiple tools share ~/.claude namespace]

### Config File Map
[Which files, who owns what, what each controls]

### Hook Namespace
[All registered hooks by event, which tool owns each, conflict risks]

### MCP Server Namespace
[Registered MCP servers, naming conventions, collision prevention]

### Plugin Namespace
[Registered plugins, naming conventions]

### Skills Namespace
[Skills folder structure, naming conventions]

### Interaction Risks
[Table of identified risks with mitigations]

### Prerequisites for Adding New Tools
[What must be checked/configured before adding any new global tool]
```

**Config file map (from 02-RESEARCH.md, verified against actual files):**

Read `~/.claude.json` and `~/.claude/settings.json` to verify current state. Document:

| File | Owner | Contents | Managed By |
|------|-------|----------|------------|
| `~/.claude.json` | CC core | mcpServers, numStartups, autoUpdates | `claude mcp add/remove` only |
| `~/.claude/settings.json` | CC core | hooks, permissions, env, model, plugins, statusLine | Edit file (structured JSON) |
| `~/.claude/get-shit-done/` | GSD | Workflows, references, templates, bin | GSD installer |
| `~/.claude/commands/gsd/` | GSD | Slash commands | GSD installer |
| `~/.claude/agents/gsd-*` | GSD | Agent files | GSD installer |
| `~/.claude/hooks/gsd-*.js` | GSD | Hook scripts | GSD installer |
| `~/.claude/graphiti/` | Graphiti | Docker stack, config, hooks | Manual setup |

**Hook namespace (verified from settings.json):**
Document ALL registered hooks by event:
- SessionStart: graphiti/session-start.sh (startup/resume), graphiti/session-start.sh (compact), gsd-check-update.js
- UserPromptSubmit: graphiti/prompt-augment.sh
- PostToolUse: graphiti/capture-change.sh (Write/Edit/MultiEdit), gsd-context-monitor.js
- PreCompact: graphiti/preserve-knowledge.sh
- Stop: graphiti/session-summary.sh

**Interaction risks (from 02-RESEARCH.md, verified):**

1. **mcpServers key collision** — two tools adding same server name. Mitigation: namespaced names.
2. **Hook timeout cascade** — multiple hooks on same event with tight timeouts. Document current timeouts per hook.
3. **Plugin enabledPlugins collision** — same plugin identifier. Document currently enabled plugins.
4. **~/.claude/skills/ namespace** — same folder name overwrites. Mitigation: descriptive subfolder names.
5. **PATH not set for stdio MCPs** — current settings.json has NO PATH in env block. This is critical: document as prerequisite. The recommended PATH value: `"PATH": "/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin"`.

**Critical finding (from 02-RESEARCH.md):** The current `~/.claude/settings.json` does NOT include an explicit PATH env variable. This must be flagged as a prerequisite for adding any new stdio MCP. Verify by reading settings.json and checking for PATH in the env block.

**Prerequisites section:** Before adding any new global tool:
1. Check ANTI-FEATURES.md pre-filter
2. Verify mcpServers name doesn't collide with existing entries
3. If stdio MCP: ensure PATH is set in settings.json env block
4. If tool has hooks: verify no matcher conflicts with existing hooks
5. If tool has skills: use unique subfolder in ~/.claude/skills/

**Per locked decision:** Flags interaction risks but does NOT include full recovery procedures. If something breaks, recovery can be figured out when needed.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/setup/COEXISTENCE.md" && grep -q "Coexistence\|coexistence" ".planning/phases/02-research/setup/COEXISTENCE.md" && grep -q "mcpServers\|MCP" ".planning/phases/02-research/setup/COEXISTENCE.md" && grep -q "hooks\|Hook" ".planning/phases/02-research/setup/COEXISTENCE.md" && grep -q "PATH" ".planning/phases/02-research/setup/COEXISTENCE.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/setup/COEXISTENCE.md` exists
    - Contains config file map table with at least 5 entries (claude.json, settings.json, GSD dirs, Graphiti dirs)
    - Contains hook namespace section listing all hooks by event (at least 7 hook registrations)
    - Contains interaction risks section with at least 5 identified risks
    - Contains PATH env var as a critical finding/prerequisite
    - PATH section includes the recommended value: `/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin`
    - Contains prerequisites section for adding new tools (at least 4 checklist items)
    - Does NOT include full recovery procedures (per locked decision)
    - Config data matches actual ~/.claude.json and ~/.claude/settings.json content (verified, not assumed)
  </acceptance_criteria>
  <done>Coexistence strategy is complete with config file map, hook namespace documented, interaction risks identified with mitigations, PATH prerequisite flagged, and new-tool prerequisites checklist provided.</done>
</task>

</tasks>

<verification>
Both setup documents exist and each is dual-audience (CC-executable commands + user-readable explanations):
- GSD-LIFECYCLE.md covers all lifecycle operations with exact commands
- COEXISTENCE.md maps all config namespaces and identifies interaction risks
- Both verified against actual local files (not just research assumptions)
</verification>

<success_criteria>
- Two documentation files exist in `.planning/phases/02-research/setup/`
- GSD-LIFECYCLE.md has all 6 update steps (verified against update.md)
- COEXISTENCE.md has actual config data from ~/.claude.json and ~/.claude/settings.json
- PATH env var prerequisite is documented in COEXISTENCE.md
- All commands are exact and copy-pasteable
- Documents are dual-audience per locked decision
</success_criteria>

<output>
After completion, create `.planning/phases/02-research/02-05-SUMMARY.md`
</output>
