---
phase: 02-research
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-research/memory/MEMO-01-BROWSING.md
  - .planning/phases/02-research/memory/MEMO-02-SESSIONS.md
  - .planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md
autonomous: true
requirements: [MEMO-01, MEMO-02, MEMO-03]
must_haves:
  truths:
    - "Memory browsing interface research documents available approaches with pros/cons table and a recommendation (existing tools only, no custom builds)"
    - "Session management visibility research documents available approaches with pros/cons table and a recommendation or gap finding (existing tools only, no custom builds)"
    - "Hook gap analysis defines an ideal memory capture system, then diffs it against the current Graphiti hooks to produce a concrete gap list with severities"
  artifacts:
    - path: ".planning/phases/02-research/memory/MEMO-01-BROWSING.md"
      provides: "Memory browsing interface approach comparison"
      contains: "## Memory Browsing Interface"
    - path: ".planning/phases/02-research/memory/MEMO-02-SESSIONS.md"
      provides: "Session management visibility approach comparison"
      contains: "## Session Management Visibility"
    - path: ".planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md"
      provides: "Graphiti hook gap analysis"
      contains: "## Hook Gap Analysis"
  key_links:
    - from: "MEMO-01 recommendation"
      to: "VETTING-PROTOCOL.md gates"
      via: "any recommended tool goes through full gate evaluation"
      pattern: "Gate.*PASS|FAIL"
    - from: "MEMO-03 ideal system"
      to: "current hooks in ~/.claude/settings.json"
      via: "diff comparison"
      pattern: "Current.*Missing|Gap"
---

<objective>
Research memory system enhancement approaches: browsing interface (MEMO-01), session visibility (MEMO-02), and Graphiti hook gaps (MEMO-03).

Purpose: These three research deliverables inform whether memory-related tools should enter the final 5-8 tool list and identify improvements to the existing Graphiti setup. All three follow the ADR-style approach comparison format per user locked decision.

Output: Three research documents in `.planning/phases/02-research/memory/`, each with approach comparison table, pros/cons, and recommendation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-methodology/VETTING-PROTOCOL.md
@.planning/phases/01-methodology/ANTI-FEATURES.md
@.planning/phases/02-research/02-CONTEXT.md
@.planning/phases/02-research/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research memory browsing interface approaches (MEMO-01)</name>
  <files>.planning/phases/02-research/memory/MEMO-01-BROWSING.md</files>
  <read_first>
    - .planning/phases/02-research/02-RESEARCH.md (MEMO-01 section with 4 approaches already identified, mcp-neo4j-cypher needs gate evaluation)
    - .planning/phases/02-research/02-CONTEXT.md (locked decision: existing tools only, no custom builds; ADR-style output format)
    - .planning/phases/01-methodology/VETTING-PROTOCOL.md (gates for any new tool candidate like mcp-neo4j-cypher)
    - .planning/phases/01-methodology/ANTI-FEATURES.md (pre-filter for mcp-neo4j-cypher)
  </read_first>
  <action>
Create `.planning/phases/02-research/memory/MEMO-01-BROWSING.md` using the ADR-style approach comparison template from 02-RESEARCH.md.

**Document structure (per locked decision):**

```markdown
## Memory Browsing Interface — Approach Comparison

### Problem Statement
No dedicated UI exists for browsing all Graphiti knowledge graph entities, facts, and episodes across scopes. The user and Claude Code need visibility into what the memory system has stored.

### Approaches Considered

| Approach | Description | Pros | Cons | Viability |

### [Per-Approach Detail Sections]

### Recommendation

### If Recommended Approach Is Not Viable
```

**Approaches to evaluate (from 02-RESEARCH.md pre-research):**

1. **Neo4j Browser (localhost:7475)** — Already running, built-in web UI
   - Pros: Already available, no install needed, full Cypher query support, visual graph view
   - Cons: Requires knowing Cypher, not indexed on Graphiti entities specifically, technical UX
   - Viability: VIABLE — exists today as a workaround

2. **Graphiti MCP tools (search_nodes, search_memory_facts, get_episodes)** — CC queries the graph directly
   - Pros: Already installed, CC-native interface, no new tools
   - Cons: No "browse all" capability, requires specific queries, not a browsing UI
   - Viability: VIABLE as workaround — not a true browsing interface

3. **mcp-neo4j-cypher (neo4j-contrib/mcp-neo4j)** — MCP converting natural language to Cypher
   - **MUST run full gate evaluation:** Verify via `gh api repos/neo4j-contrib/mcp-neo4j --jq '{stars: .stargazers_count, last_commit: .pushed_at, description: .description}'`
   - Pre-filter against ANTI-FEATURES.md first
   - Run all 4 gates from VETTING-PROTOCOL.md
   - If passes: document as recommended approach with full self-management commands
   - If fails: document which gate failed and why
   - Pros: Natural language interface to graph, bridges Graphiti data with conversational queries
   - Cons: Additional MCP = context cost, neo4j-contrib is community org (not official Neo4j vendor), must pass gates

4. **Neo4j Aura console** — Cloud-hosted Neo4j UI
   - Already documented as NOT VIABLE per 02-RESEARCH.md: "Graphiti runs locally, not on Aura — wrong deployment model"

**Step 1:** Write the problem statement.
**Step 2:** Fill the approach comparison table with all 4 approaches.
**Step 3:** For mcp-neo4j-cypher, run the full gate evaluation:
- `gh api repos/neo4j-contrib/mcp-neo4j --jq '{stars: .stargazers_count, last_commit: .pushed_at}'`
- Check ANTI-FEATURES.md pre-filter
- Apply all 4 gates per VETTING-PROTOCOL.md
- Document gate results inline
**Step 4:** Write per-approach detail sections.
**Step 5:** Write recommendation:
- If mcp-neo4j-cypher passes gates: recommend it as the improvement over current Neo4j Browser workaround
- If it fails: recommend Neo4j Browser as the existing available option and flag for v2
**Step 6:** Write fallback section.

**CRITICAL locked decision:** Do NOT propose building a custom browsing UI. If no existing tool passes gates, the answer is "use Neo4j Browser at localhost:7475 as the available workaround; flag for v2."
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/memory/MEMO-01-BROWSING.md" && grep -q "Browsing Interface\|browsing interface" ".planning/phases/02-research/memory/MEMO-01-BROWSING.md" && grep -q "Approach\|approach" ".planning/phases/02-research/memory/MEMO-01-BROWSING.md" && grep -q "Recommendation\|recommendation" ".planning/phases/02-research/memory/MEMO-01-BROWSING.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/memory/MEMO-01-BROWSING.md` exists
    - Contains problem statement section
    - Contains approach comparison table with at least 3 approaches (Neo4j Browser, Graphiti MCP tools, mcp-neo4j-cypher)
    - mcp-neo4j-cypher has gate evaluation results (stars count, recency, self-management, CC duplication)
    - Contains recommendation section with a clear recommendation
    - Does NOT propose building a custom browsing UI (locked decision)
    - Neo4j Aura documented as NOT VIABLE with reason
    - Contains "If Recommended Approach Is Not Viable" fallback section
  </acceptance_criteria>
  <done>Memory browsing interface research is complete with 4 approaches compared, mcp-neo4j-cypher gate-evaluated, and a recommendation produced per ADR-style format.</done>
</task>

<task type="auto">
  <name>Task 2: Research session management visibility approaches (MEMO-02)</name>
  <files>.planning/phases/02-research/memory/MEMO-02-SESSIONS.md</files>
  <read_first>
    - .planning/phases/02-research/02-RESEARCH.md (MEMO-02 section with 4 approaches, locked decision on no custom builds)
    - .planning/phases/02-research/02-CONTEXT.md (locked decision: existing tools only, ADR-style format)
    - .planning/phases/01-methodology/VETTING-PROTOCOL.md (in case any discovered tool needs gate evaluation)
    - ~/.claude/graphiti/hooks/session-summary.sh (how sessions are currently stored)
  </read_first>
  <action>
Create `.planning/phases/02-research/memory/MEMO-02-SESSIONS.md` using the ADR-style approach comparison template.

**Document structure:** Same as MEMO-01 (Problem Statement, Approaches Considered table, Per-Approach Detail, Recommendation, Fallback).

**Problem statement:** No structured way exists to list all past sessions, select a specific session for review, or see a session timeline. Sessions are stored in Graphiti as `session:{timestamp}` scoped episodes, but there is no listing/discovery mechanism.

**Approaches to evaluate (from 02-RESEARCH.md):**

1. **get_episodes MCP tool** — Retrieve episodes by group_id
   - Pros: Already installed, can retrieve session-scoped entries
   - Cons: Must know the exact timestamp to query `session:{timestamp}` — no listing of available sessions
   - Viability: VIABLE as workaround — limited discoverability

2. **search_memory_facts for session summaries** — Search "session summary" across all scopes
   - Pros: Already installed, returns session summary content
   - Cons: Returns flat results, not a structured session list with timeline
   - Viability: VIABLE as workaround

3. **Graphiti Python client (graphiti-helper.py)** — Run helper commands via CC Bash
   - Check if `~/.claude/graphiti/graphiti-helper.py` exists and what commands it supports
   - Pros: CC can run via Bash, direct Graphiti API access
   - Cons: CLI-only, requires Graphiti server running
   - Viability: VIABLE as workaround

4. **Custom CC slash command** — A command that lists all `session:*` scope entries
   - This is explicitly NOT VIABLE per locked decision: "research existing tools only — no custom build proposals"
   - Document as considered but ruled out by constraint

**Additional discovery:** Use WebSearch to check if any existing MCP or tool provides session/conversation history browsing that could work with Graphiti's storage model. Verify any discovered tool against ANTI-FEATURES.md and run full 4-gate evaluation if it passes pre-filter.

**Step 1:** Write problem statement.
**Step 2:** Fill approach comparison table.
**Step 3:** For each approach, test viability:
- For get_episodes: note the gap (no session listing)
- For search_memory_facts: note the gap (unstructured results)
- For graphiti-helper: check if `ls ~/.claude/graphiti/graphiti-helper.py` exists; read its help text
- For custom command: document as ruled out by locked decision
**Step 4:** Write per-approach detail sections.
**Step 5:** Write recommendation. Per 02-RESEARCH.md assessor task: "Document the gap (no structured session listing exists), document available workarounds, flag for v2 if no existing tool is found that passes gates."
**Step 6:** Write fallback section.

**Expected outcome:** This is likely a documented gap with workarounds. The structured session listing capability does not exist in any existing tool. The recommendation will be: combine existing MCP tools as workaround, flag for v2.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/memory/MEMO-02-SESSIONS.md" && grep -q "Session\|session" ".planning/phases/02-research/memory/MEMO-02-SESSIONS.md" && grep -q "Recommendation\|recommendation" ".planning/phases/02-research/memory/MEMO-02-SESSIONS.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/memory/MEMO-02-SESSIONS.md` exists
    - Contains problem statement describing the session listing gap
    - Contains approach comparison table with at least 3 approaches
    - Custom CC slash command documented as NOT VIABLE per locked decision
    - Contains recommendation section (likely: document gap, use workarounds, flag v2)
    - Does NOT propose building a custom session listing tool (locked decision)
    - Contains "If Recommended Approach Is Not Viable" fallback section
  </acceptance_criteria>
  <done>Session management visibility research is complete with approaches compared, gap documented, available workarounds listed, and recommendation produced per ADR-style format.</done>
</task>

<task type="auto">
  <name>Task 3: Analyze Graphiti hook system gaps (MEMO-03)</name>
  <files>.planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md</files>
  <read_first>
    - .planning/phases/02-research/02-RESEARCH.md (MEMO-03 section: current hook coverage table, preliminary gap analysis with 7 gap categories and severities)
    - .planning/phases/02-research/02-CONTEXT.md (locked decision: compare current hooks against ideal system, define what perfect system would capture, then diff)
    - ~/.claude/settings.json (actual hook registrations — verify current state)
    - ~/.claude/graphiti/hooks/session-start.sh (verify what SessionStart captures)
    - ~/.claude/graphiti/hooks/prompt-augment.sh (verify what UserPromptSubmit captures)
    - ~/.claude/graphiti/hooks/capture-change.sh (verify what PostToolUse captures — confirm only Write/Edit/MultiEdit)
    - ~/.claude/graphiti/hooks/preserve-knowledge.sh (verify what PreCompact captures)
    - ~/.claude/graphiti/hooks/session-summary.sh (verify what Stop captures)
  </read_first>
  <action>
Create `.planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md`.

**Document structure (per locked decision — define ideal, then diff):**

```markdown
## Hook Gap Analysis — Ideal vs. Current

### Current System Baseline
[Document all 5 current hooks with what each captures — verified by reading actual hook scripts]

### Ideal Memory Capture System
[Define what a perfect system would capture at each lifecycle event]

### Gap Analysis: Ideal vs. Current
[Table: Gap, Current Behavior, Ideal Behavior, Severity, Impact]

### Recommendations
[Prioritized list of gaps to address, sorted by severity]
```

**Step 1 — Document current system baseline:**
Read each hook script to verify what it actually does (not just what 02-RESEARCH.md says). Record:
- SessionStart: `~/.claude/graphiti/hooks/session-start.sh` — what it captures, what it injects
- UserPromptSubmit: `~/.claude/graphiti/hooks/prompt-augment.sh` — what it searches, how it curates
- PostToolUse: `~/.claude/graphiti/hooks/capture-change.sh` — confirm only fires on Write/Edit/MultiEdit
- PreCompact: `~/.claude/graphiti/hooks/preserve-knowledge.sh` — what gets preserved
- Stop: `~/.claude/graphiti/hooks/session-summary.sh` — what gets summarized and stored

Also read `~/.claude/settings.json` to verify hook registrations (matchers, timeouts).

**Step 2 — Define ideal memory capture system:**
For each CC lifecycle event, define what a perfect memory system would capture:

| Lifecycle Event | Ideal Capture |
|---|---|
| SessionStart | Global prefs, project context, recent sessions, active task state |
| UserPromptSubmit | Semantic search results + user intent classification |
| PostToolUse (Write/Edit) | File changes with semantic diff summary |
| PostToolUse (Bash) | Commands run, exit codes, key output (errors, discovered paths, test results) |
| PostToolUse (Read) | Which files were consulted (key references, not all reads) |
| PreCompact | Session summary, key decisions, unresolved questions |
| Stop | Complete session summary with outcomes, next steps |
| Error/Failure | Failed commands with error context as BugPattern entities |
| Explicit Memory | User "remember this" statements reliably captured |
| Cross-Scope Promotion | Project preferences promoted to global when appropriate |
| Feedback Loop | Track which memories are retrieved vs. never used (pruning candidates) |

**Step 3 — Gap analysis table:**
Diff ideal vs. current. Use the 7 preliminary gaps from 02-RESEARCH.md as a starting point, but verify each against actual hook code. The pre-identified gaps are:

1. Bash tool capture (PostToolUse doesn't fire on Bash) — MEDIUM
2. Read tool capture (file reads not captured) — LOW
3. TodoWrite capture (task creation not captured) — LOW
4. Explicit user memory commands (no systematic prompt) — MEDIUM
5. Error/failure capture (errors not stored as entities) — MEDIUM
6. Context quality metrics (no feedback loop) — LOW
7. Cross-project global preference capture (no scope escalation) — LOW

For each gap:
- Verify severity by reading the actual hook code
- Assess impact: what is lost by not capturing this?
- Note any technical constraints (e.g., CC hook API may not support Bash matcher)

**Step 4 — Recommendations:**
Prioritize gaps by: severity * feasibility. Some gaps may not be closable with the current CC hook API (e.g., if CC doesn't fire PostToolUse for Bash commands, no hook can capture them). Document which gaps are:
- Closable with current hook API (add new hook entry)
- Closable with Graphiti changes (modify existing scripts)
- Blocked by CC hook API limitations (flag for CC feature request)

**IMPORTANT:** This task is analysis only — do NOT modify any hook scripts or settings.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md" && grep -q "Gap Analysis\|gap analysis" ".planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md" && grep -q "Ideal\|ideal" ".planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md" && grep -q "Current\|current" ".planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/memory/MEMO-03-HOOK-GAPS.md` exists
    - Contains `### Current System Baseline` with all 5 hooks documented
    - Contains `### Ideal Memory Capture System` defining what a perfect system would capture
    - Contains gap analysis table with at least 5 gaps (from pre-research baseline of 7)
    - Each gap has: Current Behavior, Ideal Behavior, Severity, Impact
    - Contains recommendations section with gaps categorized by feasibility (closable with hooks, closable with Graphiti changes, blocked by CC API)
    - Does NOT modify any hook scripts or settings files (analysis only)
    - Hook descriptions match actual hook script content (verified by reading scripts, not just copied from research)
  </acceptance_criteria>
  <done>Hook gap analysis is complete with current system verified against actual hook scripts, ideal system defined, gaps identified and prioritized, and recommendations categorized by feasibility.</done>
</task>

</tasks>

<verification>
All three memory research documents exist and each follows the ADR-style approach comparison format:
- MEMO-01: Approaches compared, mcp-neo4j-cypher gate-evaluated, recommendation produced
- MEMO-02: Gap documented, workarounds listed, no custom build proposed
- MEMO-03: Current hooks verified, ideal system defined, gaps identified with severities
</verification>

<success_criteria>
- Three research documents exist in `.planning/phases/02-research/memory/`
- MEMO-01 and MEMO-02 follow ADR-style approach comparison format
- MEMO-01 includes full gate evaluation for mcp-neo4j-cypher
- MEMO-02 does NOT propose custom builds (locked decision)
- MEMO-03 defines ideal system BEFORE identifying gaps (locked decision methodology)
- MEMO-03 hook descriptions verified against actual script content
- All recommendations are actionable or clearly flagged for v2
</success_criteria>

<output>
After completion, create `.planning/phases/02-research/02-04-SUMMARY.md`
</output>
