---
phase: 02-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-research/assessments/CONTEXT7.md
  - .planning/phases/02-research/assessments/WPCS-SKILL.md
  - .planning/phases/02-research/assessments/GITHUB-MCP.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DEVT-01]
must_haves:
  truths:
    - "Context7 MCP has a complete assessment scorecard with all 4 gate results, context cost estimate, self-management commands, and a tier verdict"
    - "WPCS Skill has a complete assessment adapted for file-based skills (stars gate documented as N/A with explanation, maintenance assessed against WordPress project)"
    - "GitHub MCP has a complete assessment scorecard including PAT minimum scope requirements and CC duplication analysis vs gh CLI"
  artifacts:
    - path: ".planning/phases/02-research/assessments/CONTEXT7.md"
      provides: "Context7 MCP assessment"
      contains: "## Tool Assessment: Context7 MCP"
    - path: ".planning/phases/02-research/assessments/WPCS-SKILL.md"
      provides: "WPCS Skill assessment"
      contains: "## Tool Assessment: WPCS Skill"
    - path: ".planning/phases/02-research/assessments/GITHUB-MCP.md"
      provides: "GitHub MCP assessment"
      contains: "## Tool Assessment: GitHub MCP"
  key_links:
    - from: "each assessment"
      to: "VETTING-PROTOCOL.md scorecard template"
      via: "copy and fill every field"
      pattern: "Gate.*PASS|FAIL"
---

<objective>
Produce vetted assessments for Context7 MCP, WPCS Skill, and GitHub MCP by applying the Phase 1 vetting protocol scorecard to each candidate.

Purpose: These three assessments cover the documentation tools (DOCS-01, DOCS-02) and the first dev tool (DEVT-01). Each assessment feeds directly into the Phase 3 ranked report as a row with a pre-assigned tier verdict.

Output: Three complete assessment markdown files in `.planning/phases/02-research/assessments/`, each following the scorecard template from VETTING-PROTOCOL.md Section 2.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-methodology/VETTING-PROTOCOL.md
@.planning/phases/01-methodology/ANTI-FEATURES.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/phases/02-research/02-CONTEXT.md
@.planning/phases/02-research/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assess Context7 MCP (DOCS-01)</name>
  <files>.planning/phases/02-research/assessments/CONTEXT7.md</files>
  <read_first>
    - .planning/phases/01-methodology/VETTING-PROTOCOL.md (scorecard template in Section 2, gate definitions in Section 1)
    - .planning/phases/01-methodology/ANTI-FEATURES.md (pre-filter check)
    - .planning/research/FEATURES.md (existing stars/commit data for Context7)
    - .planning/phases/02-research/02-RESEARCH.md (pre-research data in "Context7 MCP (DOCS-01)" section, open questions about PHP/WP coverage)
  </read_first>
  <action>
Create `.planning/phases/02-research/assessments/CONTEXT7.md` using the exact scorecard template from VETTING-PROTOCOL.md Section 2.

**Step 1 — Pre-filter:** Check ANTI-FEATURES.md named list and category rules. Context7 is NOT listed. Proceed.

**Step 2 — Identity section:** Fill from existing data:
- Name: Context7 MCP
- Repo URL: https://github.com/upstash/context7
- Stars: Verify current count with `gh api repos/upstash/context7 --jq '.stargazers_count'`
- Last Commit: Verify with `gh api repos/upstash/context7 --jq '.pushed_at'`
- Transport Type: stdio (npx)
- Publisher: established-org (Upstash)

**Step 3 — Hard Gate Results table:**
- Gate 1 (Stars): Threshold for established-org = 500. Record actual count from gh API.
- Gate 2 (Recency): Calculate days since last commit. Record classification (PREFERRED/ACCEPTABLE/FAIL).
- Gate 3 (Self-Management): Document all 4 operations:
  - Install: `claude mcp add context7 -- npx -y @upstash/context7-mcp@latest` (source: official README)
  - Configure: No API key required for free tier; document free tier limits (60 req/hr, 1,000/month)
  - Update: `npm view @upstash/context7-mcp version` to check; reinstall command to update (same as install — npx @latest pulls newest)
  - Troubleshoot: `claude mcp list` to verify registration; `npx -y @upstash/context7-mcp@latest --help` to verify binary works; check stderr for connection errors
  - Verify each command from official docs at https://github.com/upstash/context7 README
- Gate 4 (CC Duplication): Context7 provides library documentation lookup. CC has no native library documentation tool. No duplication — PASS.

**Step 4 — Context Cost Estimate:**
- Run `gh api repos/upstash/context7` and look for tool count in README or fetch the README to count exposed MCP tools
- Fetch official docs with WebFetch to determine tool count and estimate token overhead
- Record source of measurement

**Step 5 — Self-Management Commands table:** Fill all 4 operations with exact commands and source URLs.

**Step 6 — Security Findings:**
- mcp-scan: "not yet run — Phase 3"
- Known CVEs: check `gh api repos/upstash/context7/security/advisories --jq '.[].summary'` or note "none published"
- Risk level: assess based on permissions model (stdio transport, no network access beyond upstash API)

**Step 7 — WordPress/PHP Relevance:**
- Document Context7's coverage of PHP and WordPress libraries at the free tier
- Note this is a known gap from STATE.md: "Context7 PHP/WP coverage depth unverified at free tier"
- Verify by using WebFetch on upstash.com/context7 or the official docs to check library coverage listing
- If coverage depth cannot be confirmed: document as "unverified — recommend hands-on testing in Phase 3"

**Step 8 — Pros and Cons:** List at least 3 of each based on findings.

**Step 9 — Verdict:** Apply tier criteria from VETTING-PROTOCOL.md Section 3:
- INCLUDE if: passes all gates + fills capability gap CC doesn't cover natively + no overlap with another INCLUDE
- CONSIDER if: passes all gates + overlaps or has cost implications
- Record rationale in 1-2 sentences
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/assessments/CONTEXT7.md" && grep -q "## Tool Assessment: Context7 MCP" ".planning/phases/02-research/assessments/CONTEXT7.md" && grep -q "Gate Summary:" ".planning/phases/02-research/assessments/CONTEXT7.md" && grep -q "Verdict" ".planning/phases/02-research/assessments/CONTEXT7.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/assessments/CONTEXT7.md` exists
    - Contains `## Tool Assessment: Context7 MCP`
    - Contains `### Hard Gate Results` with a table showing all 4 gates
    - Contains `Gate Summary:` with either `ALL PASS` or `FAILED`
    - Contains `### Context Cost Estimate` with `Tool count exposed` and `Estimated token overhead`
    - Contains `### Self-Management Commands` with rows for Install, Configure, Update, Troubleshoot
    - Contains `### Security Findings` with `mcp-scan result:`
    - Contains `### WordPress/PHP Relevance` section
    - Contains `### Verdict` with one of: INCLUDE, CONSIDER, DEFER, ELIMINATED
    - Contains `**Tier:**` followed by the tier assignment
  </acceptance_criteria>
  <done>Context7 MCP assessment is complete with all scorecard fields filled, all 4 gate results recorded, context cost estimated, self-management commands documented, PHP/WP relevance addressed, and a tier verdict assigned per protocol criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Assess WPCS Skill (DOCS-02)</name>
  <files>.planning/phases/02-research/assessments/WPCS-SKILL.md</files>
  <read_first>
    - .planning/phases/01-methodology/VETTING-PROTOCOL.md (scorecard template, gate definitions)
    - .planning/phases/01-methodology/ANTI-FEATURES.md (pre-filter)
    - .planning/phases/02-research/02-RESEARCH.md (WPCS Skill section, Pitfall 1 about stars gate confusion)
    - .planning/research/FEATURES.md (existing WPCS data)
  </read_first>
  <action>
Create `.planning/phases/02-research/assessments/WPCS-SKILL.md` using the scorecard template adapted for a file-based CC Skill.

**Critical pitfall avoidance (from 02-RESEARCH.md Pitfall 1):** WPCS Skill is a file-based CC Skill (SKILL.md in ~/.claude/skills/wordpress/), NOT an MCP server. It has no GitHub repo with stars. Do NOT search npm or GitHub for "wpcs-skill." The assessor must adapt the scorecard gates as follows.

**Step 1 — Pre-filter:** Check ANTI-FEATURES.md. WPCS Skill is NOT listed. Proceed.

**Step 2 — Identity section (adapted for Skills):**
- Name: WPCS Skill (WordPress Coding Standards)
- Repo URL: N/A — file-based CC Skill; underlying standard maintained at https://github.com/WordPress/WordPress-Coding-Standards
- Stars: N/A for Skill itself; WPCS repo stars: verify with `gh api repos/WordPress/WordPress-Coding-Standards --jq '.stargazers_count'`
- Last Commit: verify WPCS repo recency with `gh api repos/WordPress/WordPress-Coding-Standards --jq '.pushed_at'`
- Transport Type: N/A — file-based Skill (no transport; loaded by CC at session start)
- Publisher: vendor-official (WordPress core team maintains WPCS)

**Step 3 — Hard Gate Results (adapted):**
- Gate 1 (Stars): "N/A: File-based Skills are not distributed via GitHub repositories with star counts. Maintenance is assessed against the WordPress Coding Standards project (actively maintained by WordPress core team). WPCS repo has [X] stars — meets vendor-official threshold of 100 if applied." Record as PASS with explanation.
- Gate 2 (Recency): Check WPCS repo last commit. Record classification.
- Gate 3 (Self-Management): All 4 operations via CC tools:
  - Install: CC runs `Write` tool to create `~/.claude/skills/wordpress/SKILL.md` with WPCS content
  - Configure: No configuration needed — Skills are automatically loaded by CC
  - Update: CC runs `Edit` tool to modify the Skill file; can also `Read` the WPCS official docs to refresh content
  - Troubleshoot: CC runs `Read` on `~/.claude/skills/wordpress/SKILL.md` to verify content; `ls ~/.claude/skills/wordpress/` to verify file exists
  - All operations use CC native tools — fully self-manageable
- Gate 4 (CC Duplication): CC has no native WordPress coding standards knowledge beyond training data. A Skill file provides persistent, version-specific WPCS rules. No duplication — PASS.

**Step 4 — Context Cost Estimate:**
- Skills files are loaded by CC at session start. Typical SKILL.md size: ~30-50 tokens per skill index line (SKILL.md is a lightweight index file per skills architecture)
- Document expected token overhead as minimal (~30-130 tokens based on SKILL.md size)
- Note: if SKILL.md references rules/*.md files, those are loaded on-demand, not at startup

**Step 5 — Self-Management Commands:** Fill with CC tool commands (Write, Edit, Read, Bash ls) — not CLI commands.

**Step 6 — Security Findings:**
- mcp-scan: N/A — file-based Skill, not an MCP server; no network calls, no transport
- Risk level: LOW — local markdown file, no code execution, no network access

**Step 7 — WordPress/PHP Relevance:** This IS the WordPress-specific tool. Document what WPCS rules the Skill should cover: naming conventions, hook patterns, sanitization/escaping requirements, database query patterns, PHP 8.x compatibility notes.

**Step 8 — Pros and Cons:** List benefits (zero context cost, CC-native management, no install dependencies) and caveats (content must be manually curated, no auto-update from WPCS releases).

**Step 9 — Verdict:** Apply tier criteria. Likely INCLUDE: passes adapted gates, fills clear capability gap (WordPress-specific coding standards), no overlap with other INCLUDE candidates.
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/assessments/WPCS-SKILL.md" && grep -q "## Tool Assessment: WPCS Skill" ".planning/phases/02-research/assessments/WPCS-SKILL.md" && grep -q "Gate Summary:" ".planning/phases/02-research/assessments/WPCS-SKILL.md" && grep -q "Verdict" ".planning/phases/02-research/assessments/WPCS-SKILL.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/assessments/WPCS-SKILL.md` exists
    - Contains `## Tool Assessment: WPCS Skill`
    - Contains `### Hard Gate Results` with all 4 gates (Gate 1 showing N/A explanation for file-based Skills)
    - Contains `Gate Summary:` with verdict
    - Contains `### Context Cost Estimate` documenting minimal token overhead
    - Contains `### Self-Management Commands` with CC tool commands (Write, Edit, Read), not CLI commands
    - Contains `### Security Findings` noting N/A for mcp-scan (not an MCP server)
    - Contains `### WordPress/PHP Relevance` section describing WPCS rules coverage
    - Contains `### Verdict` with tier assignment
    - Does NOT contain a GitHub search for "wpcs-skill" npm package (that would be Pitfall 1)
  </acceptance_criteria>
  <done>WPCS Skill assessment is complete with gates adapted for file-based Skills format, self-management via CC native tools documented, WordPress/PHP relevance fully addressed, and a tier verdict assigned.</done>
</task>

<task type="auto">
  <name>Task 3: Assess GitHub MCP (DEVT-01)</name>
  <files>.planning/phases/02-research/assessments/GITHUB-MCP.md</files>
  <read_first>
    - .planning/phases/01-methodology/VETTING-PROTOCOL.md (scorecard template, gate definitions)
    - .planning/phases/01-methodology/ANTI-FEATURES.md (pre-filter)
    - .planning/research/FEATURES.md (existing GitHub MCP data)
    - .planning/phases/02-research/02-RESEARCH.md (GitHub MCP section, PAT scope question, CC duplication analysis)
  </read_first>
  <action>
Create `.planning/phases/02-research/assessments/GITHUB-MCP.md` using the exact scorecard template.

**Step 1 — Pre-filter:** Check ANTI-FEATURES.md. GitHub MCP is NOT listed. Proceed.

**Step 2 — Identity section:**
- Name: GitHub MCP Server
- Repo URL: https://github.com/github/github-mcp-server
- Stars: Verify with `gh api repos/github/github-mcp-server --jq '.stargazers_count'`
- Last Commit: Verify with `gh api repos/github/github-mcp-server --jq '.pushed_at'`
- Transport Type: HTTP (streamable)
- Publisher: vendor-official (GitHub/Microsoft)

**Step 3 — Hard Gate Results:**
- Gate 1 (Stars): Threshold for vendor-official = 100. Record actual count.
- Gate 2 (Recency): Calculate days since last commit.
- Gate 3 (Self-Management):
  - Install: Fetch the official README from GitHub to find the exact `claude mcp add` command. Expected pattern: `claude mcp add github-mcp-server --transport http --url https://api.github.com/mcp --header "Authorization: Bearer $GITHUB_PERSONAL_ACCESS_TOKEN"` or similar. Verify exact syntax from official docs.
  - Configure: Document PAT requirements. Use WebFetch on the official README or GitHub docs to determine minimum PAT scopes. From 02-RESEARCH.md: "PAT scope verification flagged as gap — check GitHub OAuth docs for minimum scopes for read repos + issues read/write." Document: `export GITHUB_PERSONAL_ACCESS_TOKEN=ghp_xxx` (user must create PAT in GitHub Settings > Developer settings > Personal access tokens).
  - Update: For HTTP transport MCPs, update is server-side (GitHub manages it). Document how to verify current version or re-register.
  - Troubleshoot: `claude mcp list` to verify registration; check MCP health via `claude mcp get github-mcp-server` or equivalent; test with a simple API call.
- Gate 4 (CC Duplication): CC has `gh` CLI via Bash tool. 02-RESEARCH.md provides the analysis: "gh CLI is manual shell invocation, GitHub MCP provides structured tool-level access — NOT a duplicate." The MCP provides semantic tool-level integration (create_issue, search_code, create_pull_request as discrete tools) vs. raw shell `gh` commands. Document this distinction. PASS — not a duplicate, provides structured tool access.

**Step 4 — Context Cost Estimate:**
- Fetch the official README to count the number of MCP tools exposed by GitHub MCP (it exposes many tools — issues, PRs, repos, code search, etc.)
- Estimate token overhead based on tool count * ~150 tokens per tool definition
- This is likely a HIGH context cost MCP — document it

**Step 5 — Self-Management Commands:** Fill with exact commands.

**Step 6 — Security Findings:**
- mcp-scan: "not yet run — Phase 3"
- PAT security: Document that PAT grants access to user's GitHub account; narrow scoping is essential; document recommended minimum scopes
- Risk level: assess (PAT handling, network access to GitHub API)

**Step 7 — WordPress/PHP Relevance:** General-purpose dev tool, relevant to all repos including WordPress projects. Omit this section or note "general-purpose — no stack-specific considerations."

**Step 8 — Pros and Cons:** Include the CC duplication nuance (gh CLI exists but MCP provides structured access), PAT requirement, context cost, and the value of semantic GitHub tool integration.

**Step 9 — Verdict:** Apply tier criteria. Likely CONSIDER: passes all gates, provides value, but CC already has gh CLI via Bash (overlap, even if not duplication), and requires PAT with cost implications (not monetary, but security surface).
  </action>
  <verify>
    <automated>test -f ".planning/phases/02-research/assessments/GITHUB-MCP.md" && grep -q "## Tool Assessment: GitHub MCP" ".planning/phases/02-research/assessments/GITHUB-MCP.md" && grep -q "Gate Summary:" ".planning/phases/02-research/assessments/GITHUB-MCP.md" && grep -q "Verdict" ".planning/phases/02-research/assessments/GITHUB-MCP.md" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02-research/assessments/GITHUB-MCP.md` exists
    - Contains `## Tool Assessment: GitHub MCP`
    - Contains `### Hard Gate Results` with all 4 gates
    - Contains `Gate Summary:` with verdict
    - Gate 4 discussion includes explicit analysis of `gh` CLI overlap vs. MCP structured tool access distinction
    - Contains `### Context Cost Estimate` with tool count and token overhead (expected HIGH)
    - Contains `### Self-Management Commands` including PAT setup instructions
    - Contains `### Security Findings` with PAT scope documentation
    - Contains `### Verdict` with tier assignment and rationale
    - PAT minimum scope requirements are documented (specific scopes listed, not just "narrow-scoped")
  </acceptance_criteria>
  <done>GitHub MCP assessment is complete with all scorecard fields filled, PAT scope requirements documented, gh CLI overlap explicitly analyzed, context cost estimated (with tool count), and a tier verdict assigned.</done>
</task>

</tasks>

<verification>
All three assessment files exist in `.planning/phases/02-research/assessments/` and each contains:
- Complete Identity section with verified GitHub data
- Hard Gate Results table with all 4 gates evaluated
- Gate Summary with pass/fail determination
- Context Cost Estimate with measurement source
- Self-Management Commands with 4 operations documented
- Security Findings section
- Pros and Cons list
- Verdict with tier assignment and rationale
</verification>

<success_criteria>
- Three assessment markdown files exist and are non-empty
- Each file follows the VETTING-PROTOCOL.md Section 2 scorecard template exactly
- Gate data is verified via `gh api` calls (not copied from stale research data without verification)
- WPCS Skill assessment correctly adapts gates for file-based Skills (does NOT fail trying to find a GitHub repo for "WPCS Skill")
- GitHub MCP assessment documents PAT minimum scopes with specific scope names
- Each assessment has a tier verdict with rationale applying Section 3 criteria
</success_criteria>

<output>
After completion, create `.planning/phases/02-research/02-01-SUMMARY.md`
</output>
