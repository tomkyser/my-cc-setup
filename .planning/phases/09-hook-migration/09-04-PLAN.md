---
phase: 09-hook-migration
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - claude-config/settings-hooks.json
autonomous: false
requirements: [LDG-01, LDG-02, LDG-03, LDG-04, LDG-05, LDG-06, LDG-09, LDG-10]

must_haves:
  truths:
    - "settings.json points all 5 hook events to dynamo-hooks.cjs"
    - "settings.json.bak exists as rollback safety net"
    - "CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS is set to 10000 in settings.json env block"
    - "GSD hooks remain untouched in settings.json"
    - "A real Claude Code session lifecycle works with the new CJS hooks"
  artifacts:
    - path: "~/.claude/settings.json.bak"
      provides: "Backup of pre-switchover settings for rollback"
    - path: "~/.claude/settings.json"
      provides: "Updated hook registrations pointing to CJS dispatcher"
      contains: "dynamo-hooks.cjs"
    - path: "claude-config/settings-hooks.json"
      provides: "Git-tracked settings template updated to CJS"
  key_links:
    - from: "~/.claude/settings.json"
      to: "~/.claude/dynamo/hooks/dynamo-hooks.cjs"
      via: "hook command registration"
      pattern: "dynamo-hooks\\.cjs"
    - from: "~/.claude/settings.json"
      to: "CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS"
      via: "env block"
      pattern: "SESSIONEND_HOOKS_TIMEOUT"
---

<objective>
Switch settings.json to point all 5 hook events to the CJS dispatcher (dynamo-hooks.cjs), with settings.json.bak backup and CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS=10000 environment variable. Then verify with a human smoke test.

Purpose: This is the switchover -- the moment the old Python/Bash hooks are retired and CJS takes over. The backup ensures safe rollback.
Output: Updated settings.json, settings.json.bak, updated settings-hooks.json template, human verification of live session.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hook-migration/09-CONTEXT.md
@.planning/phases/09-hook-migration/09-RESEARCH.md
@.planning/phases/09-hook-migration/09-03-SUMMARY.md

<interfaces>
<!-- Target settings.json structure from RESEARCH.md -->

```json
{
  "env": {
    "CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS": "10000"
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 30
        }]
      },
      {
        "matcher": "compact",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 30
        }]
      },
      {
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/hooks/gsd-check-update.js\""
        }]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 15
        }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 10
        }]
      },
      {
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/hooks/gsd-context-monitor.js\""
        }]
      }
    ],
    "PreCompact": [
      {
        "matcher": "",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 30
        }]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [{
          "type": "command",
          "command": "node \"$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs\"",
          "timeout": 30
        }]
      }
    ]
  }
}
```

Note: GSD hooks (gsd-check-update.js, gsd-context-monitor.js) remain untouched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backup settings.json and switch hook registrations to CJS dispatcher</name>
  <files>
    claude-config/settings-hooks.json
  </files>
  <read_first>
    ~/.claude/settings.json,
    claude-config/settings-hooks.json,
    ~/.claude/dynamo/hooks/dynamo-hooks.cjs
  </read_first>
  <action>
Perform the settings switchover in 3 steps:

**Step 1: Backup** (hard requirement from CONTEXT.md)
```bash
cp ~/.claude/settings.json ~/.claude/settings.json.bak
```
Verify backup exists and is identical to current settings.json.

**Step 2: Update ~/.claude/settings.json**
Read the current settings.json. Modify it to match the target structure from the interfaces block above. Key changes:
- Add `"env"` block with `"CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS": "10000"`
- Replace all Python/Bash hook commands with `node "$HOME/.claude/dynamo/hooks/dynamo-hooks.cjs"`
- Keep GSD hooks (`gsd-check-update.js`, `gsd-context-monitor.js`) unchanged in their existing positions
- SessionStart: two matchers (`startup|resume` and `compact`), both pointing to dynamo-hooks.cjs, plus GSD check-update hook
- UserPromptSubmit: one entry with empty matcher, timeout 15
- PostToolUse: one entry with `Write|Edit|MultiEdit` matcher, timeout 10, plus GSD context-monitor hook
- PreCompact: one entry with empty matcher, timeout 30
- Stop: one entry with empty matcher, timeout 30

**Step 3: Update repo template**
Write the same structure to `claude-config/settings-hooks.json` (the git-tracked template).

Verify the dynamo-hooks.cjs file exists at the path referenced in settings.json:
```bash
ls -la ~/.claude/dynamo/hooks/dynamo-hooks.cjs
```

Run the full test suite one final time:
```bash
cd ~/.claude/dynamo && node --test tests/*.test.cjs
```
  </action>
  <verify>
    <automated>diff ~/.claude/settings.json ~/.claude/settings.json.bak > /dev/null 2>&1 && echo "ERROR: settings.json unchanged" || echo "settings.json updated"; test -f ~/.claude/settings.json.bak && echo "backup exists" || echo "ERROR: no backup"; grep -q "dynamo-hooks.cjs" ~/.claude/settings.json && echo "CJS hooks registered" || echo "ERROR: CJS hooks not found"; grep -q "SESSIONEND_HOOKS_TIMEOUT" ~/.claude/settings.json && echo "timeout env set" || echo "ERROR: timeout env missing"; grep -q "gsd-context-monitor" ~/.claude/settings.json && echo "GSD hooks preserved" || echo "ERROR: GSD hooks missing"</automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/settings.json.bak exists and contains the pre-switchover content
    - ~/.claude/settings.json contains `"CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS": "10000"` in env block
    - ~/.claude/settings.json contains `dynamo-hooks.cjs` for all 5 hook events
    - ~/.claude/settings.json contains `gsd-check-update.js` (GSD hook preserved)
    - ~/.claude/settings.json contains `gsd-context-monitor.js` (GSD hook preserved)
    - ~/.claude/settings.json contains `"timeout": 30` for SessionStart, PreCompact, and Stop hooks
    - ~/.claude/settings.json contains `"timeout": 15` for UserPromptSubmit hook
    - ~/.claude/settings.json contains `"timeout": 10` for PostToolUse hook
    - ~/.claude/settings.json does NOT contain `graphiti-helper.py` or `session-start.sh` or `prompt-augment.sh`
    - claude-config/settings-hooks.json matches the same structure as ~/.claude/settings.json
    - Full test suite passes: `cd ~/.claude/dynamo && node --test tests/*.test.cjs` exits 0
  </acceptance_criteria>
  <done>settings.json switched to CJS hooks with backup. All 5 events point to dynamo-hooks.cjs. GSD hooks untouched. Timeout env var set. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test -- verify CJS hooks work in a real Claude Code session</name>
  <what-built>
    All 5 Claude Code hook events now routed through the CJS dispatcher (dynamo-hooks.cjs) instead of the Python/Bash system. The settings.json has been updated with backup at settings.json.bak.
  </what-built>
  <how-to-verify>
    1. Start a NEW Claude Code session (close this one and open a new terminal)
    2. Verify SessionStart fires: look for `[GRAPHITI MEMORY CONTEXT]` in the session preamble (if Graphiti is running)
    3. Type a substantial prompt (20+ characters) and verify UserPromptSubmit fires: look for `[RELEVANT MEMORY]` context injection (if Graphiti is running)
    4. Make a file edit (any small change) and verify PostToolUse fires: check `~/.claude/dynamo/hook-errors.log` for any errors (absence of errors = good)
    5. End the session and check:
       - `~/.claude/graphiti/sessions.json` has a new entry for this session
       - `~/.claude/dynamo/hook-errors.log` shows no unexpected errors
    6. If anything fails: rollback is `cp ~/.claude/settings.json.bak ~/.claude/settings.json`

    **Expected outcomes:**
    - Session starts with memory context (or graceful degradation if Graphiti not running)
    - Prompts get memory augmentation
    - File edits are captured
    - Session ends with summary and auto-name in sessions.json
    - No hooks crash (all exit 0)

    **If Graphiti is NOT running:** Hooks should still exit 0 gracefully. No crash, no hang. Memory context will be absent but session should work normally.
  </how-to-verify>
  <resume-signal>Type "approved" if hooks work correctly, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
1. settings.json.bak exists
2. settings.json has all 5 events pointing to dynamo-hooks.cjs
3. CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS=10000 in env block
4. GSD hooks preserved
5. Human verified: new Claude Code session works with CJS hooks
</verification>

<success_criteria>
- Settings.json successfully switched from Python/Bash to CJS hooks
- Backup exists for rollback safety
- Stop hook timeout extended to 10s via environment variable
- Human confirms: a real Claude Code session lifecycle works with the new hooks
- All existing functionality preserved (GSD hooks, memory context, session naming)
</success_criteria>

<output>
After completion, create `.planning/phases/09-hook-migration/09-04-SUMMARY.md`
</output>
