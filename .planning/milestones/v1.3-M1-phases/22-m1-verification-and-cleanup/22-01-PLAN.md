---
phase: 22-m1-verification-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dynamo/tests/m1-verification.test.cjs
  - .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - ARCH-05
  - ARCH-06
  - ARCH-07
  - MGMT-01
  - MGMT-08a
  - MGMT-08b
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Full test suite (479+ tests) passes with zero failures"
    - "Tmpdir sandbox fresh install produces all 6 subsystem directories and key files"
    - "Sync round-trip shows 8/8 pairs in sync between repo and tmpdir deployment"
    - "Hook dispatcher accepts valid JSON and rejects invalid JSON with boundary markers in output"
    - "SQLite migration converts sample sessions.json to SQLite database and fallback works when node:sqlite unavailable"
    - "Circular dependency detection test passes with no new cycles"
    - "Boundary enforcement tests pass confirming six-subsystem isolation"
  artifacts:
    - path: "dynamo/tests/m1-verification.test.cjs"
      provides: "End-to-end M1 verification test suite"
      min_lines: 150
    - path: ".planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md"
      provides: "Draft verification report documenting all check results"
      contains: "Requirement Validation Matrix"
  key_links:
    - from: "dynamo/tests/m1-verification.test.cjs"
      to: "subsystems/switchboard/install.cjs"
      via: "copyTree to tmpdir"
      pattern: "copyTree"
    - from: "dynamo/tests/m1-verification.test.cjs"
      to: "lib/layout.cjs"
      via: "getLayoutPaths and getSyncPairs"
      pattern: "getLayoutPaths|getSyncPairs"
---

<objective>
Automated end-to-end verification of all 14 M1 requirements via tmpdir sandbox install, full test suite execution, and targeted smoke tests. Produces a draft VERIFICATION.md report as persistent milestone evidence.

Purpose: Establish that all M1 deliverables (six-subsystem architecture, centralized resolver, layout mapping, sync/install/deploy pipelines, management hardening, SQLite sessions) work correctly before any cleanup changes modify the codebase.
Output: m1-verification.test.cjs test file, draft VERIFICATION.md report
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-m1-verification-and-cleanup/22-CONTEXT.md
@.planning/phases/22-m1-verification-and-cleanup/22-RESEARCH.md

<interfaces>
<!-- Key exports the executor needs from existing modules -->

From lib/layout.cjs:
```javascript
function getLayoutPaths(root) // returns {dynamo, ledger, switchboard, lib, assay, terminus, reverie, cc}
function getSyncPairs(repoRoot, liveDir) // returns array of 8 {repo, live, label, excludes, filesOnly?}
module.exports = { getLayoutPaths, getSyncPairs };
```

From subsystems/switchboard/install.cjs:
```javascript
function copyTree(src, dst, excludes) // returns count of files copied
const INSTALL_EXCLUDES = ['tests', '.last-sync', '.git', '.DS_Store', 'node_modules'];
module.exports = { run, rollback, copyTree, generateConfig, mergeSettings, retirePython, restoreSettings, restorePython };
```

From subsystems/switchboard/sync.cjs:
```javascript
function walkDir(dir, excludes, globExcludes, base, filesOnly) // returns {relPath: hash} map
function diffTrees(srcFiles, dstFiles) // returns {toCopy, toDelete}
module.exports = { run, walkDir, diffTrees, detectConflicts, copyFiles, deleteFiles, SYNC_PAIRS };
```

From lib/core.cjs (re-exports at lines 337-348):
```javascript
// Re-exports: MCPClient, parseSSE, SCOPE, SCOPE_PATTERN, validateGroupId, sanitize, loadSessions, listSessions
```

From subsystems/terminus/session-store.cjs:
```javascript
// SQLite session store with: isAvailable(), open(), close(), getSession(), getAllSessions(), upsertSession(), labelSession(), migrateFromJson()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create M1 verification test suite with tmpdir sandbox and smoke tests</name>
  <files>dynamo/tests/m1-verification.test.cjs</files>
  <read_first>
    - lib/layout.cjs (getLayoutPaths and getSyncPairs -- source of truth for expected directories and sync pairs)
    - subsystems/switchboard/install.cjs (copyTree function and INSTALL_EXCLUDES constant)
    - subsystems/switchboard/sync.cjs (walkDir and diffTrees functions)
    - cc/hooks/dynamo-hooks.cjs (dispatcher for smoke test -- find the dispatch function, input validation, and boundary marker wrapping)
    - subsystems/terminus/session-store.cjs (isAvailable, migrateFromJson, getSession, getAllSessions)
    - dynamo/tests/boundary.test.cjs (existing boundary enforcement patterns to avoid duplication)
    - dynamo/tests/circular-deps.test.cjs (existing circular dep detection to confirm it's already tested)
  </read_first>
  <action>
Create `dynamo/tests/m1-verification.test.cjs` using `node:test` and `node:assert/strict`. This is an integration test file that validates all 14 M1 requirements in a tmpdir sandbox. Structure:

**Setup (before block):**
- Create tmpdir via `fs.mkdtempSync(path.join(os.tmpdir(), 'dynamo-m1-verify-'))`
- Use `install.copyTree(REPO_ROOT, tmpLive, INSTALL_EXCLUDES)` to copy repo to tmpdir
- REPO_ROOT is `path.join(__dirname, '..', '..')` (same pattern as install.cjs)
- INSTALL_EXCLUDES is `['tests', '.last-sync', '.git', '.DS_Store', 'node_modules']`

**Teardown (after block):**
- `fs.rmSync(tmpDir, { recursive: true, force: true })`

**CRITICAL: Do NOT use resolve.cjs in sandbox verification.** resolve.cjs caches paths via lazy singleton `_paths`. Use `layout.getLayoutPaths(tmpLive)` directly for path building in sandbox context.

**Test groups (use `describe` blocks):**

1. **ARCH-01: Six-Subsystem Directory Structure**
   - Assert these directories exist in tmpLive: `subsystems/switchboard`, `subsystems/assay`, `subsystems/ledger`, `subsystems/terminus`, `subsystems/reverie`, `cc/hooks`, `cc/prompts`, `lib`, `dynamo`
   - Use `fs.existsSync(path.join(tmpLive, dir))` for each

2. **ARCH-02: No Ad-Hoc Resolver Functions**
   - Read all `.cjs` files in tmpLive (excluding `dynamo/tests/`) recursively
   - Assert none contain `resolveSibling`, `resolveHandlers`, `resolveCore` as function definitions (i.e., grep for `function resolveSibling\|function resolveHandlers\|function resolveCore`)
   - Use `fs.readFileSync` + string search

3. **ARCH-04 + ARCH-05: Layout Mapping and Sync**
   - Call `layout.getSyncPairs(REPO_ROOT, tmpLive)` -- assert exactly 8 pairs returned
   - Assert labels are: `['root', 'dynamo-meta', 'switchboard', 'assay', 'ledger', 'terminus', 'cc', 'lib']`
   - For each pair, use `sync.walkDir` on both repo and live sides, then `sync.diffTrees` -- assert `toCopy.length === 0` and `toDelete.length === 0` for each pair (all 8 in sync after copyTree)

4. **ARCH-06: Install Pipeline Key Files**
   - Assert these key files exist in tmpLive: `dynamo.cjs`, `lib/resolve.cjs`, `lib/core.cjs`, `lib/layout.cjs`, `cc/hooks/dynamo-hooks.cjs`, `cc/settings-hooks.json`, `subsystems/terminus/session-store.cjs`, `subsystems/terminus/health-check.cjs`, `subsystems/switchboard/install.cjs`, `subsystems/switchboard/sync.cjs`
   - Assert `dynamo.cjs` is executable by checking it contains `#!/usr/bin/env node` or at minimum contains `require(`

5. **MGMT-08a + MGMT-08b: Hook Dispatcher Smoke Test**
   - Require the dispatcher from the REPO (not tmpdir since it needs resolve.cjs): `cc/hooks/dynamo-hooks.cjs`
   - Find the function that validates/dispatches hooks. Read the file first to identify the exact function signature and how to call it in isolation.
   - Test with valid JSON input containing a known event name (e.g., `{"event": "UserPromptSubmit", "additionalContext": "test content"}`) and verify it does not throw
   - Test with malformed JSON (non-JSON string) and verify it handles gracefully (logs error, does not throw)
   - If the dispatcher has a boundary marker wrapping function, verify `additionalContext` output includes boundary markers (look for `BEGIN_CONTEXT` / `END_CONTEXT` or similar markers -- read the dispatcher to confirm exact strings)
   - NOTE: The dispatcher may require stdin or specific calling conventions. Read dynamo-hooks.cjs thoroughly before writing the smoke test. If it reads from stdin, use `child_process.execSync` with `input` option to pipe JSON.

6. **DATA-01 + DATA-02 + DATA-03 + DATA-04: SQLite Session Smoke Test**
   - Require `subsystems/terminus/session-store.cjs`
   - Check `isAvailable()` returns true (we're on Node 24.x with node:sqlite)
   - Create a tmpdir for the SQLite test, write a sample `sessions.json` file with 2 session entries:
     ```json
     {"session-001": {"id": "session-001", "timestamp": "2026-03-20T00:00:00Z", "label": "test session 1"}, "session-002": {"id": "session-002", "timestamp": "2026-03-20T01:00:00Z"}}
     ```
   - Call `migrateFromJson(sessionsJsonPath, dbPath)` -- assert it does not throw and returns a count (or completes successfully)
   - Call `open(dbPath)`, then `getAllSessions()` -- assert 2 sessions returned
   - Call `getSession('session-001')` -- assert it returns the session with label "test session 1"
   - Call `close()` to clean up
   - Clean up tmpdir

**Important notes:**
- Import `node:test` (`describe`, `it`, `before`, `after`) and `node:assert/strict`
- Follow project convention: functional style, no classes, options-based injection where available
- Each test should be independent where possible
- The file should be runnable standalone: `node --test dynamo/tests/m1-verification.test.cjs`
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node --test dynamo/tests/m1-verification.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - dynamo/tests/m1-verification.test.cjs exists and contains `const { describe, it, before, after } = require('node:test')`
    - File contains describe blocks for at minimum: 'ARCH-01', 'ARCH-02', 'ARCH-04', 'ARCH-06', 'MGMT-08', 'DATA-01'
    - File contains `mkdtempSync` for tmpdir creation
    - File contains `copyTree` call for sandbox install
    - File contains `getSyncPairs` and `diffTrees` for sync verification
    - File contains `isAvailable` for SQLite availability check
    - File contains `migrateFromJson` for migration smoke test
    - `node --test dynamo/tests/m1-verification.test.cjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All M1 verification tests pass in tmpdir sandbox: directory structure (ARCH-01), no ad-hoc resolvers (ARCH-02), 8 sync pairs in sync (ARCH-04/05), key files present (ARCH-06), dispatcher handles valid/invalid JSON with boundary markers (MGMT-08a/b), SQLite migration and query works (DATA-01/02/03/04)</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and produce draft VERIFICATION.md report</name>
  <files>.planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md</files>
  <read_first>
    - dynamo/tests/m1-verification.test.cjs (just created in Task 1 -- confirm it passes)
    - .planning/REQUIREMENTS.md (requirement IDs and descriptions for the validation matrix)
    - .planning/phases/22-m1-verification-and-cleanup/22-RESEARCH.md (VERIFICATION.md structure recommendation at lines 340-380)
  </read_first>
  <action>
1. Run the full test suite and capture results:
   ```bash
   node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs 2>&1
   ```
   Record: total tests, pass count, fail count, skip count, duration.

2. Run the M1 verification test specifically and capture per-test results:
   ```bash
   node --test --test-reporter=spec dynamo/tests/m1-verification.test.cjs 2>&1
   ```

3. Run boundary tests:
   ```bash
   node --test dynamo/tests/boundary.test.cjs 2>&1
   ```

4. Run circular dependency tests:
   ```bash
   node --test dynamo/tests/circular-deps.test.cjs 2>&1
   ```

5. Create `.planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md` with this structure:

```markdown
# v1.3-M1 Verification Report (DRAFT)

**Date:** 2026-03-20
**Node.js:** {output of node --version}
**Status:** DRAFT -- pending cleanup verification and real install

## Requirement Validation Matrix

| ID | Requirement | Method | Result | Notes |
|----|-------------|--------|--------|-------|
| ARCH-01 | Six-subsystem directory structure | Tmpdir install structure check | {PASS/FAIL} | {details} |
| ARCH-02 | Centralized dual-layout resolver | Ad-hoc function scan in tmpdir | {PASS/FAIL} | {details} |
| ARCH-03 | Circular dependency detection | circular-deps.test.cjs execution | {PASS/FAIL} | {details} |
| ARCH-04 | Unified layout mapping | getSyncPairs returns 8 pairs | {PASS/FAIL} | {details} |
| ARCH-05 | Sync with new layout | Sync round-trip all 8 pairs | {PASS/FAIL} | {details} |
| ARCH-06 | Install pipeline works | Key files exist in tmpdir | {PASS/FAIL} | {details} |
| ARCH-07 | All tests pass | Full test suite run | {PASS/FAIL} | {total} tests, {pass} pass, {fail} fail |
| MGMT-01 | Node.js + Graphiti check | health-check.test.cjs | {PASS/FAIL} | {details} |
| MGMT-08a | Hook input validation | Dispatcher smoke test | {PASS/FAIL} | {details} |
| MGMT-08b | Boundary markers | Dispatcher smoke test | {PASS/FAIL} | {details} |
| DATA-01 | SQLite session storage | session-store.test.cjs | {PASS/FAIL} | {details} |
| DATA-02 | Session query interface | sessions.test.cjs + m1-verification | {PASS/FAIL} | {details} |
| DATA-03 | JSON-to-SQLite migration | Migration smoke test | {PASS/FAIL} | {details} |
| DATA-04 | Graceful JSON fallback | sessions.test.cjs | {PASS/FAIL} | {details} |

## Test Suite Results

- **Total:** {N}
- **Pass:** {N}
- **Skip:** {N} ({reason})
- **Fail:** {N}
- **Duration:** {N}ms

## Tmpdir Sandbox Verification

- Directory structure: {PASS/FAIL} ({N}/{N} directories)
- Key files: {PASS/FAIL} ({N}/{N} files)
- Sync round-trip: {PASS/FAIL} ({N}/8 pairs in sync)
- Ad-hoc resolver scan: {PASS/FAIL} (no resolveSibling/resolveHandlers/resolveCore found)

## Dispatcher Smoke Test

- Valid JSON accepted: {PASS/FAIL}
- Invalid JSON rejected: {PASS/FAIL}
- Boundary markers present: {PASS/FAIL}

## SQLite Session Smoke Test

- node:sqlite available: {PASS/FAIL}
- Migration from JSON: {PASS/FAIL}
- Session queries work: {PASS/FAIL}

## Issues Found

{List any issues discovered with details. If none: "No issues found during automated verification."}

---
*Draft produced by Phase 22 Plan 01. Finalized after cleanup and real install verification.*
```

Fill in ALL fields with actual results from the test runs. Do not use placeholder text. Every {PASS/FAIL} must be replaced with the actual result.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && test -f .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md && grep -c "PASS\|FAIL" .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md exists
    - File contains "# v1.3-M1 Verification Report"
    - File contains "Requirement Validation Matrix" with all 14 requirement IDs (ARCH-01 through ARCH-07, MGMT-01, MGMT-08a, MGMT-08b, DATA-01 through DATA-04)
    - File contains "Test Suite Results" section with actual test counts (not placeholders)
    - File contains "Tmpdir Sandbox Verification" section
    - File contains "Dispatcher Smoke Test" section
    - File contains "SQLite Session Smoke Test" section
    - No unreplaced `{PASS/FAIL}` or `{N}` placeholders remain in the file
    - grep -c "PASS" returns at least 14 (one per requirement in the matrix)
  </acceptance_criteria>
  <done>Draft VERIFICATION.md produced with actual test results for all 14 M1 requirements. Full test suite passes (479+ tests). No unreplaced placeholders.</done>
</task>

</tasks>

<verification>
All 14 M1 requirements validated via automated tests:
- `node --test dynamo/tests/m1-verification.test.cjs` passes (tmpdir sandbox, dispatcher smoke, SQLite smoke)
- `node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs` passes (full suite 479+ tests)
- `node --test dynamo/tests/boundary.test.cjs` passes (six-subsystem isolation)
- `node --test dynamo/tests/circular-deps.test.cjs` passes (no new circular deps)
- 22-VERIFICATION-DRAFT.md contains 14/14 PASS entries (or documents any failures)
</verification>

<success_criteria>
1. m1-verification.test.cjs exists and passes with all tests green
2. Full test suite (479+ tests) passes with 0 failures
3. 22-VERIFICATION-DRAFT.md exists with all 14 requirement validation rows filled in with actual results
4. No placeholder text remains in VERIFICATION-DRAFT.md
</success_criteria>

<output>
After completion, create `.planning/phases/22-m1-verification-and-cleanup/22-01-SUMMARY.md`
</output>
