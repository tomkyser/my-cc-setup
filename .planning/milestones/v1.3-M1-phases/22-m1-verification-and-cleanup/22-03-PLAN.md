---
phase: 22-m1-verification-and-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md
autonomous: false
requirements:
  - ARCH-01
  - ARCH-06
  - ARCH-07
  - MGMT-01

must_haves:
  truths:
    - "Fresh dynamo install on clean ~/.claude/dynamo/ produces functional deployment"
    - "dynamo health-check reports all stages green on fresh deployment"
    - "Full test suite passes after cleanup changes"
    - "VERIFICATION.md finalized with all results including real install verification"
  artifacts:
    - path: ".planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md"
      provides: "Final verification report with all M1 results"
      contains: "Real Install Verification"
  key_links:
    - from: ".planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md"
      to: ".planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md"
      via: "Incorporates draft results plus real install results"
      pattern: "Requirement Validation Matrix"
---

<objective>
Perform real fresh install verification with scripted backup/restore and finalize the VERIFICATION.md report with complete results.

Purpose: Validate that the cleaned-up codebase (after Plan 02 re-export and dead code changes) produces a fully functional deployment in the real `~/.claude/dynamo/` directory. The real install is the ultimate validation that everything works end-to-end, not just in a tmpdir sandbox.
Output: Finalized VERIFICATION.md report with all 14 requirements validated
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-m1-verification-and-cleanup/22-CONTEXT.md
@.planning/phases/22-m1-verification-and-cleanup/22-RESEARCH.md
@.planning/phases/22-m1-verification-and-cleanup/22-01-SUMMARY.md
@.planning/phases/22-m1-verification-and-cleanup/22-02-SUMMARY.md
@.planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run post-cleanup test suite and M1 verification tests</name>
  <files><!-- read-only: no files created or modified; this task only runs tests to confirm post-cleanup green state --></files>
  <read_first>
    - .planning/phases/22-m1-verification-and-cleanup/22-02-SUMMARY.md (confirm cleanup completed successfully)
    - dynamo/tests/m1-verification.test.cjs (the verification test to re-run)
  </read_first>
  <action>
Re-run the full test suite and M1 verification tests after the Plan 02 cleanup changes to confirm nothing broke:

1. Run full test suite:
```bash
node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs
```
Must report 479+ tests, 0 failures.

2. Run M1 verification specifically:
```bash
node --test dynamo/tests/m1-verification.test.cjs
```
Must pass all tests.

3. Run boundary + circular-deps tests:
```bash
node --test dynamo/tests/boundary.test.cjs dynamo/tests/circular-deps.test.cjs
```
Must pass.

If any test fails, stop and fix the issue before proceeding to the real install checkpoint. The real install must test a green codebase.

Record post-cleanup test results for inclusion in VERIFICATION.md:
- Total tests, pass, fail, skip counts
- Any differences from pre-cleanup counts (Plan 01 baseline)
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs` exits 0
    - Output shows `fail 0`
    - Output shows tests count >= 479 (the new m1-verification.test.cjs adds tests to the total)
  </acceptance_criteria>
  <done>Post-cleanup full test suite passes with 0 failures. M1 verification tests still pass. Codebase is green and ready for real install.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real fresh install with scripted backup/restore</name>
  <files></files>
  <action>
Perform a real fresh install with scripted backup/restore to validate the deployment pipeline end-to-end.

1. Back up the live deployment:
```bash
mv ~/.claude/dynamo/ ~/.claude/dynamo-m1-backup/
```

2. Run fresh install from the repo:
```bash
cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node dynamo.cjs install
```

3. Run health-check to verify all 8 stages:
```bash
node ~/.claude/dynamo/dynamo.cjs health-check
```

4. Run a quick functional check:
```bash
node ~/.claude/dynamo/dynamo.cjs status
```

5. Present results to the user and wait for their decision:
   - "keep" to keep the fresh install (remove backup)
   - "restore" to restore the backup (remove fresh install, restore backup)

If user says "keep":
```bash
rm -rf ~/.claude/dynamo-m1-backup/
```

If user says "restore":
```bash
rm -rf ~/.claude/dynamo/ && mv ~/.claude/dynamo-m1-backup/ ~/.claude/dynamo/
```

WARNING: This temporarily removes the live Dynamo deployment. Other active Claude Code sessions will have hook failures during the window between backup and install completion.
  </action>
  <verify>
    <automated>test -d ~/.claude/dynamo/ && node ~/.claude/dynamo/dynamo.cjs status 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/dynamo/ directory exists after the operation completes
    - `node ~/.claude/dynamo/dynamo.cjs status` exits 0
    - Health-check reported 8/8 stages (may have Docker-related failures if Docker not running -- that is acceptable for the install verification)
    - User confirmed "keep" or "restore"
  </acceptance_criteria>
  <done>Real fresh install verified: install pipeline completed, health-check ran, user confirmed the deployment state.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize VERIFICATION.md with complete results</name>
  <files>.planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION-DRAFT.md (draft from Plan 01 with pre-cleanup results)
    - .planning/phases/22-m1-verification-and-cleanup/22-02-SUMMARY.md (cleanup summary with counts of removed code/fixed comments)
  </read_first>
  <action>
Create the final `.planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md` by combining:

1. **All content from 22-VERIFICATION-DRAFT.md** (pre-cleanup automated verification results)
2. **Post-cleanup test results** from Task 1 of this plan
3. **Real install verification results** from Task 2 of this plan
4. **Cleanup summary** from Plan 02

Update the document:
- Change title from "DRAFT" to final: `# v1.3-M1 Verification Report`
- Change status from "DRAFT -- pending cleanup verification and real install" to "COMPLETE"
- Add `## Post-Cleanup Test Results` section with:
  - Total tests, pass, fail, skip (should match or exceed pre-cleanup)
  - Note any test count changes due to m1-verification.test.cjs being added
- Add `## Real Install Verification` section with:
  - Backup status: OK
  - Fresh install: OK (N steps, all OK) or details of any issues
  - Health-check: 8/8 green (or details of any failures)
  - User confirmation: YES (keep/restore)
- Add `## Cleanup Summary` section with:
  - Re-exports removed: list which ones (parseSSE, SCOPE_PATTERN, sanitize, loadSessions, listSessions)
  - Consumers updated: list which files switched to direct imports
  - Dead code removed: count of lines/instances
  - Stale comments fixed: count of instances
  - Circular dependency allowlist changes: what entries added/removed
- Update the `## Issues Found` section with any issues discovered across all three plans
- Remove the draft footer note

The file should be a comprehensive, permanent record of M1 milestone verification.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && test -f .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md && grep -c "COMPLETE\|PASS\|Real Install" .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/22-m1-verification-and-cleanup/22-VERIFICATION.md exists
    - File contains "# v1.3-M1 Verification Report" (not "DRAFT")
    - File contains "Status:** COMPLETE" or "**Status:** COMPLETE"
    - File contains "Requirement Validation Matrix" with all 14 requirement IDs
    - File contains "Real Install Verification" section
    - File contains "Post-Cleanup Test Results" section
    - File contains "Cleanup Summary" section with re-export and dead code details
    - No unreplaced `{PASS/FAIL}` or `{N}` placeholders remain
    - grep -c "DRAFT" returns 0 (no draft markers remain)
  </acceptance_criteria>
  <done>VERIFICATION.md finalized with complete results: pre-cleanup automated verification, post-cleanup test results, real install verification, and cleanup summary. All 14 M1 requirements documented.</done>
</task>

</tasks>

<verification>
- `node --test dynamo/tests/**/*.test.cjs dynamo/tests/*.test.cjs` passes (post-cleanup)
- `node --test dynamo/tests/m1-verification.test.cjs` passes (post-cleanup)
- Real install completed and user-approved
- VERIFICATION.md exists with all sections filled, no DRAFT markers, all 14 requirements documented
</verification>

<success_criteria>
1. Post-cleanup test suite passes with 0 failures
2. Real fresh install completed successfully (backup/install/health-check/user-confirm)
3. VERIFICATION.md finalized with COMPLETE status and all 14 requirement validations
4. No placeholder text remains in VERIFICATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/22-m1-verification-and-cleanup/22-03-SUMMARY.md`
</output>
