---
phase: 18-restructure-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/resolve.cjs
  - lib/dep-graph.cjs
  - dynamo/tests/resolve.test.cjs
  - dynamo/tests/dep-graph.test.cjs
  - dynamo/tests/circular-deps.test.cjs
autonomous: true
requirements: [ARCH-02, ARCH-03]
must_haves:
  truths:
    - "resolve('ledger', 'search.cjs') returns the correct absolute path in repo layout"
    - "resolve('dynamo', 'core.cjs') returns the correct absolute path in repo layout"
    - "resolve('badname', 'file.cjs') throws with unknown subsystem error listing known subsystems"
    - "resolve('ledger', 'nonexistent.cjs') throws with not-found error including checked path"
    - "buildGraph() discovers all production .cjs files across dynamo/, ledger/, switchboard/ directories"
    - "detectCycles() finds cycles in a graph with cycles and returns empty for acyclic graphs"
    - "detectCycles() suppresses allowlisted cycles (core.cjs <-> ledger modules)"
    - "No non-allowlisted circular require() chains exist in the current production codebase"
  artifacts:
    - path: "lib/resolve.cjs"
      provides: "Centralized dual-layout resolver with logical name API"
      exports: ["resolve (default export)"]
      contains: "detectLayout"
    - path: "lib/dep-graph.cjs"
      provides: "Dependency graph builder and DFS cycle detector"
      exports: ["extractRequires", "buildGraph", "detectCycles"]
    - path: "dynamo/tests/resolve.test.cjs"
      provides: "Unit tests for resolver module"
      contains: "resolve("
    - path: "dynamo/tests/dep-graph.test.cjs"
      provides: "Unit tests for dep-graph module"
      contains: "detectCycles"
    - path: "dynamo/tests/circular-deps.test.cjs"
      provides: "Structural test detecting circular require() chains"
      contains: "ALLOWLIST"
  key_links:
    - from: "dynamo/tests/circular-deps.test.cjs"
      to: "lib/dep-graph.cjs"
      via: "require(path.join(REPO_ROOT, 'lib', 'dep-graph.cjs'))"
      pattern: "buildGraph.*detectCycles"
    - from: "lib/resolve.cjs"
      to: "fs.existsSync"
      via: "layout detection and path validation"
      pattern: "existsSync"
---

<objective>
Create the lib/ shared substrate directory with the centralized dual-layout resolver and dependency graph cycle detector modules, plus comprehensive tests for both.

Purpose: These modules are the foundation that Phase 18 Plan 02 migrates all production code to use. The resolver replaces 16+ ad-hoc resolveCore/resolveSibling/resolveHandlers functions. The dep-graph module provides static circular dependency detection that runs as a test.

Output: 2 library modules (lib/resolve.cjs, lib/dep-graph.cjs), 3 test files (resolve.test.cjs, dep-graph.test.cjs, circular-deps.test.cjs)
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-restructure-prerequisites/18-CONTEXT.md
@.planning/phases/18-restructure-prerequisites/18-RESEARCH.md

<interfaces>
<!-- Source files the resolver will resolve to (verify these exist for test assertions) -->
From dynamo/core.cjs (lines 320-355):
```javascript
module.exports = {
  DYNAMO_DIR, output, error, safeReadFile, loadConfig,
  loadEnv, detectProject, fetchWithTimeout, logError,
  healthGuard, loadPrompt, isEnabled
};
// ... then Object.assign re-exports MCPClient, parseSSE, SCOPE, etc. from ledger
```

From switchboard/sync.cjs (line 32-36):
```javascript
const SYNC_PAIRS = [
  { repo: path.join(REPO_ROOT, 'dynamo'), live: LIVE_DIR, label: 'dynamo', excludes: [...SYNC_EXCLUDES, 'tests'] },
  { repo: path.join(REPO_ROOT, 'ledger'), live: path.join(LIVE_DIR, 'ledger'), label: 'ledger', excludes: SYNC_EXCLUDES },
  { repo: path.join(REPO_ROOT, 'switchboard'), live: path.join(LIVE_DIR, 'switchboard'), label: 'switchboard', excludes: SYNC_EXCLUDES },
];
```

Existing resolveCore pattern (repeated 14x):
```javascript
function resolveCore() {
  const deployed = path.join(__dirname, '..', 'core.cjs');
  if (fs.existsSync(deployed)) return deployed;
  return path.join(__dirname, '..', 'dynamo', 'core.cjs');
}
```

core.cjs resolveLedger pattern (lines 338-342):
```javascript
function resolveLedger(moduleName) {
  const deployed = path.join(__dirname, 'ledger', moduleName);
  if (fs.existsSync(deployed)) return deployed;
  return path.join(__dirname, '..', 'ledger', moduleName);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/resolve.cjs centralized resolver and its unit tests</name>
  <files>lib/resolve.cjs, dynamo/tests/resolve.test.cjs</files>
  <read_first>
    - dynamo/core.cjs (see resolveCore/resolveLedger patterns to replace, lines 338-355)
    - dynamo/dynamo.cjs (see resolveSibling pattern, lines 14-18)
    - dynamo/hooks/dynamo-hooks.cjs (see resolveHandlers pattern, lines 12-18)
    - switchboard/stages.cjs (see resolveCore pattern, lines 10-13)
    - .planning/phases/18-restructure-prerequisites/18-RESEARCH.md (full resolver spec with code examples)
  </read_first>
  <behavior>
    - Test 1 (repo layout detection): When resolve.cjs is at `<repo>/lib/resolve.cjs` and `<repo>/dynamo/core.cjs` exists but `<repo>/core.cjs` does not exist, detectLayout returns 'repo'
    - Test 2 (resolve repo path): `resolve('ledger', 'search.cjs')` returns absolute path ending in `ledger/search.cjs` when file exists
    - Test 3 (resolve dynamo): `resolve('dynamo', 'core.cjs')` returns absolute path ending in `dynamo/core.cjs` when file exists
    - Test 4 (resolve switchboard): `resolve('switchboard', 'sync.cjs')` returns absolute path ending in `switchboard/sync.cjs` when file exists
    - Test 5 (resolve lib): `resolve('lib', 'resolve.cjs')` returns absolute path ending in `lib/resolve.cjs`
    - Test 6 (unknown subsystem throws): `resolve('bogus', 'file.cjs')` throws Error containing "unknown subsystem 'bogus'" and "Known:"
    - Test 7 (missing file throws): `resolve('ledger', 'nonexistent-file.cjs')` throws Error containing "not found" and the checked path
    - Test 8 (cache reset): After calling resolve._reset(), layout is re-detected on next call
    - Test 9 (future subsystem names): `resolve('assay', 'foo.cjs')` throws not-found (not unknown-subsystem) because 'assay' is a known subsystem name even though directory doesn't exist yet
    - Test 10 (directory resolution): `resolve('ledger', 'hooks')` returns path when `ledger/hooks/` directory exists (fs.existsSync works for dirs)
  </behavior>
  <action>
    Create `lib/` directory at repo root. Create `lib/resolve.cjs` implementing:

    1. Layout auto-detection via `detectLayout()`:
       - `const root = path.join(__dirname, '..')`
       - Check `fs.existsSync(path.join(root, 'core.cjs'))` -- if true, layout is 'deployed' (core.cjs is at root in deployed layout)
       - Otherwise layout is 'repo' (core.cjs is inside dynamo/ subdirectory)
       - Cache result in module-level `_layout` variable

    2. Path map via `getPaths()`:
       - For 'deployed' layout, root is `~/.claude/dynamo/`:
         ```
         dynamo: root, ledger: root/ledger, switchboard: root/switchboard, lib: root/lib,
         assay: root/subsystems/assay, terminus: root/subsystems/terminus,
         reverie: root/subsystems/reverie, cc: root/cc
         ```
       - For 'repo' layout, root is `<repo>/`:
         ```
         dynamo: root/dynamo, ledger: root/ledger, switchboard: root/switchboard, lib: root/lib,
         assay: root/subsystems/assay, terminus: root/subsystems/terminus,
         reverie: root/subsystems/reverie, cc: root/cc
         ```
       - Cache result in module-level `_paths` variable

    3. Main `resolve(subsystem, file)` function:
       - Look up subsystem in paths map; throw with "unknown subsystem" listing known names if not found
       - Build `fullPath = path.join(dir, file)`
       - Check `fs.existsSync(fullPath)`; throw with "not found in {layout} layout. Checked: {fullPath}" if missing
       - Return fullPath

    4. Export: `module.exports = resolve` (function is the default export)
       - Also attach `resolve._reset = () => { _layout = null; _paths = null; }` for test isolation
       - Also attach `resolve._detectLayout = detectLayout` for test inspection

    Add `'use strict';` and module identity comment `// Dynamo > Lib > resolve.cjs` at top.

    Then create `dynamo/tests/resolve.test.cjs` with node:test implementing all behavior tests above. Tests run against the real repo layout (no mocking needed for repo-layout tests). Use `resolve._reset()` in a `beforeEach` to clear cache between tests.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node --test dynamo/tests/resolve.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/resolve.cjs exists and starts with `'use strict';`
    - lib/resolve.cjs contains `function detectLayout()`
    - lib/resolve.cjs contains `function getPaths()`
    - lib/resolve.cjs contains `function resolve(subsystem, file)`
    - lib/resolve.cjs contains `module.exports = resolve`
    - lib/resolve.cjs contains `resolve._reset`
    - lib/resolve.cjs layout map contains all 8 subsystem keys: dynamo, ledger, switchboard, lib, assay, terminus, reverie, cc
    - lib/resolve.cjs error messages contain `unknown subsystem` for bad subsystem and `not found` for missing file
    - dynamo/tests/resolve.test.cjs exists and uses `require('node:test')`
    - dynamo/tests/resolve.test.cjs contains at least 8 `it(` or `test(` blocks
    - `node --test dynamo/tests/resolve.test.cjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>lib/resolve.cjs passes all unit tests covering repo layout detection, path resolution for all current subsystems, error throwing for unknown subsystems and missing files, cache reset, future subsystem name acceptance, and directory resolution</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create lib/dep-graph.cjs dependency graph module and its tests including circular dependency detection</name>
  <files>lib/dep-graph.cjs, dynamo/tests/dep-graph.test.cjs, dynamo/tests/circular-deps.test.cjs</files>
  <read_first>
    - dynamo/tests/boundary.test.cjs (existing extractRequires pattern at lines 26-38 to align with)
    - dynamo/core.cjs (lines 338-355 -- Object.assign re-export cycle to understand allowlist needs)
    - ledger/mcp-client.cjs (first 15 lines -- to see it requires core.cjs, forming the known cycle)
    - ledger/scope.cjs (first 10 lines -- to see if it requires core.cjs)
    - ledger/sessions.cjs (first 10 lines -- to see if it requires core.cjs)
    - .planning/phases/18-restructure-prerequisites/18-RESEARCH.md (dep-graph spec with code examples, allowlist definition)
  </read_first>
  <behavior>
    dep-graph.test.cjs:
    - Test 1 (extractRequires string literal): Given source `const x = require('./foo.cjs');`, extractRequires returns array containing resolved path for `./foo.cjs`
    - Test 2 (extractRequires node: builtin skip): Given source `require('node:fs')`, extractRequires returns empty array
    - Test 3 (extractRequires npm skip): Given source `require('some-package')`, extractRequires returns empty array (no `.` or `/` prefix)
    - Test 4 (extractRequires path.join pattern): Given source `require(path.join(__dirname, 'bar.cjs'))`, extractRequires returns array containing resolved path
    - Test 5 (buildGraph scans directory): Given a tmpdir with 2 .cjs files requiring each other, buildGraph returns Map with 2 entries
    - Test 6 (buildGraph excludes test files): Given a tmpdir with a .test.cjs file, that file is not in the graph
    - Test 7 (detectCycles finds cycle): Given graph A->B->A, detectCycles returns array with one cycle
    - Test 8 (detectCycles no cycle): Given graph A->B->C (no back edges), detectCycles returns empty array
    - Test 9 (detectCycles allowlist): Given graph A->B->A with allowlist [A, B], detectCycles returns empty array
    - Test 10 (detectCycles multi-node cycle): Given graph A->B->C->A, detectCycles returns the cycle

    circular-deps.test.cjs:
    - Test 1: Build graph of entire production codebase (dynamo/, ledger/, switchboard/, lib/), run detectCycles with allowlist for known core.cjs <-> ledger module cycles, assert zero non-allowlisted cycles found
  </behavior>
  <action>
    Create `lib/dep-graph.cjs` implementing three exported functions:

    1. `extractRequires(filePath, source)`:
       - Regex pattern for `require('...')` and `require("...")`: `/require\(\s*['"]([^'"]+)['"]\s*\)/g`
       - Skip targets starting with `node:` (builtins)
       - Skip targets not starting with `.` or `/` (npm packages)
       - Resolve relative paths from filePath's directory using `require.resolve(path.resolve(dir, target))` in try/catch
       - Regex pattern for `require(path.join(__dirname, ...))`: `/require\(\s*path\.join\(\s*__dirname\s*,\s*(['"][^'"]+['"](?:\s*,\s*['"][^'"]+['"])*)\s*\)\s*\)/g`
       - Extract string args, resolve with `path.join(dir, ...args)`, resolve with require.resolve in try/catch
       - Return deduplicated array via `[...new Set(requires)]`

    2. `buildGraph(dirs, options)`:
       - `options.excludeDirs` defaults to `['tests', 'node_modules', '.planning', '.git']`
       - `options.excludePatterns` defaults to `['.test.cjs']`
       - Recursively scan each dir for .cjs files, skipping excluded dirs and patterns
       - For each file: read contents, call extractRequires, add to Map
       - Return `Map<string, string[]>` (filePath -> dependency paths)

    3. `detectCycles(graph, allowlist)`:
       - DFS with visited set and inStack set
       - When a node in inStack is encountered, extract cycle from stack
       - Filter out allowlisted cycles via `isAllowlisted()` helper
       - `isAllowlisted(cycle, allowlist)` checks if cycle files match any allowlist entry (set comparison)
       - Return array of cycle arrays

    Add `'use strict';` and module identity comment `// Dynamo > Lib > dep-graph.cjs`.
    Export: `module.exports = { extractRequires, buildGraph, detectCycles };`

    Then create `dynamo/tests/dep-graph.test.cjs` with unit tests using tmpdir-based file fixtures.

    Then create `dynamo/tests/circular-deps.test.cjs`:
    - Imports buildGraph and detectCycles from `lib/dep-graph.cjs`
    - Defines ALLOWLIST array with known intentional cycles. To determine the exact cycles, first read core.cjs lines 338-355 to identify which ledger modules it requires (mcp-client.cjs, scope.cjs, sessions.cjs), then check each of those files to see if they require core.cjs back. The allowlist entries are arrays of absolute paths like:
      ```
      [path.join(REPO_ROOT, 'dynamo', 'core.cjs'), path.join(REPO_ROOT, 'ledger', 'mcp-client.cjs')]
      ```
    - Builds graph scanning dynamo/, ledger/, switchboard/, lib/ directories
    - Calls detectCycles with the allowlist
    - Asserts zero cycles found; on failure, formats cycle paths as relative paths for readable error
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node --test dynamo/tests/dep-graph.test.cjs && node --test dynamo/tests/circular-deps.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - lib/dep-graph.cjs exists and starts with `'use strict';`
    - lib/dep-graph.cjs contains `function extractRequires(`
    - lib/dep-graph.cjs contains `function buildGraph(`
    - lib/dep-graph.cjs contains `function detectCycles(`
    - lib/dep-graph.cjs contains `module.exports = { extractRequires, buildGraph, detectCycles }`
    - dynamo/tests/dep-graph.test.cjs exists and contains at least 8 `it(` or `test(` blocks
    - dynamo/tests/circular-deps.test.cjs exists and contains `ALLOWLIST`
    - dynamo/tests/circular-deps.test.cjs scans at least `path.join(REPO_ROOT, 'dynamo')` and `path.join(REPO_ROOT, 'ledger')` and `path.join(REPO_ROOT, 'switchboard')`
    - `node --test dynamo/tests/dep-graph.test.cjs` exits 0
    - `node --test dynamo/tests/circular-deps.test.cjs` exits 0
  </acceptance_criteria>
  <done>lib/dep-graph.cjs passes all unit tests for require extraction, graph building, and DFS cycle detection. The circular-deps.test.cjs structural test confirms no non-allowlisted circular require() chains exist in the production codebase.</done>
</task>

</tasks>

<verification>
All 3 new test files pass:
```bash
node --test dynamo/tests/resolve.test.cjs dynamo/tests/dep-graph.test.cjs dynamo/tests/circular-deps.test.cjs
```

Existing test suite still passes (no regressions from adding lib/):
```bash
node --test dynamo/tests/*.test.cjs dynamo/tests/ledger/*.test.cjs dynamo/tests/switchboard/*.test.cjs
```
</verification>

<success_criteria>
- lib/ directory exists at repo root with resolve.cjs and dep-graph.cjs
- resolve.cjs implements logical name API: resolve('subsystem', 'file') with layout auto-detection
- dep-graph.cjs implements extractRequires, buildGraph, detectCycles
- All new tests pass (resolve, dep-graph, circular-deps)
- All existing 374+ tests pass (no regressions)
- Circular dependency test confirms no non-allowlisted cycles in current codebase
</success_criteria>

<output>
After completion, create `.planning/phases/18-restructure-prerequisites/18-01-SUMMARY.md`
</output>
