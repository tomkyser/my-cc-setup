---
phase: 14-documentation-and-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - STAB-01
  - STAB-03

must_haves:
  truths:
    - "README describes Dynamo as a CJS-based Claude Code platform with Ledger and Switchboard subsystems"
    - "README contains a Mermaid architecture diagram showing the 3-component architecture"
    - "README installation instructions reference dynamo install CLI command, not install.sh"
    - "README CLI reference matches all commands from dynamo.cjs showHelp()"
    - "README contains zero references to Python, Bash hooks, graphiti-helper.py, install.sh, or my-cc-setup"
    - "README covers all 10 specified sections: What It Does, Architecture, Installation, CLI Commands, Hook System, Configuration, Scoping, Troubleshooting, Development Guide, Design Decisions"
  artifacts:
    - path: "README.md"
      provides: "Comprehensive single-file documentation for Dynamo"
      contains: "```mermaid"
  key_links:
    - from: "README.md"
      to: "dynamo/dynamo.cjs"
      via: "CLI command reference"
      pattern: "dynamo (search|remember|recall|health-check|diagnose|start|stop)"
    - from: "README.md"
      to: "dynamo/hooks/dynamo-hooks.cjs"
      via: "Hook system documentation"
      pattern: "SessionStart|UserPromptSubmit|PostToolUse|PreCompact|Stop"
---

<objective>
Rewrite README.md from scratch to reflect the current Dynamo CJS architecture, replacing the entirely outdated Python/Bash documentation.

Purpose: The current README describes a legacy system that no longer exists. This README must serve as the single comprehensive documentation artifact for both human developers and future Claude sessions, covering all 10 required sections with Mermaid diagrams, CLI reference, and code examples.

Output: A complete README.md (~300-500 lines) covering What It Does, Architecture (Mermaid), Installation, CLI Commands, Hook System, Configuration, Scoping, Troubleshooting, Development Guide, and Design Decisions.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-documentation-and-branding/14-CONTEXT.md
@.planning/phases/14-documentation-and-branding/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete README.md from scratch</name>
  <files>README.md</files>
  <read_first>
    README.md
    dynamo/dynamo.cjs
    dynamo/core.cjs
    dynamo/hooks/dynamo-hooks.cjs
    dynamo/config.json
    switchboard/install.cjs
    switchboard/sync.cjs
    claude-config/CLAUDE.md.template
    claude-config/settings-hooks.json
    .planning/phases/14-documentation-and-branding/14-RESEARCH.md
  </read_first>
  <action>
Delete the entire existing README.md content and write a completely new README from scratch. The tone is developer-to-developer, technical, direct, no hand-holding. Heavy on code examples and diagrams, concise prose. Assumes reader is a power user or Claude session.

The README MUST contain exactly these 10 sections in this order:

1. **Title + tagline**: `# Dynamo` followed by a one-line description: "A Claude Code power-user platform for persistent memory and self-management, built on Node/CJS with a Graphiti knowledge graph backend."

2. **What It Does**: 6 bullet points covering the core capabilities. Extract from the 5 hook behaviors (session start context injection, prompt augmentation, change tracking, pre-compaction preservation, session summarization) plus the CLI-based memory operations. Do NOT reference MCP tools directly -- they are an internal implementation detail now wrapped by the CLI.

3. **Architecture (Mermaid)**: Use the Mermaid diagram from 14-RESEARCH.md (the `graph TB` diagram showing Claude Code -> Hook Dispatcher + CLI Router -> Ledger/Switchboard -> MCP Client -> Docker stack). Follow it with the directory tree showing the 3-directory structure. Use the exact tree from 14-RESEARCH.md section "Current 3-Directory Architecture":

```
dynamo/                 # Orchestration layer
  dynamo.cjs            # CLI router (25 commands)
  core.cjs              # Shared substrate
  config.json           # Runtime config
  VERSION               # Semantic version
  hooks/
    dynamo-hooks.cjs    # Single hook dispatcher (5 events)
  prompts/              # Curation prompt templates
  tests/                # All tests (272+)

ledger/                 # Memory subsystem
  mcp-client.cjs        # MCPClient + SSE parsing
  scope.cjs             # Scope constants and validation
  search.cjs            # Combined/fact/node search
  episodes.cjs          # Episode add/extract
  curation.cjs          # Haiku curation pipeline
  sessions.cjs          # Session management
  hooks/                # 5 hook handlers
  graphiti/             # Docker infrastructure
    docker-compose.yml
    config.yaml
    start-graphiti.sh
    stop-graphiti.sh

switchboard/            # Operations subsystem
  install.cjs           # CJS installer
  sync.cjs              # Bidirectional sync
  health-check.cjs      # 6-stage health check
  diagnose.cjs          # 13-stage diagnostics
  verify-memory.cjs     # Pipeline verification
  stack.cjs             # Docker start/stop
  stages.cjs            # Shared diagnostic stages
  pretty.cjs            # Human-readable formatters

claude-config/          # Integration templates
  CLAUDE.md.template    # Memory system rules
  settings-hooks.json   # Hook definitions
```

4. **Installation**: Steps: (a) Clone repo from `https://github.com/tomkyser/dynamo`, (b) Create `.env` at `ledger/graphiti/.env` with `OPENROUTER_API_KEY`, (c) Run `node dynamo/dynamo.cjs install`, (d) Start stack with `node dynamo/dynamo.cjs start`, (e) Restart Claude Code. Note that the repo name is `dynamo` (not `my-cc-setup`). Include the deployed layout tree from 14-RESEARCH.md showing `~/.claude/dynamo/` structure.

5. **CLI Commands**: Table of all commands extracted from `dynamo.cjs` showHelp(). Group into categories:
   - Memory Operations: search, remember, recall, edge, forget, clear
   - Session Management: session (list, view, label, backfill)
   - System Operations: start, stop, install, rollback, sync, toggle, status, version
   - Diagnostics: health-check, diagnose, verify-memory, test

   Include usage examples for the most common operations. Show `--scope` and `--format` options.

6. **Hook System**: Document all 5 hooks with their trigger events, what they do, and their exit behavior. Extract from `dynamo-hooks.cjs`. Key points: single dispatcher, toggle gate (exits silently when disabled), project auto-detection, foreground execution with timeouts. Use the hook dispatcher flow from 14-RESEARCH.md.

7. **Configuration**: Document `config.json` structure using the exact defaults from 14-RESEARCH.md. Document `.env` variables needed (OPENROUTER_API_KEY). Document `settings-hooks.json` hook definitions.

8. **Scoping**: Table with 4 scope types (global, project-NAME, session-TIMESTAMP, task-DESCRIPTOR). Note the dash separator (NOT colon). Document auto-detection from git remote/package.json/composer.json. Show scope usage examples with CLI.

9. **Troubleshooting**: Common issues and fixes. Include:
   - "Dynamo is disabled" error -> `dynamo toggle on` or `DYNAMO_DEV=1` for dev
   - Stack not starting -> `dynamo health-check`, check Docker
   - Stale session IDs -> restart Claude Code
   - Hook errors -> check `~/.claude/dynamo/hook-errors.log`
   - Health check failures -> `dynamo diagnose` for 13-stage deep inspection

10. **Development Guide**: Document the dev workflow: edit in repo -> `dynamo sync` to deploy -> test. Show SYNC_PAIRS mapping (dynamo/ -> ~/.claude/dynamo/, ledger/ -> ~/.claude/dynamo/ledger/, switchboard/ -> ~/.claude/dynamo/switchboard/). Document test running (`dynamo test` or `node --test`). Document toggle for dev: `DYNAMO_DEV=1` overrides global off.

11. **Design Decisions**: Summary table only (not full detail -- that lives in PROJECT.md). Include all entries from the Key Decisions table in PROJECT.md. Each row: Decision | Rationale | Outcome. Add a note: "For detailed decision context, alternatives, and implications, see `.planning/PROJECT.md`."

12. **License**: `MIT`

CRITICAL constraints:
- ZERO references to: graphiti-helper.py, install.sh, any .sh hook script, Python venv, jq, my-cc-setup, MCP tool direct usage (mcp__graphiti__*)
- All scope examples use dash separators (project-name, NOT project:name)
- All paths reference ~/.claude/dynamo/ for deployed code
- Docker infrastructure correctly at ledger/graphiti/ in repo and ~/.claude/graphiti/ deployed
- GitHub repo URL is https://github.com/tomkyser/dynamo
  </action>
  <verify>
    <automated>grep -c "graphiti-helper\|install\.sh\|\.sh.*hook\|my-cc-setup\|mcp__graphiti\|project:name\|\.venv" README.md | grep -q "^0$" && echo "PASS: No stale references" || echo "FAIL: Stale references found"</automated>
  </verify>
  <acceptance_criteria>
    - README.md first line is `# Dynamo`
    - README.md contains "```mermaid" (Mermaid diagram present)
    - README.md contains "## What It Does"
    - README.md contains "## Architecture"
    - README.md contains "## Installation"
    - README.md contains "## CLI Commands"
    - README.md contains "## Hook System"
    - README.md contains "## Configuration"
    - README.md contains "## Scoping"
    - README.md contains "## Troubleshooting"
    - README.md contains "## Development Guide"
    - README.md contains "## Design Decisions"
    - `grep -c "graphiti-helper" README.md` returns 0
    - `grep -c "install\.sh" README.md` returns 0
    - `grep -c "my-cc-setup" README.md` returns 0
    - `grep -c "mcp__graphiti" README.md` returns 0
    - `grep -c "project:name\|project:myapp" README.md` returns 0
    - README.md contains "https://github.com/tomkyser/dynamo"
    - README.md contains "dynamo install" (CJS installer reference)
    - README.md contains "dynamo start" and "dynamo stop"
    - README.md contains "OPENROUTER_API_KEY"
    - README.md contains "dynamo search" and "dynamo remember"
    - README.md contains "SessionStart" and "UserPromptSubmit" and "PostToolUse" and "PreCompact" and "Stop"
    - README.md contains "project-" (dash scope separator used)
    - README.md contains "MIT"
  </acceptance_criteria>
  <done>README.md is a complete, accurate single-file documentation artifact for Dynamo with all 10 required sections, Mermaid architecture diagram, CLI command reference matching dynamo.cjs, and zero references to the legacy Python/Bash system.</done>
</task>

</tasks>

<verification>
- `grep -c "graphiti-helper\|install\.sh\|\.sh.*hook\|my-cc-setup\|mcp__graphiti" README.md` returns 0
- README.md contains all 10 section headers
- Mermaid code block present
- CLI commands match dynamo.cjs showHelp() output
- All scope examples use dash separators
</verification>

<success_criteria>
- README.md fully rewritten with all 10 sections
- Zero legacy references (Python, Bash hooks, my-cc-setup, MCP tools)
- Mermaid architecture diagram renders on GitHub
- CLI reference matches actual dynamo.cjs commands
- Installation instructions reference CJS installer
- GitHub repo URL is https://github.com/tomkyser/dynamo
</success_criteria>

<output>
After completion, create `.planning/phases/14-documentation-and-branding/14-01-SUMMARY.md`
</output>
