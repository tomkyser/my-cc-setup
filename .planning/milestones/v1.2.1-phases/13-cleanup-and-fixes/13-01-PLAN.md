---
phase: 13-cleanup-and-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphiti/ (removed)
  - install.sh (removed)
  - sync-graphiti.sh (removed)
autonomous: true
requirements: [STAB-02]

must_haves:
  truths:
    - "Git tag v1.2-legacy-archive exists on the commit immediately before deletion"
    - "The entire graphiti/ directory is removed from the repo"
    - "Root-level install.sh and sync-graphiti.sh are removed from the repo"
    - "ledger/graphiti/ directory is untouched and intact"
    - "Legacy archive at ~/.claude/graphiti-legacy/ exists with the original Python files"
  artifacts:
    - path: "graphiti/"
      provides: "Must NOT exist after this plan"
      contains: "ABSENT"
    - path: "install.sh"
      provides: "Must NOT exist after this plan"
      contains: "ABSENT"
    - path: "sync-graphiti.sh"
      provides: "Must NOT exist after this plan"
      contains: "ABSENT"
    - path: "ledger/graphiti/docker-compose.yml"
      provides: "Phase 12 Docker infra must still exist"
      contains: "graphiti-neo4j"
    - path: "ledger/graphiti/start-graphiti.sh"
      provides: "Phase 12 start script must still exist"
  key_links:
    - from: "git tag v1.2-legacy-archive"
      to: "commit before deletion"
      via: "git tag points to HEAD before git rm"
      pattern: "git tag -l v1.2-legacy-archive"
---

<objective>
Archive the legacy Python/Bash system by creating a git tag for historical reference, then removing all legacy files from the repository.

Purpose: The CJS rewrite (v1.2) has fully replaced the Python/Bash hooks, installer, and sync script. These legacy files are dead code that creates confusion about which system is active. Removing them makes the codebase reflect reality.

Output: Clean repository with only the CJS-based Dynamo system. Git tag `v1.2-legacy-archive` preserves access to the legacy code for historical reference.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cleanup-and-fixes/13-CONTEXT.md
@.planning/phases/13-cleanup-and-fixes/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify legacy archive and create git tag</name>
  <files>N/A (git operations only)</files>
  <read_first>
    - ledger/graphiti/docker-compose.yml (confirm Phase 12 infra exists and will NOT be touched)
    - ledger/graphiti/start-graphiti.sh (confirm Phase 12 script exists)
    - ledger/graphiti/stop-graphiti.sh (confirm Phase 12 script exists)
  </read_first>
  <action>
    1. Verify the current branch is `dev`:
       ```
       git branch --show-current
       ```
       Must output `dev`. If not, STOP and report.

    2. Verify the legacy archive exists at `~/.claude/graphiti-legacy/`:
       ```
       ls ~/.claude/graphiti-legacy/graphiti-helper.py
       ls ~/.claude/graphiti-legacy/diagnose.py
       ls ~/.claude/graphiti-legacy/health-check.py
       ls ~/.claude/graphiti-legacy/hooks/
       ```
       All must exist. If not, STOP and report.

    3. Verify the legacy files exist in the repo (to confirm we are deleting the right things):
       ```
       test -d graphiti/ && test -f install.sh && test -f sync-graphiti.sh
       ```

    4. Verify Phase 12 files exist and will NOT be touched (safety check):
       ```
       test -f ledger/graphiti/docker-compose.yml
       test -f ledger/graphiti/start-graphiti.sh
       test -f ledger/graphiti/stop-graphiti.sh
       ```

    5. Create the archive tag on the current commit:
       ```
       git tag v1.2-legacy-archive
       ```

    6. Verify the tag was created:
       ```
       git tag -l v1.2-legacy-archive
       ```
       Must output `v1.2-legacy-archive`.
  </action>
  <verify>
    <automated>git tag -l v1.2-legacy-archive | grep -q v1.2-legacy-archive && ls ~/.claude/graphiti-legacy/graphiti-helper.py > /dev/null 2>&1 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v1.2-legacy-archive` outputs `v1.2-legacy-archive`
    - `git branch --show-current` outputs `dev`
    - `ls ~/.claude/graphiti-legacy/graphiti-helper.py` exits 0
    - `ledger/graphiti/docker-compose.yml` still exists
    - `ledger/graphiti/start-graphiti.sh` still exists
    - `ledger/graphiti/stop-graphiti.sh` still exists
  </acceptance_criteria>
  <done>Git tag v1.2-legacy-archive exists. Legacy archive at ~/.claude/graphiti-legacy/ verified. Phase 12 files confirmed safe.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all legacy files from repository</name>
  <files>graphiti/ (removed), install.sh (removed), sync-graphiti.sh (removed)</files>
  <read_first>
    - graphiti/README.md (final look at what is being removed)
    - install.sh (confirm this is the legacy Bash installer, not the CJS one)
    - sync-graphiti.sh (confirm this is the legacy Bash sync, not the CJS one)
  </read_first>
  <action>
    1. Remove the entire legacy graphiti/ directory:
       ```
       git rm -r graphiti/
       ```
       This removes all files including: graphiti-helper.py, diagnose.py, health-check.py, hooks/*.sh, docker-compose.yml (the duplicate), config.yaml, .env.example, README.md, requirements.txt, SCOPE_FALLBACK.md, start-graphiti.sh, stop-graphiti.sh, curation/prompts.yaml, .last-sync.

    2. Remove root-level legacy scripts:
       ```
       git rm install.sh
       git rm sync-graphiti.sh
       ```

    3. Verify git status shows ONLY the intended deletions:
       ```
       git status
       ```
       Must show deleted files ONLY under `graphiti/`, plus `install.sh` and `sync-graphiti.sh`.
       Must NOT show any deletions under `ledger/graphiti/`.
       Must NOT show any other unexpected changes.

    4. Commit the removal:
       ```
       git commit -m "remove(legacy): archive and remove Python/Bash system

       Tag v1.2-legacy-archive marks the last commit with legacy files.
       Legacy archive preserved at ~/.claude/graphiti-legacy/.

       Removed:
       - graphiti/ directory (3 Python files, 6 Bash hooks, configs)
       - install.sh (replaced by switchboard/install.cjs)
       - sync-graphiti.sh (replaced by switchboard/sync.cjs)

       Addresses: STAB-02"
       ```

    5. Post-commit verification:
       ```
       test ! -d graphiti/ && test ! -f install.sh && test ! -f sync-graphiti.sh && echo PASS
       test -f ledger/graphiti/docker-compose.yml && echo LEDGER_SAFE
       ```
  </action>
  <verify>
    <automated>test ! -d graphiti/ && test ! -f install.sh && test ! -f sync-graphiti.sh && test -f ledger/graphiti/docker-compose.yml && test -f ledger/graphiti/start-graphiti.sh && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d graphiti/` exits 0 (directory gone)
    - `test ! -f install.sh` exits 0 (file gone)
    - `test ! -f sync-graphiti.sh` exits 0 (file gone)
    - `test -f ledger/graphiti/docker-compose.yml` exits 0 (Phase 12 infra intact)
    - `test -f ledger/graphiti/start-graphiti.sh` exits 0 (Phase 12 script intact)
    - `test -f ledger/graphiti/stop-graphiti.sh` exits 0 (Phase 12 script intact)
    - `git log -1 --oneline` shows the removal commit
    - `git status` is clean (no uncommitted changes)
  </acceptance_criteria>
  <done>All legacy Python/Bash files removed from repo. ledger/graphiti/ Phase 12 infrastructure intact. Removal committed with descriptive message.</done>
</task>

</tasks>

<verification>
1. `git tag -l v1.2-legacy-archive` outputs the tag name
2. `test ! -d graphiti/ && test ! -f install.sh && test ! -f sync-graphiti.sh` exits 0
3. `test -f ledger/graphiti/docker-compose.yml && test -f ledger/graphiti/start-graphiti.sh` exits 0
4. `ls ~/.claude/graphiti-legacy/graphiti-helper.py` exits 0
5. `node --test dynamo/tests/*.test.cjs` passes (regression check)
</verification>

<success_criteria>
- Git tag v1.2-legacy-archive exists and points to the commit before deletion
- Legacy graphiti/ directory, install.sh, and sync-graphiti.sh are removed from the repo
- ledger/graphiti/ directory is completely untouched
- Legacy archive at ~/.claude/graphiti-legacy/ is verified accessible
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-cleanup-and-fixes/13-01-SUMMARY.md`
</output>
