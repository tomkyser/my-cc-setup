---
phase: 13-cleanup-and-fixes
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - ledger/graphiti/docker-compose.yml
  - dynamo/core.cjs
  - README.md
autonomous: false
requirements: [STAB-07, STAB-02]

must_haves:
  truths:
    - "Neo4j admin browser is accessible at http://localhost:7475"
    - "Neo4j Bolt protocol is reachable at bolt://localhost:7687"
    - "Docker stack is healthy after restart with new port mapping"
    - "No stale references to legacy Python/Bash paths remain in active code files"
    - "README.md has a deprecation notice directing users to the Dynamo CLI"
  artifacts:
    - path: "ledger/graphiti/docker-compose.yml"
      provides: "Corrected Bolt port mapping"
      contains: "\"7687:7687\""
    - path: "dynamo/core.cjs"
      provides: "Updated provenance comments (past tense)"
      contains: "ported from Python"
    - path: "README.md"
      provides: "Deprecation notice at top"
      contains: "dynamo"
  key_links:
    - from: "ledger/graphiti/docker-compose.yml"
      to: "~/.claude/graphiti/docker-compose.yml"
      via: "manual cp (dynamo sync deploys to different path)"
      pattern: "7687:7687"
    - from: "dynamo toggle off"
      to: "Docker restart"
      via: "safe restart pattern from Phase 12"
      pattern: "toggle.*off.*stop.*start.*toggle.*on"
---

<objective>
Fix the Neo4j admin browser connectivity by remapping the Bolt port, clean up stale references to legacy paths in active code, and add a README deprecation notice.

Purpose: The Neo4j browser at localhost:7475 cannot connect because the Bolt port is mapped to 7688 externally while the browser defaults to bolt://localhost:7687. Fixing this gives users visual access to the knowledge graph. Cleaning stale references in active code prevents confusion now that the legacy system is removed (Plan 01). The README needs a minimal notice since it entirely describes the removed legacy system.

Output: Working Neo4j browser, clean active codebase with no stale legacy references, README with deprecation notice.
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cleanup-and-fixes/13-CONTEXT.md
@.planning/phases/13-cleanup-and-fixes/13-RESEARCH.md
@.planning/phases/13-cleanup-and-fixes/13-01-SUMMARY.md

<interfaces>
<!-- Key constants the executor needs to understand for the Docker restart -->

From switchboard/stack.cjs line 20-21:
```javascript
const GRAPHITI_DIR = path.join(os.homedir(), '.claude', 'graphiti');
const COMPOSE_FILE = path.join(GRAPHITI_DIR, 'docker-compose.yml');
```

CRITICAL: `dynamo start/stop` reads docker-compose from `~/.claude/graphiti/docker-compose.yml`.
`dynamo sync` deploys to `~/.claude/dynamo/ledger/graphiti/docker-compose.yml` (different path).
Both locations must be updated for the port change to take effect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Bolt port mapping and restart Docker stack</name>
  <files>ledger/graphiti/docker-compose.yml</files>
  <read_first>
    - ledger/graphiti/docker-compose.yml (see current port mapping on line 7)
    - switchboard/stack.cjs (confirm GRAPHITI_DIR is ~/.claude/graphiti on line 20)
  </read_first>
  <action>
    1. Edit `ledger/graphiti/docker-compose.yml` line 7: change `"7688:7687"` to `"7687:7687"`.
       The full ports section for neo4j should read:
       ```yaml
       ports:
         - "7475:7474"
         - "7687:7687"
       ```
       Do NOT change any other lines. The HTTP port `7475:7474`, MCP port `8100:8000`, NEO4J_URI `bolt://neo4j:7687` (internal Docker network), and all other settings remain unchanged.

    2. Toggle Dynamo off for safe restart:
       ```
       node ~/.claude/dynamo/dynamo.cjs toggle off
       ```
       Verify toggle is off:
       ```
       node ~/.claude/dynamo/dynamo.cjs status
       ```
       Must show disabled/off state.

    3. Stop the Docker stack:
       ```
       node ~/.claude/dynamo/dynamo.cjs stop
       ```

    4. Deploy the updated docker-compose to BOTH deployed locations:

       a) Sync to `~/.claude/dynamo/ledger/graphiti/` via dynamo sync:
       ```
       node ~/.claude/dynamo/dynamo.cjs sync
       ```

       b) Copy directly to `~/.claude/graphiti/` where stack.cjs actually reads:
       ```
       cp ledger/graphiti/docker-compose.yml ~/.claude/graphiti/docker-compose.yml
       ```

    5. Start the Docker stack with new port mapping:
       ```
       node ~/.claude/dynamo/dynamo.cjs start
       ```
       Wait for health check to pass.

    6. Re-enable Dynamo:
       ```
       node ~/.claude/dynamo/dynamo.cjs toggle on
       ```

    7. Run full health check:
       ```
       node ~/.claude/dynamo/dynamo.cjs health-check
       ```

    8. Verify Neo4j browser is accessible:
       ```
       curl -sf http://localhost:7475 > /dev/null && echo "Neo4j browser accessible"
       ```

    9. Verify Bolt port is correctly mapped:
       ```
       docker ps --filter name=graphiti-neo4j --format "{{.Ports}}"
       ```
       Must show `7687->7687` (not `7688->7687`).

    10. Verify data volumes are intact (not wiped):
        ```
        node ~/.claude/dynamo/dynamo.cjs health-check --format json
        ```
        Health check should show neo4j and mcp as healthy. If the knowledge graph had data before, it should still be there.
  </action>
  <verify>
    <automated>grep '"7687:7687"' ledger/graphiti/docker-compose.yml > /dev/null && curl -sf http://localhost:7475 > /dev/null && docker ps --filter name=graphiti-neo4j --format "{{.Ports}}" | grep -q "7687->7687" && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"7687:7687"' ledger/graphiti/docker-compose.yml` finds the line (port changed in repo)
    - `grep '"7687:7687"' ~/.claude/graphiti/docker-compose.yml` finds the line (port changed in deployed location)
    - `grep '"7687:7687"' ~/.claude/dynamo/ledger/graphiti/docker-compose.yml` finds the line (port changed in synced location)
    - `curl -sf http://localhost:7475` exits 0 (Neo4j browser accessible)
    - `docker ps --filter name=graphiti-neo4j --format "{{.Ports}}"` contains `7687->7687`
    - `node ~/.claude/dynamo/dynamo.cjs status` shows enabled/on
    - `node ~/.claude/dynamo/dynamo.cjs health-check` shows healthy
  </acceptance_criteria>
  <done>Neo4j Bolt port remapped from 7688 to 7687. Docker stack restarted via toggle-off/toggle-on pattern. Neo4j browser accessible at localhost:7475. Data volumes intact.</done>
</task>

<task type="auto">
  <name>Task 2: Clean stale legacy references and add README deprecation notice</name>
  <files>dynamo/core.cjs, README.md</files>
  <read_first>
    - dynamo/core.cjs (lines 78-115 to see the two provenance comments at lines 82 and 112)
    - README.md (entire file -- it describes the legacy system being removed)
  </read_first>
  <action>
    1. Update provenance comments in `dynamo/core.cjs`:

       Line 82 -- change from:
       ```
       // --- .env loading (port from Python graphiti-helper.py lines 42-52) ---
       ```
       to:
       ```
       // --- .env loading (originally ported from Python graphiti-helper.py) ---
       ```

       Line 112 -- change from:
       ```
       // --- Project detection (port from Python graphiti-helper.py lines 293-359) ---
       ```
       to:
       ```
       // --- Project detection (originally ported from Python graphiti-helper.py) ---
       ```

       These are the only two stale references in active CJS code. The line number references are no longer meaningful since the Python file has been removed.

    2. Add a deprecation notice to the top of `README.md`. Insert the following block at line 1, BEFORE the existing `# Claude Code + Graphiti Memory System` heading:

       ```markdown
       > **Note:** This README describes the original Python/Bash memory system which has been fully replaced by [Dynamo](./dynamo/), a CJS-based rewrite. The information below is outdated. For current usage, run `dynamo --help` after installation. A complete README rewrite is planned for Phase 14.

       ```

       Do NOT modify any other content in README.md. The full rewrite is Phase 14 (STAB-01) scope.

    3. Scan for any remaining stale references in active code files (*.cjs) to legacy paths. Run:
       ```
       grep -rn "graphiti/hooks/" --include="*.cjs" .
       grep -rn "graphiti-helper\.py" --include="*.cjs" .
       grep -rn "diagnose\.py" --include="*.cjs" .
       grep -rn "health-check\.py" --include="*.cjs" .
       ```
       After the core.cjs updates, these should return no results (or only the updated past-tense provenance comments). If any functional references remain, update them.

    4. Commit all Phase 13 changes (docker-compose.yml port fix from Task 1, plus core.cjs and README.md):
       ```
       git add ledger/graphiti/docker-compose.yml dynamo/core.cjs README.md
       git commit -m "fix(13): Neo4j port fix, stale reference cleanup, README deprecation notice

       - Remapped Neo4j Bolt port from 7688 to 7687 in docker-compose.yml
       - Updated provenance comments in core.cjs to past tense (legacy Python removed)
       - Added deprecation notice to README.md (full rewrite deferred to Phase 14)

       Addresses: STAB-02 (reference cleanup), STAB-07 (port fix)"
       ```
  </action>
  <verify>
    <automated>grep -q "originally ported from Python" dynamo/core.cjs && head -1 README.md | grep -q "Note" && node --test dynamo/tests/*.test.cjs 2>&1 | tail -5 && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep "originally ported from Python" dynamo/core.cjs` matches two lines (lines 82 and 112)
    - `grep -c "port from Python graphiti-helper.py lines" dynamo/core.cjs` returns 0 (old comments gone)
    - `head -3 README.md` contains "Note" and "Dynamo" and "outdated"
    - `grep -rn "graphiti/hooks/" --include="*.cjs" .` returns no results
    - `node --test dynamo/tests/*.test.cjs` passes (regression)
    - `git status` is clean after commit
  </acceptance_criteria>
  <done>Provenance comments in core.cjs updated to past tense. README.md has deprecation notice. No stale legacy references remain in active CJS code. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Neo4j browser and knowledge graph visibility</name>
  <files>N/A</files>
  <action>
    Present the following verification steps to the user. This is a human verification checkpoint -- the user must confirm the Neo4j browser works visually.

    Neo4j admin browser connectivity fix (Bolt port remapped from 7688 to 7687) and legacy system removal have been completed. User must verify:

    1. Open http://localhost:7475 in a web browser
    2. The Neo4j Browser should load without errors
    3. It should auto-connect to `bolt://localhost:7687` (check the connection indicator at top)
    4. Run a test Cypher query: `MATCH (n) RETURN count(n)` -- should return a count (proves data volumes survived the restart)
    5. Optionally: `MATCH (n) RETURN n LIMIT 25` to see knowledge graph nodes visually
    6. Verify no errors appear in the browser console

    Wait for user response: "approved" or description of issues.
  </action>
  <verify>
    <automated>curl -sf http://localhost:7475 > /dev/null && echo PASS || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Neo4j browser loads at http://localhost:7475
    - User confirms Cypher query returns data (data volumes intact)
    - User types "approved"
  </acceptance_criteria>
  <done>User has verified Neo4j browser works and displays knowledge graph data.</done>
</task>

</tasks>

<verification>
1. `grep '"7687:7687"' ledger/graphiti/docker-compose.yml` confirms port change in repo
2. `curl -sf http://localhost:7475` confirms Neo4j browser accessible
3. `docker ps --filter name=graphiti-neo4j --format "{{.Ports}}"` shows `7687->7687`
4. `node ~/.claude/dynamo/dynamo.cjs health-check` shows healthy
5. `grep "originally ported from Python" dynamo/core.cjs` confirms comment cleanup
6. `head -3 README.md` shows deprecation notice
7. `node --test dynamo/tests/*.test.cjs` passes
8. Human confirms Neo4j browser works at localhost:7475
</verification>

<success_criteria>
- Neo4j admin browser loads at http://localhost:7475 and auto-connects via Bolt
- Docker stack is healthy with correct port mapping (7687:7687)
- Provenance comments in core.cjs updated to past tense
- README.md has deprecation notice directing to Dynamo CLI
- No stale legacy path references in active CJS code
- All existing tests pass
- Human verification confirms Neo4j browser displays knowledge graph data
</success_criteria>

<output>
After completion, create `.planning/phases/13-cleanup-and-fixes/13-02-SUMMARY.md`
</output>
