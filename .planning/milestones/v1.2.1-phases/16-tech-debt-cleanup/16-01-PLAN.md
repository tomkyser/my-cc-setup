---
phase: 16-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - claude-config/CLAUDE.md.template
  - claude-config/settings-hooks.json
autonomous: true
requirements:
  - STAB-01
  - STAB-03
  - STAB-04
  - STAB-05
  - STAB-10
gap_closure: true

must_haves:
  truths:
    - "README.md System Operations table lists check-update, update, and an updated rollback description"
    - "CLAUDE.md.template System Operations table lists check-update, update, and an updated rollback description"
    - "settings-hooks.json contains zero mcp__graphiti__ permission entries"
    - "dynamo install deploys current code to live ~/.claude/dynamo/"
  artifacts:
    - path: "README.md"
      provides: "CLI reference with Phase 15 commands"
      contains: "check-update"
    - path: "claude-config/CLAUDE.md.template"
      provides: "CLAUDE.md template with Phase 15 commands"
      contains: "check-update"
    - path: "claude-config/settings-hooks.json"
      provides: "Clean hook settings without stale MCP permissions"
  key_links:
    - from: "claude-config/settings-hooks.json"
      to: "~/.claude/settings.json"
      via: "dynamo install merges settings-hooks.json into settings.json"
      pattern: "hooks"
    - from: "claude-config/CLAUDE.md.template"
      to: "~/.claude/CLAUDE.md"
      via: "dynamo install deploys template as CLAUDE.md"
      pattern: "check-update"
---

<objective>
Close all 6 audit gaps (INT-01, INT-02, INT-03, FLOW-01, FLOW-02, FLOW-03) from the v1.2.1 milestone audit by updating documentation for Phase 15 commands, removing stale MCP permission entries, and deploying to live.

Purpose: The Phase 15 update system (check-update, update, rollback) is built and tested but not reflected in user-facing docs and not deployed. Stale Graphiti MCP permission entries remain in settings-hooks.json from the pre-Phase-12 era. This plan closes all gaps so v1.2.1 can ship clean.

Output: Updated README.md, updated CLAUDE.md.template, cleaned settings-hooks.json, live deployment at ~/.claude/dynamo/
</objective>

<execution_context>
@/Users/tom.kyser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tom.kyser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and CLAUDE.md.template CLI tables for Phase 15 commands</name>
  <files>README.md, claude-config/CLAUDE.md.template</files>
  <read_first>README.md, claude-config/CLAUDE.md.template, dynamo/dynamo.cjs</read_first>
  <action>
Update both README.md and CLAUDE.md.template to add the Phase 15 update system commands to their CLI reference tables.

**README.md -- System Operations table (around line 207-216):**

The current table has these rows:
```
| `dynamo install` | Deploy CJS system to `~/.claude/dynamo/` |
| `dynamo rollback` | Restore `settings.json.bak` and undo retirement |
```

Replace/update those two rows and add two new rows. The final System Operations table should contain these rows (in this order, after `stop`):

```
| `dynamo install` | Deploy Dynamo to `~/.claude/dynamo/` (backup, copy, config, hooks, health check) |
| `dynamo rollback` | Restore previous version from backup |
| `dynamo check-update` | Check for available Dynamo updates (current vs latest version) |
| `dynamo update` | Update Dynamo to latest version (backup, pull, migrate, verify, auto-rollback on failure) |
| `dynamo sync <direction>` | Bidirectional sync between repo and live deployment |
```

The rest of the table rows (toggle, status, version) remain unchanged.

**CLAUDE.md.template -- System Operations table (around line 39-48):**

The current table has these rows:
```
| `dynamo install` | Deploy CJS system to ~/.claude/dynamo/ | Initial install or reinstall |
| `dynamo rollback` | Restore settings.json.bak and undo retirement | Reverting a bad install |
```

Replace/update those two rows and add two new rows. The final System Operations table should contain these rows (in this order, after `stop`):

```
| `dynamo install` | Deploy Dynamo to ~/.claude/dynamo/ (backup, copy, config, hooks, health check) | Initial install or reinstall |
| `dynamo rollback` | Restore previous version from backup | Reverting a bad update or install |
| `dynamo check-update` | Check for available Dynamo updates (current vs latest) | Checking if a newer version exists |
| `dynamo update` | Update Dynamo to latest version (backup, pull, migrate, verify, auto-rollback) | Upgrading to a new release |
| `dynamo sync` | Bidirectional sync between repo and live deployment | After editing source files in repo |
```

The rest of the table rows (toggle, status, version) remain unchanged.

**CLAUDE.md.template -- Maintenance > Updating Dynamo section (around line 151-154):**

The current section reads:
```
### Updating Dynamo
- Pull latest from repo: `cd <repo-dir> && git pull`
- Sync changes to live: `dynamo sync`
- Or full reinstall: `dynamo install`
```

Replace it with:
```
### Updating Dynamo
- Check for updates: `dynamo check-update`
- Update to latest version: `dynamo update` (creates backup, pulls code, runs migrations, verifies health, auto-rolls back on failure)
- Or manual: `cd <repo-dir> && git pull` then `dynamo install`
```

**CLAUDE.md.template -- Checking Version section (around line 156-157):**

After "Checking Version", add a new line:
```
- Check for updates: `dynamo check-update`
```

So the section becomes:
```
### Checking Version
- `dynamo version` shows current version
- `dynamo check-update` shows current vs latest version
```

Descriptions must match what the router (dynamo.cjs) actually does per its help text and COMMAND_HELP object:
- `check-update`: "Check for available Dynamo updates" -- shows current vs latest version
- `update`: "Update Dynamo to the latest version" -- creates backup, pulls code, runs migrations, verifies health, auto-rolls back on failure
- `rollback`: "Restore previous version from backup" (NOT the old "Restore settings.json.bak and undo retirement")
  </action>
  <verify>
    <automated>grep -c "check-update" README.md && grep -c "check-update" claude-config/CLAUDE.md.template && grep "dynamo update" README.md | head -2 && grep "dynamo update" claude-config/CLAUDE.md.template | head -2 && grep "rollback" README.md | grep -v "settings.json.bak" | head -1 && grep "rollback" claude-config/CLAUDE.md.template | grep -v "settings.json.bak" | head -1</automated>
  </verify>
  <acceptance_criteria>
- README.md contains at least 1 line with `check-update`
- README.md contains a row with `dynamo update` and description mentioning "latest version"
- README.md `rollback` row says "Restore previous version from backup" (NOT "Restore settings.json.bak and undo retirement")
- CLAUDE.md.template contains at least 2 occurrences of `check-update` (table row + maintenance section)
- CLAUDE.md.template contains a row with `dynamo update` and description mentioning "latest version"
- CLAUDE.md.template `rollback` row says "Restore previous version from backup" (NOT "Restore settings.json.bak and undo retirement")
- CLAUDE.md.template Maintenance > Updating Dynamo section mentions `dynamo check-update` and `dynamo update`
  </acceptance_criteria>
  <done>Both README.md and CLAUDE.md.template have check-update, update, and updated rollback descriptions in their CLI reference tables. CLAUDE.md.template maintenance section reflects the new update workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale mcp__graphiti__ permission entries from settings-hooks.json</name>
  <files>claude-config/settings-hooks.json</files>
  <read_first>claude-config/settings-hooks.json</read_first>
  <action>
Edit `claude-config/settings-hooks.json` to remove the entire `"permissions"` block. The file currently contains:

```json
"permissions": {
    "allow": [
      "mcp__graphiti__add_memory",
      "mcp__graphiti__search_nodes",
      "mcp__graphiti__search_memory_facts",
      "mcp__graphiti__get_episodes",
      "mcp__graphiti__delete_episode",
      "mcp__graphiti__get_entity_edge",
      "mcp__graphiti__get_status"
    ],
    "ask": [
      "mcp__graphiti__clear_graph",
      "mcp__graphiti__delete_entity_edge"
    ]
  },
```

Remove the entire `"permissions"` key and its value from the JSON object. The file should retain only `_comment`, `env`, and `hooks` keys. The resulting file structure should be:

```json
{
  "_comment": "Merge these into your ~/.claude/settings.json. This file shows ONLY the Dynamo/Graphiti-specific additions.",

  "env": {
    "CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS": "10000"
  },

  "hooks": {
    ...existing hooks unchanged...
  }
}
```

Ensure valid JSON (no trailing commas after removing the permissions block). The `"env"` block's closing brace should be followed by a comma, then the `"hooks"` block.
  </action>
  <verify>
    <automated>node -e "const j=require('./claude-config/settings-hooks.json'); if(j.permissions) { console.log('FAIL: permissions still present'); process.exit(1); } else { console.log('PASS: no permissions key'); }" && grep -c "mcp__graphiti__" claude-config/settings-hooks.json; echo "Count should be 0"</automated>
  </verify>
  <acceptance_criteria>
- `claude-config/settings-hooks.json` parses as valid JSON (node require succeeds)
- The JSON object has NO `permissions` key
- The file contains zero occurrences of the string `mcp__graphiti__`
- The file still contains `_comment`, `env`, and `hooks` keys
- All 5 hook event definitions (SessionStart x2, UserPromptSubmit, PostToolUse, PreCompact, Stop) remain intact
  </acceptance_criteria>
  <done>settings-hooks.json contains zero mcp__graphiti__ permission entries. Only _comment, env, and hooks keys remain. File is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy current code to live ~/.claude/dynamo/ via dynamo install</name>
  <files></files>
  <read_first>switchboard/install.cjs</read_first>
  <action>
Run `dynamo install` from the repo root to deploy the current codebase (with the cleaned settings-hooks.json and updated docs) to the live `~/.claude/dynamo/` directory.

Execute:
```bash
node dynamo/dynamo.cjs install
```

This will:
1. Back up existing deployment
2. Copy dynamo/, ledger/, switchboard/ trees to ~/.claude/dynamo/
3. Generate config.json from .env values
4. Merge the now-clean settings-hooks.json into ~/.claude/settings.json (no stale MCP permissions)
5. Register MCP server
6. Run health check

After install completes, verify the deployment:
1. Run `node ~/.claude/dynamo/dynamo.cjs version` to confirm the deployed version matches the repo VERSION file
2. Run `grep -c "mcp__graphiti__" ~/.claude/settings.json` to confirm zero stale permission entries in the deployed settings
3. Run `node ~/.claude/dynamo/dynamo.cjs check-update` to confirm the check-update command works from the deployed path

If install fails, check the error output and retry. The install command is idempotent.
  </action>
  <verify>
    <automated>node ~/.claude/dynamo/dynamo.cjs version 2>/dev/null && node -e "const s=require(require('os').homedir()+'/.claude/settings.json'); const perms=JSON.stringify(s.permissions||{}); const count=(perms.match(/mcp__graphiti__/g)||[]).length; console.log('stale_perms:', count); if(count>0) process.exit(1);" && echo "PASS: deployed and clean"</automated>
  </verify>
  <acceptance_criteria>
- `node ~/.claude/dynamo/dynamo.cjs version` executes without error and outputs a version object
- `~/.claude/settings.json` contains zero `mcp__graphiti__` permission entries
- `node ~/.claude/dynamo/dynamo.cjs check-update` executes without crashing (may show network error if offline, but should not throw)
- The deployed VERSION file matches the repo VERSION file
  </acceptance_criteria>
  <done>Current Dynamo code is deployed to live ~/.claude/dynamo/. The deployed settings.json has no stale MCP permissions. check-update and update commands are available at the deployed path.</done>
</task>

</tasks>

<verification>
All 6 audit gaps closed:
- INT-01 (not deployed): Fixed by Task 3 -- dynamo install deploys Phase 15 code to live
- INT-02 (docs missing commands): Fixed by Task 1 -- README.md and CLAUDE.md.template updated
- INT-03 (stale permissions): Fixed by Task 2 -- settings-hooks.json cleaned
- FLOW-01 (check-update fails deployed): Fixed by Task 3 -- deployed path now has update modules
- FLOW-02 (docs missing update instructions): Fixed by Task 1 -- docs updated
- FLOW-03 (install writes stale perms): Fixed by Task 2 + Task 3 -- clean source deployed

Run this compound check:
```bash
# Doc gaps closed
grep "check-update" README.md && grep "check-update" claude-config/CLAUDE.md.template

# Config gap closed
node -e "const j=require('./claude-config/settings-hooks.json'); console.log('permissions key present:', !!j.permissions)"

# Deploy gap closed
node ~/.claude/dynamo/dynamo.cjs version
```
</verification>

<success_criteria>
1. README.md CLI reference table includes `check-update`, `update`, and updated `rollback` descriptions
2. CLAUDE.md.template CLI reference includes `check-update`, `update`, and updated `rollback` descriptions
3. settings-hooks.json contains zero `mcp__graphiti__*` permission entries
4. `dynamo install` deploys current code to live `~/.claude/dynamo/`
5. Deployed `~/.claude/settings.json` contains zero stale MCP permission entries
</success_criteria>

<output>
After completion, create `.planning/phases/16-tech-debt-cleanup/16-01-SUMMARY.md`
</output>
