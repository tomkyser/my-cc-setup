---
phase: 24-cognitive-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - subsystems/reverie/curation.cjs
  - dynamo/tests/reverie/curation.test.cjs
  - cc/prompts/iv-briefing.md
  - cc/prompts/iv-injection.md
  - cc/prompts/iv-adversarial.md
  - cc/prompts/iv-precompact.md
  - cc/prompts/iv-synthesis.md
autonomous: true
requirements: [IV-07, IV-08, IV-05]

must_haves:
  truths:
    - "Curation functions produce formatted output without calling OpenRouter/Haiku API"
    - "Template fallback produces functional output when subagent spawn is unavailable"
    - "Adversarial counter-prompting templates wrap facts with user-experience qualifiers"
    - "Session-start briefing respects 500-token limit"
    - "Mid-session injection respects 150-token limit"
    - "Urgent injection respects 50-token limit"
  artifacts:
    - path: "subsystems/reverie/curation.cjs"
      provides: "Subagent-based curation with template fallbacks"
      exports: ["curateForInjection", "formatBriefing", "formatSynthesis", "formatPreCompact", "generateSessionName", "TOKEN_LIMITS"]
    - path: "dynamo/tests/reverie/curation.test.cjs"
      provides: "Unit tests for curation module"
      min_lines: 150
    - path: "cc/prompts/iv-briefing.md"
      provides: "Session-start briefing template"
      contains: "briefing"
    - path: "cc/prompts/iv-injection.md"
      provides: "Mid-session injection template"
      contains: "injection"
    - path: "cc/prompts/iv-adversarial.md"
      provides: "Adversarial counter-prompting template"
      contains: "experience"
    - path: "cc/prompts/iv-precompact.md"
      provides: "PreCompact summary template"
      contains: "compact"
    - path: "cc/prompts/iv-synthesis.md"
      provides: "Session-end synthesis template"
      contains: "synthesis"
  key_links:
    - from: "subsystems/reverie/curation.cjs"
      to: "cc/prompts/iv-*.md"
      via: "loadPrompt() from lib/core.cjs"
      pattern: "loadPrompt.*iv-"
    - from: "subsystems/reverie/curation.cjs"
      to: "lib/core.cjs"
      via: "require for loadPrompt, logError"
      pattern: "require.*core"
---

<objective>
Create the Reverie curation module and 5 new Inner Voice prompt templates. curation.cjs migrates curation intelligence from Ledger (which uses OpenRouter/Haiku) to Reverie (which uses template-based formatting for hot path and delegates to the inner-voice subagent for deliberation). The module provides template-based fallback formatting that works without any LLM call, producing reduced-quality but functional injections when subagent spawn is unavailable.

Purpose: This module is the formatting layer that inner-voice.cjs (Plan 03) will import for all injection content generation. The prompt templates define the voice and structure of Inner Voice communications per D-01, D-02, D-03, D-04.
Output: subsystems/reverie/curation.cjs, 5 prompt template files in cc/prompts/
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cognitive-pipeline/24-CONTEXT.md
@.planning/phases/24-cognitive-pipeline/24-RESEARCH.md

<interfaces>
<!-- Existing Ledger curation module being migrated (reference, NOT copied) -->
From subsystems/ledger/curation.cjs:
```javascript
// These functions use OpenRouter/Haiku API -- Reverie replaces with template-based + subagent
module.exports = {
  callHaiku,           // (promptName, variables, options) -> { text, uncurated }
  curateResults,       // (memories, contextText, options) -> string
  summarizeText,       // (text, options) -> string
  generateSessionName  // (summaryText, options) -> string
};
```

From lib/core.cjs:
```javascript
// loadPrompt reads YAML-frontmatter .md files from cc/prompts/
function loadPrompt(promptName) -> { system, user } | null
// Splits on first '---' line: content before = system, content after = user
```

Existing prompt template format (cc/prompts/curation.md):
```markdown
You are a context curator for a software development AI assistant.
Your job is to filter retrieved memories to only what's directly relevant
for starting a new coding session in the given project.

---

PROJECT: {project_name}
SESSION TYPE: {session_type}

RETRIEVED MEMORIES:
{memories}

Return ONLY the 3-5 most relevant items as concise markdown bullets.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 Inner Voice prompt templates in cc/prompts/</name>
  <files>cc/prompts/iv-briefing.md, cc/prompts/iv-injection.md, cc/prompts/iv-adversarial.md, cc/prompts/iv-precompact.md, cc/prompts/iv-synthesis.md</files>
  <read_first>
    - cc/prompts/curation.md (existing template format reference -- system prompt above ---, user prompt below)
    - cc/prompts/session-summary.md (existing template format reference)
    - .planning/phases/24-cognitive-pipeline/24-CONTEXT.md (D-01 contextual narrative, D-02 hot vs deliberation format, D-03 adversarial framing, D-04 session-start format)
    - .planning/phases/24-cognitive-pipeline/24-RESEARCH.md (Pattern 6 token limits: 500/150/50)
  </read_first>
  <action>
    Create 5 prompt template files following the existing format: system prompt above `---` divider, user prompt with `{variable}` placeholders below.

    **1. cc/prompts/iv-briefing.md** (Session-start briefing, D-04, IV-05: 500 token limit):
    System prompt: "You are the Inner Voice of Dynamo, generating a session-start briefing. Frame all information from the user's perspective and experience. Use contextual narrative tone: 'When you worked on X last session, you decided Y because Z.' Never assert canonical definitions -- always qualify with 'as you described it' or 'from your experience'. Keep the briefing under 500 tokens."
    User prompt: Variables `{project_name}`, `{recent_sessions}`, `{active_entities}`, `{predictions}`, `{relationship_model}`. Instruction: "Generate a session-start briefing covering: what was happening, key decisions made, active concerns, and what might come up next. Contextual narrative format -- read like a personal note, not a database dump."

    **2. cc/prompts/iv-injection.md** (Mid-session injection, D-01, D-02, IV-05: 150 token limit):
    System prompt: "You are the Inner Voice of Dynamo, generating a mid-session injection. This surfaces when something crosses the activation threshold -- a relevant memory, pattern, or connection. Keep injections under 150 tokens. Use shorter, bullet-oriented narrative. Frame facts from the user's experience."
    User prompt: Variables `{prompt_text}`, `{crossed_entities}`, `{domain_frame}`, `{activation_context}`, `{self_model}`. Instruction: "Generate a concise injection surfacing the relevant memory or connection. Contextual narrative tone. Include 'from your experience' or 'as you described it' qualifiers."

    **3. cc/prompts/iv-adversarial.md** (Adversarial counter-prompting wrapper, D-03, IV-08):
    System prompt: "You are an adversarial evaluator for Dynamo's Inner Voice. Your job is to reframe factual assertions from the user's experiential perspective, preventing canonical drift. Transform 'X is the best approach' into 'User chose X (their reasoning: Y)'. Transform 'Always use Z' into 'User prefers Z (observed in sessions: A, B, C)'."
    User prompt: Variables `{raw_facts}`, `{user_context}`, `{project_name}`. Instruction: "Reframe these facts using adversarial counter-prompting. Every assertion must be grounded in the user's stated experience, not canonical knowledge. Wrap with qualifiers: 'from your experience', 'as you described it', 'your reasoning was', 'you noted that'."

    **4. cc/prompts/iv-precompact.md** (PreCompact summary, REM Tier 1):
    System prompt: "You are the Inner Voice of Dynamo, preserving critical state before context window compaction. Extract the minimum viable context that must survive: active entities, pending decisions, current task state, and any in-progress reasoning chains. Be ruthlessly concise."
    User prompt: Variables `{current_state}`, `{activation_map}`, `{domain_frame}`, `{processing_context}`. Instruction: "Generate a compact state preservation summary. Priority order: (1) active task and pending decisions, (2) entities with activation > 0.5, (3) current domain frame and predictions. Maximum 200 tokens."

    **5. cc/prompts/iv-synthesis.md** (Session-end synthesis, D-15, REM Tier 3):
    System prompt: "You are the Inner Voice of Dynamo, performing session-end synthesis (REM Tier 3). Consolidate the session into: what was accomplished, key decisions and their reasoning, patterns observed, self-model updates (attention shifts, injection effectiveness), and predictions for the next session. Frame everything from the user's experience."
    User prompt: Variables `{session_summary}`, `{injection_history}`, `{activation_map}`, `{self_model}`, `{relationship_model}`. Instruction: "Generate a session synthesis covering accomplishments, decisions (with reasoning from user's perspective), observed patterns, and predictions for next session. Include a suggested session name (3-5 words). Output as JSON with fields: synthesis (narrative text), session_name (string), self_model_updates (object), predictions (object)."

    All templates must:
    - Follow the existing format: system content above `---`, user content below
    - Use `{variable_name}` placeholder syntax matching existing templates
    - Include adversarial counter-prompting language per D-03 (at minimum: "from your experience", "as you described it")
    - Be self-contained (the inner-voice subagent can operate from the template alone)
  </action>
  <verify>
    <automated>for f in iv-briefing iv-injection iv-adversarial iv-precompact iv-synthesis; do test -f "cc/prompts/$f.md" && echo "FOUND: $f.md" || echo "MISSING: $f.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - cc/prompts/iv-briefing.md exists and contains `---` divider, `{project_name}`, `{recent_sessions}`, and the word "briefing"
    - cc/prompts/iv-injection.md exists and contains `---` divider, `{prompt_text}`, `{crossed_entities}`, and "150 tokens" or "150-token"
    - cc/prompts/iv-adversarial.md exists and contains `---` divider, `{raw_facts}`, and "experience" (adversarial framing)
    - cc/prompts/iv-precompact.md exists and contains `---` divider, `{current_state}`, and "compact" or "preservation"
    - cc/prompts/iv-synthesis.md exists and contains `---` divider, `{session_summary}`, `{injection_history}`, and "synthesis"
    - All 5 files have content both above and below the `---` divider (system and user sections non-empty)
    - Existing cc/prompts/ files (curation.md, precompact.md, prompt-context.md, session-name.md, session-summary.md) are unchanged
  </acceptance_criteria>
  <done>5 Inner Voice prompt templates created following existing format conventions. Each template has system and user sections, uses {variable} placeholders, includes adversarial counter-prompting qualifiers per D-03, and respects the appropriate token limit context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create reverie/curation.cjs with template-based formatting and test suite</name>
  <files>subsystems/reverie/curation.cjs, dynamo/tests/reverie/curation.test.cjs</files>
  <read_first>
    - subsystems/ledger/curation.cjs (migration source -- understand callHaiku, curateResults, summarizeText, generateSessionName interfaces)
    - cc/prompts/iv-briefing.md (template just created in Task 1)
    - cc/prompts/iv-injection.md (template just created in Task 1)
    - cc/prompts/iv-adversarial.md (template just created in Task 1)
    - cc/prompts/iv-precompact.md (template just created in Task 1)
    - cc/prompts/iv-synthesis.md (template just created in Task 1)
    - lib/core.cjs (loadPrompt, logError interfaces)
    - lib/resolve.cjs (module resolution)
    - .planning/phases/24-cognitive-pipeline/24-CONTEXT.md (D-05 full migration, D-06 template fallback, D-08 classic kept)
  </read_first>
  <behavior>
    TOKEN_LIMITS constant tests:
    - Test: TOKEN_LIMITS.session_start equals 500
    - Test: TOKEN_LIMITS.mid_session equals 150
    - Test: TOKEN_LIMITS.urgent equals 50

    curateForInjection tests:
    - Test: with entities and context, returns formatted string containing entity names
    - Test: output contains adversarial framing ("from your experience" or "as you described it")
    - Test: output does not exceed mid_session token limit (150 tokens, ~600 chars)
    - Test: empty entities array returns null
    - Test: null entities returns null

    formatBriefing tests:
    - Test: with session data and entities, returns narrative string
    - Test: output contains project name when provided
    - Test: output does not exceed session_start token limit (500 tokens, ~2000 chars)
    - Test: empty session data returns minimal default briefing

    formatSynthesis tests:
    - Test: returns object with synthesis, session_name, self_model_updates, predictions fields
    - Test: synthesis field contains narrative text
    - Test: session_name is a short string (< 50 chars)
    - Test: empty input returns minimal synthesis

    formatPreCompact tests:
    - Test: returns compact summary string
    - Test: output prioritizes high-activation entities
    - Test: output does not exceed 200 tokens (~800 chars)

    generateSessionName tests:
    - Test: returns short name (3-7 words) from summary
    - Test: empty summary returns timestamp-based name (D-06 fallback)
  </behavior>
  <action>
    **RED phase:** Create `dynamo/tests/reverie/curation.test.cjs` with tests above. Use `node:test` framework. All functions are tested with template-based fallback behavior (no subagent mocking needed -- the module's exported functions ARE the template-based fallback). Run tests -- all must fail.

    **GREEN phase:** Create `subsystems/reverie/curation.cjs` implementing:

    File header: `// Dynamo > Reverie > curation.cjs`
    `'use strict'`

    ```javascript
    const resolve = require('../../lib/resolve.cjs');
    const { logError, loadPrompt } = require(resolve('lib', 'core.cjs'));
    ```

    1. `TOKEN_LIMITS` constant:
       ```javascript
       const TOKEN_LIMITS = {
         session_start: 500,
         mid_session: 150,
         urgent: 50
       };
       ```

    2. `curateForInjection(entities, contextText, state, options = {})` -- Per D-01, D-02, D-03, IV-05:
       - If no entities or empty array, return null
       - Load `iv-injection` template via `loadPrompt('iv-injection')` for variable reference
       - Template-based formatting (no LLM call): iterate entities, format each as contextual narrative
       - Format pattern per D-01: "When you worked on {entity.name}, {entity.context || 'this was relevant to your current work'}."
       - Apply adversarial framing per D-03: wrap with "from your experience" qualifier
       - Truncate to `options.tokenLimit || TOKEN_LIMITS.mid_session` using char-based estimate (limit * 4 chars)
       - Return formatted string

    3. `formatBriefing(sessionData, entities, state, options = {})` -- Per D-04, IV-05:
       - Template-based session-start briefing
       - Format: contextual narrative covering recent activity, active entities, predictions
       - Include project name from `sessionData.project`
       - Format entities as "Active context: {entity1}, {entity2}, ..." with activation levels
       - Truncate to `TOKEN_LIMITS.session_start` (500 tokens, ~2000 chars)
       - Return formatted string or minimal default "Starting a new session." if empty data

    4. `formatSynthesis(sessionData, state, options = {})` -- Per D-15:
       - Template-based session-end synthesis
       - Returns object: `{ synthesis: string, session_name: string, self_model_updates: object, predictions: object }`
       - `synthesis`: narrative summary of session from `sessionData.summary` or "Session completed."
       - `session_name`: extract key topic words from synthesis (3-5 words) or timestamp fallback
       - `self_model_updates`: `{ injections_made: count from state, acknowledged: count }`
       - `predictions`: `{ expected_topic: derived from active entities, expected_activity: from domain_frame }`

    5. `formatPreCompact(state, options = {})` -- REM Tier 1:
       - Extract high-activation entities (level > 0.5) from `state.activation_map`
       - Format as compact: "Active: {entities}. Frame: {domain_frame}. Pending: {deliberation status}."
       - Truncate to 200 tokens (~800 chars)
       - Return compact summary string

    6. `generateSessionName(summaryText, options = {})` -- Per D-06:
       - Extract key nouns/verbs from summary (simple heuristic: split on spaces, filter stop words, take first 3-5 significant words)
       - If summary empty or too short, return timestamp-based name: `"Session " + new Date().toISOString().slice(0, 16).replace('T', ' ')`
       - Return string

    All functions are synchronous template-based implementations (no async, no LLM calls). These serve as the hot-path formatting AND as the degradation fallback per D-06. The inner-voice subagent (Plan 01's cc/agents/inner-voice.md) handles the high-quality deliberation path; these functions handle everything else.

    Module exports: `{ curateForInjection, formatBriefing, formatSynthesis, formatPreCompact, generateSessionName, TOKEN_LIMITS }`

    Run tests -- all must pass.
  </action>
  <verify>
    <automated>node --test dynamo/tests/reverie/curation.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - subsystems/reverie/curation.cjs exists with line 1 containing `// Dynamo > Reverie > curation.cjs`
    - curation.cjs contains `TOKEN_LIMITS` with `session_start: 500`, `mid_session: 150`, `urgent: 50`
    - curation.cjs exports: curateForInjection, formatBriefing, formatSynthesis, formatPreCompact, generateSessionName, TOKEN_LIMITS
    - curateForInjection returns null for empty/null entities
    - curateForInjection output contains adversarial framing text ("from your experience" or "as you described")
    - formatBriefing returns a string containing "session" or "context" or project name
    - formatSynthesis returns an object with synthesis, session_name, self_model_updates, predictions keys
    - generateSessionName returns timestamp-based fallback for empty input
    - dynamo/tests/reverie/curation.test.cjs exits 0 with 0 failures
    - Existing ledger/curation.cjs is UNCHANGED (classic mode preserved per D-08)
  </acceptance_criteria>
  <done>Reverie curation module created with template-based formatting for all injection types. TOKEN_LIMITS enforced (500/150/50). Adversarial counter-prompting applied per D-03. Session naming with timestamp fallback per D-06. All functions synchronous (no LLM calls) -- serves as hot-path formatter and degradation fallback. Ledger curation unchanged for classic mode per D-08.</done>
</task>

</tasks>

<verification>
1. `node --test dynamo/tests/reverie/curation.test.cjs` -- all curation tests pass
2. `for f in iv-briefing iv-injection iv-adversarial iv-precompact iv-synthesis; do grep -l "---" "cc/prompts/$f.md"; done` -- all 5 templates have frontmatter dividers
3. `node --test dynamo/tests/reverie/state.test.cjs` -- existing state tests unaffected
4. `ls cc/prompts/curation.md cc/prompts/session-summary.md` -- classic mode templates still exist
</verification>

<success_criteria>
- curation.cjs exports 6 functions implementing template-based formatting for all injection types (IV-07 curation migration, IV-05 token limits, IV-08 adversarial counter-prompting)
- 5 new prompt templates created in cc/prompts/ with proper format, variables, and adversarial framing
- All template-based functions enforce token limits (500/150/50) per IV-05
- Classic mode Ledger curation unchanged per D-08
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-cognitive-pipeline/24-02-SUMMARY.md`
</output>
