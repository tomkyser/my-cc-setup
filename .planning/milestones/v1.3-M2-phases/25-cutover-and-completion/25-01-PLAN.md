---
phase: 25-cutover-and-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cc/hooks/dynamo-hooks.cjs
  - lib/config.cjs
  - subsystems/terminus/stages.cjs
  - subsystems/ledger/curation.cjs
  - subsystems/ledger/hooks/session-start.cjs
  - subsystems/ledger/hooks/prompt-augment.cjs
  - subsystems/ledger/hooks/session-summary.cjs
  - subsystems/ledger/hooks/preserve-knowledge.cjs
  - subsystems/ledger/hooks/capture-change.cjs
  - cc/prompts/curation.md
  - cc/prompts/precompact.md
  - cc/prompts/prompt-context.md
  - cc/prompts/session-name.md
  - cc/prompts/session-summary.md
  - dynamo.cjs
  - dynamo/tests/config.test.cjs
  - dynamo/tests/ledger/dispatcher.test.cjs
  - dynamo/tests/ledger/curation.test.cjs
  - dynamo/tests/switchboard/stages.test.cjs
  - dynamo/tests/switchboard/install.test.cjs
autonomous: true
requirements: [FLAG-02]

must_haves:
  truths:
    - "Dispatcher always routes cognitive events to Reverie handlers regardless of config"
    - "No OpenRouter/Haiku code path exists anywhere in the codebase"
    - "reverie.mode config key is not validated or used"
    - "Classic Ledger hooks and curation module are removed"
    - "Classic prompt templates (5 files) are deleted"
    - "Session backfill uses Reverie generateSessionName"
    - "All tests pass after classic removal"
  artifacts:
    - path: "cc/hooks/dynamo-hooks.cjs"
      provides: "Simplified dispatcher with two branches (subagent vs Reverie)"
      contains: "JSON_OUTPUT_EVENTS.has(event)"
    - path: "lib/config.cjs"
      provides: "VALIDATORS map without reverie.mode"
    - path: "dynamo.cjs"
      provides: "Session backfill using reverie/curation.cjs"
      contains: "resolve('reverie', 'curation.cjs')"
  key_links:
    - from: "cc/hooks/dynamo-hooks.cjs"
      to: "subsystems/reverie/handlers/"
      via: "always-route (no mode check)"
      pattern: "REVERIE_ROUTE\\[event\\]"
    - from: "dynamo.cjs"
      to: "subsystems/reverie/curation.cjs"
      via: "session backfill import"
      pattern: "resolve\\('reverie', 'curation.cjs'\\)"
---

<objective>
Remove the classic Ledger curation path entirely, making Reverie the only processing pipeline. Eliminate mode switching, OpenRouter dependency, classic prompt templates, and dead Ledger hook handlers.

Purpose: Clean break from the dual-path architecture. Per user decision: "I want to immediately switch to the new mode and drop classic."
Output: Simplified dispatcher, cleaned config, removed dead code, passing test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cutover-and-completion/25-CONTEXT.md
@.planning/phases/25-cutover-and-completion/25-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From cc/hooks/dynamo-hooks.cjs (lines 118-222):
Current three-branch dispatcher:
1. `if (mode !== 'classic' && !JSON_OUTPUT_EVENTS.has(event))` -> Reverie routing
2. `else if (JSON_OUTPUT_EVENTS.has(event))` -> Subagent routing (SubagentStart/SubagentStop)
3. `else` -> Classic Ledger routing (BEING REMOVED)

Target two-branch dispatcher:
1. `if (JSON_OUTPUT_EVENTS.has(event))` -> Subagent routing (unchanged)
2. `else` -> Reverie routing for all cognitive events

From lib/config.cjs (line 14):
```javascript
const VALIDATORS = {
  'reverie.mode': (v) => ['classic', 'hybrid', 'cortex'].includes(v),  // REMOVE THIS LINE
  'reverie.operational.subagent_daily_cap': (v) => typeof v === 'number' && v > 0 && v <= 100,
  // ... remaining validators stay
};
```

From subsystems/reverie/curation.cjs (line 249):
```javascript
function generateSessionName(summaryText, options = {})
```
This replaces ledger/curation.cjs generateSessionName for session backfill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove classic mode from dispatcher, config, and health checks</name>
  <files>
    cc/hooks/dynamo-hooks.cjs,
    lib/config.cjs,
    subsystems/terminus/stages.cjs,
    dynamo.cjs
  </files>
  <read_first>
    cc/hooks/dynamo-hooks.cjs,
    lib/config.cjs,
    subsystems/terminus/stages.cjs,
    dynamo.cjs,
    subsystems/reverie/curation.cjs
  </read_first>
  <action>
    **cc/hooks/dynamo-hooks.cjs** (per D-05):
    1. Remove line 120: `const mode = (config.reverie && config.reverie.mode) || 'classic';`
    2. Remove the entire `const config = loadConfig();` on line 119 (no longer needed for mode check -- if loadConfig is used elsewhere in the file, keep it; if it is only used for mode, remove)
    3. Restructure the three-branch conditional into two branches:
       - Branch 1: `if (JSON_OUTPUT_EVENTS.has(event))` -- SubagentStart/SubagentStop routing (lines 156-176, unchanged)
       - Branch 2: `else` -- All other cognitive events route to Reverie handlers (lines 122-155, but remove the `mode !== 'classic' &&` condition)
    4. Delete lines 177-222 (the classic `else` branch with Ledger switch/case)
    5. Remove any `loadConfig` import if it was only used for mode check (verify by grepping file)

    **lib/config.cjs** (per D-06):
    1. Remove line 14: `'reverie.mode': (v) => ['classic', 'hybrid', 'cortex'].includes(v),`
    2. Keep all other validators unchanged

    **subsystems/terminus/stages.cjs** (per D-04):
    1. At line 145, remove `const hasOpenRouter = !!process.env.OPENROUTER_API_KEY;`
    2. Remove all logic paths that check or report on OPENROUTER_API_KEY (lines 145-156, 175)
    3. The env vars stage should check only NEO4J_PASSWORD (and any other non-OpenRouter vars)
    4. Remove 'OPENROUTER_API_KEY' from the requiredKeys array at line 175

    **dynamo.cjs** (per D-04, D-06):
    1. At line 398, change `require(resolve('ledger', 'curation.cjs'))` to `require(resolve('reverie', 'curation.cjs'))`
    2. The function signature is identical: `generateSessionName(summaryText, options)` -- no other changes needed for the backfill call
    3. At line 135, update config help text: remove the `reverie.mode` examples. Replace with:
       ```
       'config': 'Usage: dynamo config <get|set> <key> [value]\n  Get or set configuration values using dot notation.\n  Examples:\n    dynamo config get reverie.activation.sublimation_threshold\n    dynamo config set reverie.activation.sublimation_threshold 0.5\n    dynamo config set reverie.operational.subagent_daily_cap 30',
       ```
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && grep -c "reverie.mode" cc/hooks/dynamo-hooks.cjs lib/config.cjs dynamo.cjs && echo "FAIL: reverie.mode references remain" || echo "PASS: no reverie.mode references" && grep -c "OPENROUTER" subsystems/terminus/stages.cjs && echo "FAIL: OPENROUTER references remain" || echo "PASS: no OPENROUTER references" && grep -c "resolve('reverie', 'curation.cjs')" dynamo.cjs && echo "PASS: backfill redirected"</automated>
  </verify>
  <acceptance_criteria>
    - cc/hooks/dynamo-hooks.cjs does NOT contain the string `reverie.mode` or `|| 'classic'`
    - cc/hooks/dynamo-hooks.cjs does NOT contain `resolve('ledger', 'hooks')` or `case 'SessionStart':` inside a classic routing block
    - cc/hooks/dynamo-hooks.cjs contains exactly two conditional branches: `if (JSON_OUTPUT_EVENTS.has(event))` and `else`
    - lib/config.cjs does NOT contain `'reverie.mode'` in the VALIDATORS map
    - subsystems/terminus/stages.cjs does NOT contain `OPENROUTER` anywhere
    - dynamo.cjs line ~398 contains `resolve('reverie', 'curation.cjs')` instead of `resolve('ledger', 'curation.cjs')`
    - dynamo.cjs help text for 'config' does NOT contain `reverie.mode`
  </acceptance_criteria>
  <done>Dispatcher always routes to Reverie, config validator removed, health check no longer checks OpenRouter, session backfill uses Reverie curation</done>
</task>

<task type="auto">
  <name>Task 2: Delete classic dead code and update all affected tests</name>
  <files>
    cc/prompts/curation.md,
    cc/prompts/precompact.md,
    cc/prompts/prompt-context.md,
    cc/prompts/session-name.md,
    cc/prompts/session-summary.md,
    subsystems/ledger/curation.cjs,
    subsystems/ledger/hooks/session-start.cjs,
    subsystems/ledger/hooks/prompt-augment.cjs,
    subsystems/ledger/hooks/session-summary.cjs,
    subsystems/ledger/hooks/preserve-knowledge.cjs,
    subsystems/ledger/hooks/capture-change.cjs,
    dynamo/tests/config.test.cjs,
    dynamo/tests/ledger/dispatcher.test.cjs,
    dynamo/tests/ledger/curation.test.cjs,
    dynamo/tests/switchboard/stages.test.cjs,
    dynamo/tests/switchboard/install.test.cjs
  </files>
  <read_first>
    cc/prompts/curation.md,
    subsystems/ledger/curation.cjs,
    subsystems/ledger/hooks/session-start.cjs,
    subsystems/ledger/hooks/prompt-augment.cjs,
    subsystems/ledger/hooks/session-summary.cjs,
    subsystems/ledger/hooks/preserve-knowledge.cjs,
    subsystems/ledger/hooks/capture-change.cjs,
    dynamo/tests/config.test.cjs,
    dynamo/tests/ledger/dispatcher.test.cjs,
    dynamo/tests/ledger/curation.test.cjs,
    dynamo/tests/switchboard/stages.test.cjs,
    dynamo/tests/switchboard/install.test.cjs
  </read_first>
  <action>
    **Delete 5 classic prompt templates** (per D-03):
    Delete these files entirely using fs/rm:
    - `cc/prompts/curation.md`
    - `cc/prompts/precompact.md`
    - `cc/prompts/prompt-context.md`
    - `cc/prompts/session-name.md`
    - `cc/prompts/session-summary.md`
    Only the 5 iv-* templates should remain in cc/prompts/.

    **Delete Ledger curation module** (per D-01, D-04):
    Delete `subsystems/ledger/curation.cjs` entirely. This removes callHaiku, curateResults, summarizeText, and the Ledger version of generateSessionName.

    **Delete 5 Ledger hook handlers** (per D-05, avoiding Pitfall 3):
    Delete the entire `subsystems/ledger/hooks/` directory and all 5 files:
    - `session-start.cjs`
    - `prompt-augment.cjs`
    - `session-summary.cjs`
    - `preserve-knowledge.cjs`
    - `capture-change.cjs`
    These are dead code since the dispatcher no longer routes to them. Deleting them avoids the require() breakage described in Pitfall 3.

    **Update tests** (per Pitfall 5):

    `dynamo/tests/config.test.cjs`:
    - Remove all `reverie.mode` tests (get, set, validate for classic/hybrid/cortex values)
    - Add a test confirming `reverie.mode` is NOT in VALIDATORS: `it('reverie.mode is not a validated key', () => { assert.strictEqual(config.validate('reverie.mode', 'anything'), null); });`
    - Keep all other validator tests (subagent_daily_cap, sublimation_threshold, etc.)

    `dynamo/tests/ledger/dispatcher.test.cjs`:
    - Remove tests: 'dispatcher source reads reverie.mode from config', 'dispatcher source defaults mode to classic', 'dispatcher preserves classic switch/case in else block'
    - Add test: `it('dispatcher always routes cognitive events to Reverie', () => { const src = fs.readFileSync(dispatcherPath, 'utf8'); assert.ok(!src.includes("|| 'classic'"), 'no classic fallback'); assert.ok(!src.includes("resolve('ledger', 'hooks')"), 'no Ledger hook routing'); assert.ok(src.includes("REVERIE_ROUTE[event]"), 'routes to Reverie handlers'); });`

    `dynamo/tests/ledger/curation.test.cjs`:
    - Delete the entire test file (the module it tests is deleted). Or if there are tests for reverie curation here, keep only those.

    `dynamo/tests/switchboard/stages.test.cjs`:
    - Remove all tests that check for OPENROUTER_API_KEY
    - Keep all other stage tests

    `dynamo/tests/switchboard/install.test.cjs`:
    - Remove any assertions about `config.curation` section in generated config
    - Update generateConfig tests to verify the curation key is NOT present in the output

    After all changes, run the full test suite to verify.
  </action>
  <verify>
    <automated>cd "/Users/tom.kyser/Library/Mobile Documents/com~apple~CloudDocs/dev/my-cc-setup" && node --test dynamo/tests/*.test.cjs dynamo/tests/ledger/*.test.cjs dynamo/tests/switchboard/*.test.cjs dynamo/tests/reverie/*.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `cc/prompts/curation.md` does NOT exist
    - File `cc/prompts/precompact.md` does NOT exist
    - File `cc/prompts/prompt-context.md` does NOT exist
    - File `cc/prompts/session-name.md` does NOT exist
    - File `cc/prompts/session-summary.md` does NOT exist
    - File `subsystems/ledger/curation.cjs` does NOT exist
    - Directory `subsystems/ledger/hooks/` does NOT exist (or is empty)
    - Files `cc/prompts/iv-adversarial.md`, `cc/prompts/iv-briefing.md`, `cc/prompts/iv-injection.md`, `cc/prompts/iv-precompact.md`, `cc/prompts/iv-synthesis.md` still exist
    - `dynamo/tests/config.test.cjs` does NOT contain `reverie.mode`
    - `dynamo/tests/ledger/dispatcher.test.cjs` does NOT contain `classic` mode assertions
    - `dynamo/tests/switchboard/stages.test.cjs` does NOT contain `OPENROUTER`
    - Full test suite exits 0 with all tests passing
  </acceptance_criteria>
  <done>All classic-mode dead code deleted, all tests updated and passing, only Reverie artifacts remain</done>
</task>

</tasks>

<verification>
1. `grep -r "reverie.mode" --include="*.cjs" --include="*.md" .` returns zero matches (excluding .planning/ docs)
2. `grep -r "OPENROUTER" --include="*.cjs" .` returns zero matches
3. `grep -r "callHaiku" --include="*.cjs" .` returns zero matches
4. `ls cc/prompts/` shows only iv-* files (5 files)
5. `node --test dynamo/tests/*.test.cjs dynamo/tests/ledger/*.test.cjs dynamo/tests/switchboard/*.test.cjs dynamo/tests/reverie/*.test.cjs` exits 0
</verification>

<success_criteria>
- Reverie is the only processing pipeline -- no mode switching exists
- Zero references to OpenRouter, callHaiku, classic mode in production code
- Five classic prompt templates deleted, five iv-* templates preserved
- Session backfill uses Reverie's deterministic generateSessionName
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/25-cutover-and-completion/25-01-SUMMARY.md`
</output>
